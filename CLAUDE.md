# 스킬 크래프트 배틀 — 프로젝트 규칙 (Claude Code 필독)

이 파일은 Claude Code가 세션마다 자동으로 읽는 프로젝트 지침이다.
모든 담당 에이전트(PM · 코디네이터 · 디자인 · 기획/밸런스 · 개발 · QA)는 아래 규칙을 지킨다.

## 게임 한 줄 요약
유저가 그림판에서 직접 스킬을 그려 QWER 4칸에 세팅하고, 5:5로 겨루는 3인칭 실시간 대전 게임.
자세한 기획은 `docs/게임기획.md` 참고. 엔진은 Godot 4, **3D 3인칭**(프로토타입부터).
움직임은 멧챠 카멜레온풍, 이동은 방향키 전용. **배그식 조준·스프린트는 넣지 않는다.**

## 깃(Git) 규칙 — 작업을 절대 잃지 않기
1. 자주 커밋한다. 의미 있는 작업 한 덩이마다 `git add -A && git commit`.
2. 커밋 메시지는 `[역할] 무엇을 했는지` 형식.
3. 작업이 끝나면 `git push`. main 브랜치 사용.
4. 컨텍스트(토큰)가 차오르기 전에 반드시 체크포인트 커밋을 한다.
5. 자동 저장 훅이 `.claude/settings.json`에 있다: 서브에이전트 종료 / 컨텍스트 정리 직전 / 세션 종료 시 `.claude/checkpoint.ps1`이 실행되어 자동 커밋·푸시한다.

## STATUS.md 규칙 — 진행 현황판
- 루트의 `STATUS.md`는 항상 최신 상태 유지. 이 한 장으로 전체 진행 파악.
- 갱신 책임자: 코디네이터. `reports/` 메모를 읽고 종합한다.
- 담을 내용: 완료 / 진행 중+담당 / 다음 할 일 / 막힌 것·결정 대기.

## 에이전트 협업 규칙
- 각 담당은 진행을 `reports/<역할>.md`에 남긴다.
- 코디네이터가 그걸 모아 `STATUS.md`로 정리한다.
- PM은 STATUS.md를 보고 다음 우선순위를 정해 분배한다.

## 🛡 기획 이탈 방지 — 가장 중요한 규칙
2026-07-31, 한 세션이 기획서를 확대해석해 게임을 통째로 배그식 슈터로 만들고
프로토타입 시점까지 임의로 갈아엎는 사고가 있었다. 다시 없게 한다.

1. **기획서에 없는 기능은 넣지 않는다.** 넣고 싶으면 먼저 사용자에게 묻는다.
   "있으면 좋을 것 같아서"는 사유가 아니다.
2. **기획서가 다른 게임을 언급하면, 그 비유가 걸린 범위만 가져온다.**
   예) `배그처럼 안전구역 축소` → 안전구역에만 해당. 조작·카메라·전투는 무관.
3. **`✅ 결정됨` 항목은 사용자만 바꾼다.** 방향을 틀어야 할 것 같으면 작업을 멈추고
   물어본다. 코드를 먼저 바꾸고 문서를 나중에 맞추는 것은 금지다.
4. **로드맵 단계를 건너뛰지 않는다.** 특히 3단계는 3a → 3b 순서.
5. **작업 묶음이 끝나면 `auditor`(감시자) 에이전트로 검사한다.**
   판정이 `반려`면 커밋하지 말고 되돌리거나 사용자에게 확인받는다.
   검사 결과는 `reports/auditor.md`에 `[감시]` 커밋으로 남는다.

## 코딩 규칙
- Godot 4 / GDScript. 이름은 목적이 드러나게. 큰 기능은 작게 쪼개 각각 커밋.
