# 프로젝트 진행 현황판 (STATUS)

> 코디네이터가 관리. 각 reports/*.md를 종합해 최신화. 이 한 장으로 전체 진행 파악.
> 최종 갱신: 2026-07-31 (3b + QA 1차 + 밸런스 재조정 + 아트 1차까지 반영)

## 📌 다음 세션에서 가장 먼저 할 일

### 1️⃣ **사용자가 직접 플레이해서 "재미있는가"를 확인한다** — 다른 모든 것보다 먼저
기획서 87~88행은 **「3a 가 돌아가고 재미가 확인된 뒤에 3b 를 붙인다」** 였는데,
사용자 승인으로 이 게이트를 건너뛰고 3b(그림 반영)까지 왔다. **뒤늦게라도 확인이 필요하다.**
지금은 코드·수치는 다 맞는데 **"재미있는지"를 아무도 확인한 적이 없는 상태**다.
QA 도 「재미 평가: 지금 단계에서는 판단 불가」라고 적었다(치명 버그 때문 — 그 버그들은 이후 고쳤다).

실행: `게임 열기.cmd` → **F5**. 아래를 순서대로 보고 느낀 걸 그대로 말해주면 된다.

- **① 그리는 게 재미있는가** (이게 이 게임의 전부다)
  - Tab → 제작창 → 그림을 그린다. **원 / 세로 막대 / 점 여러 개 / 별** 을 각각 그려보고
    태그(둥긂·길쭉함·뾰족함·흩어짐)가 **내가 그린 대로 나오는가**. 엉뚱하게 나오는 게 있나?
  - 그린 그림이 **그대로 투사체 모양으로 날아가는가** (3b 의 핵심. 그림=겉모습)
  - **공들여 그릴 마음이 드는가?** 아니면 "대충 찍어도 되네" 싶은가 →
    후자면 아래 「⛔ 잘 그릴 이유가 없다」가 실제 문제라는 뜻이다. 꼭 알려달라.
- **② 쏘는 손맛**
  - Q 를 눌러 허수아비를 쏴본다. 발동 0.31초 → 발사 → **쿨타임 2.01초**
    (기획이 4.94초 → 2.01초로 줄였다. 이제 적당한가? 아직 답답한가?)
  - 데미지·범위 슬라이더를 바꿔 저장 → 다시 쏜다. **세면 답답하고 약하면 시원한** 그 교환이 재미있는가?
  - 태그별로 체감이 다른가 — 둥긂(구 1발) / 길쭉함(창) / 뾰족함(3발 부챗살) / 흩어짐(5발 산탄)
- **③ 움직임** — 멧챠 카멜레온풍인가? (`move_speed 6.0` / `jump_height 1.2`)
  방향키 이동 + 스페이스 점프. 화면은 고정각이라 안 돈다.
- **④ 새로 바뀐 그림(아트) 확인** — 캐릭터가 **2등신 마법사**로, 아레나가 **거대한 원룸**(침대·책상·
  머그컵·연필…)으로 바뀌었다. 원룸으로 읽히는가? 캐릭터가 귀여운가?
  - 디자인 담당이 QA 에 확인 요청한 것: **의자 등받이 옆을 지날 때 카메라가 확 당겨지는지**
    (불편하면 등받이를 낮추면 된다) / **책 3권(1.1m)을 밟고 침대 위로 올라가지는지**

### 2️⃣ 「⛔ 막힌 것 / 결정 대기」의 **6가지를 결정한다**
전부 사용자만 정할 수 있는 것들이고, 여기가 막히면 개발·기획이 다음 단계로 못 간다.

### 3️⃣ 미커밋 작업물부터 저장한다 ⚠
`git log` 최신 커밋은 `771d0eb [개발] 3a` 인데, **3b · 아트 재작업 · 밸런스 재조정 ·
QA 버그 수정 5건 · 리포트 3개가 전부 커밋되지 않은 상태**로 확인됐다(코디 확인 시점 기준).
날리면 이 세션 전체가 사라진다. 먼저 커밋·푸시할 것.

---

## ✅ 완료

### 이번 세션 (2026-07-31)

- **🎨 로드맵 3b: 그린 그림이 그대로 날아간다** (개발)
  - `Projectile.gd` 가 32×32 마스크를 `Image`(RGBA8) → `Sprite3D` 로 띄운다. 칠한 픽셀만 스킬 색,
    나머지 투명. `NEAREST` 필터로 픽셀 느낌, 빌보드라 고정각 카메라에서 항상 정면.
    빈 그림이면 예전처럼 밋밋한 구. (기획서 35행 "원본 그림을 투사체 스프라이트로 그대로 사용")
  - **판정은 태그가 고른 프리셋**이지 픽셀 모양이 아니다. `Player.gd` 가 저장된 태그를 믿지 않고
    `Balance.tag_from_mask()` 로 **매번 다시 뽑는다** → 기획이 임계값을 바꾸면 기존 스킬에도 즉시 반영
  - 히트박스 4종 완성: 둥긂=구(1발) / 길쭉함=캡슐(1발) / 뾰족함=부챗살(3발 ÷3) / 흩어짐=산탄(5발 ÷5)
  - 검증(헤드리스): 그림 4종 → 태그 판정 → 장착 → 발사 → 명중까지 전부 확인
  - ⚠ 사용자 승인으로 「3a 재미 확인」 게이트를 건너뛰고 붙였다 → 위 📌 1번이 그 빚을 갚는 일이다

- **🐞 QA 가 잡은 치명 버그 5건 수정** (개발, 코드에서 확인함)
  | 번호 | 증상 | 조치 |
  |---|---|---|
  | S1 🔴 | **Q·R 이 한 발도 안 나갔다** — 총구(y0.749)보다 판정 반경이 크면 태어나자마자 바닥과 겹쳐 자폭 | `_spawn_point()` 가 `반경 + GROUND_CLEARANCE` 만큼 최소 높이를 보장 |
  | S2 🔴 | 부채꼴이 판정각 90°를 그대로 조준 퍼짐으로 써서 10m 앞 바깥 탄이 10m 옆으로 감 | `CONE_SPREAD_DEG = 11.0` 신설 — 판정 각도와 퍼짐각 분리 |
  | S4 🟠 | 캡슐 절반이 등 뒤에 있어 **뒤 5m 의 적이 맞았다** | `col.position.z = -height*0.5` — 원점 기준 앞쪽으로 이동 |
  | S7 🟡 | 쓰러진 허수아비가 탄을 막는 "시체 방패" | `take_damage` 가 bool 반환 → false 면 투사체가 통과 |
  | S9 🔵 | NaN 이 들어가면 `_casting` 이 영영 안 풀려 스킬 영구 잠김 | `Player._safe_time()` 가드 (NaN/무한대/음수 → 기본값) |
  - ⚠ **재검증이 안 됐다.** 위 5건은 코드상 반영을 확인했을 뿐, 고친 뒤 QA 실측을 다시 돌리지 않았다

- **⚖ 밸런스 재조정 + 예산 초과 정책 결정** (기획) — 전부 10,000 조합 전수 검산 근거 있음
  - **예산 초과 = ③ 초과분 페널티로 결정.** 저장·발사는 막지 않고
    `쿨타임 ×(1+초과율×3.0)` / `발동시간 ×(1+초과율×1.5)`.
    - ①저장 차단을 안 고른 결정적 이유: `데미지100 + 최소범위1 = 100.6pt` 라 **무조건 초과** →
      막으면 기획서 8행이 명시한 데미지 100 이 영영 도달 불가능한 UI 가 된다
    - 계수 3.0 검산: 페널티 없으면 초과 조합이 **2배 이득**(21.72 vs 11.24). 2.0 에서 뒤집히지만
      마진이 없어 3.0 채택 → **초과가 이득인 조합 0개.** 절벽도 없다(100.2pt 는 페널티 0.6%뿐)
    - QA 실측 확인: 비용 160 → 쿨 25.20초/발동 2.09초. 페널티 확실히 작동
  - **🐞 쿨타임 곡선 교정** (불꽃탄 4.94s → **2.01s**). `COOLDOWN_MIN 1.5→0.6` / `MAX 12.0→9.0` /
    **`COOLDOWN_CURVE 1.6` 신설**. 기존 공식은 쿨이 예산에 정비례라 **약한 스킬이 일방적으로 손해**였다
    (DPS 최대/최소 비 **2.79배 → 1.52배**). 지수 2.0 은 중간 데미지가 최강이 되는 역전이 생겨 1.6 채택
    - 기본 4개: 불꽃탄 4.94→**2.01s** / 관통창 8.12→**4.61s** / 산탄 6.04→**2.79s** / 대폭발 11.87→**8.84s**
  - **🐞 길쭉함 캡슐 길이 폭주 차단** — 기획서 24행이 "얇은 선으로 맵 관통 악용 원천 차단"이라 못박았는데
    코드는 폭 0.45m 고정 + `길이 = 면적÷0.45` 라 **범위 100 에서 길이 26.7m 캡슐**이 나왔다.
    `길이 = 3.0×√면적`, `폭 = 면적÷길이` 로 교체 → 어느 범위에서든 같은 9:1 창 모양, 길이 10.4m 로 억제
  - **🐞 손으로 그린 원이 부채꼴로 판정되던 문제** — 복잡도는 `둘레²`라 손떨림에 2차로 반응한다.
    `T_COMPLEXITY_SHARP 2.2→2.5` + **`T_FILL_SHARP 0.50` 신설(AND 조건)**.
    떨림 원 5.22 와 별 5.49 는 복잡도만으론 분리 불가 — 채움률과 AND 로 묶어야만 갈린다.
    기본 스킬 4개 태그는 이 변경으로 안 바뀜(검산 완료)
  - **그림 커스텀 흥미 강화안 설계** — 안전 규칙 3개(면적 보존 / 제로섬 / 정규화된 지표만)와
    우선순위 5단계. 상수는 `Balance.gd` 에 **이미 확정**돼 있고 배선만 남았다(→ D-5)
  - 안 건드린 것: `✅ 결정됨` 항목 전부 / 기존 함수명·반환 키 전부(추가만 함) /
    `docs/게임기획.md` 와 `Balance.gd` 외 어떤 파일도 안 열었다

- **🎨 아트 1차: 임시 도형 → 실제 디자인** (디자인, `Player.tscn` · `Arena.tscn` 두 파일만)
  - **캐릭터 = 2등신 마법사.** 흰 캡슐+검은 박스 → 머리·몸통·팔다리·발·고깔모자·노란 목도리·
    망토깃·튀어나온 눈·마법봉. -32° 부감에서는 머리가 화면 대부분이라 2등신이라야 "사람"으로 읽힌다
    - 마법봉을 위로 32° 젖힌 이유: 안 젖히면 **땅을 가리키는 것처럼** 보인다(실제 렌더로 확인)
    - 색은 파랑 2톤 + 목도리 노랑만. 투사체 색을 유저가 정하므로 캐릭터가 색을 먹으면 이펙트가 안 읽힌다
    - **목도리 = 나중에 팀 구분색으로 쓸 자리**로 비워 뒀다
  - **아레나 = 「거대한 원룸 한 칸」**(기획서 10행). 상자 3개 → 침대·책 3권·책상·의자·머그컵·
    방석·택배상자·연필·창문·문·걸레받이. 방 60m ÷ 캐릭터 1.7m ≒ 6m 방 안의 17cm 사람 비율
    - 가구마다 전투 의도가 있다: 책 3권(1.1m×3) = **유일한 고지대 진입로**, 책상 밑 = 통로+지붕,
      의자 등받이 = 남북 시야 차단벽, 머그컵 = 돌면서 싸우는 원기둥 엄폐
    - **창문을 서쪽에 둔 이유**: 카메라 yaw 0° 고정이라 북쪽 벽은 언제나 배경 —
      거기 밝은 창을 두면 투사체를 잡아먹는다
    - 가운데(러그 위)는 교전용으로 비워 뒀다
  - 배경은 채도를 죽인 중간 톤. 실패한 조합 3종을 리포트에 기록해 뒀다(다시 밟지 말 것)
  - 검증: `--headless --import` 오류 0 / 실제 부팅 오류 0 / `--write-movie` 로 3방향 렌더 육안 확인

- **🔍 QA 1차 전면 검증** (헤드리스 실측 400발 이상) — `reports/qa.md`
  - S1~S12 보고. 그중 **S1·S2·S4·S7·S9 는 개발이 수정 완료**, 나머지 7건은 미수정(아래 참조)
  - 문제 없던 것도 실측으로 확인: 투사체 누수 0 / 벽 충돌 정상 / 사거리 소진 소멸 정확 /
    **태그별 명목 총 데미지 4종 전부 정확히 60.00, 오차 0** / 총 판정 면적 정규화 오차 0 /
    저장·불러오기 왕복 정확 / 창 열린 채 Q 발사 0발 / 허수아비 오버킬·부활 정상
  - 다음 QA 세션용 방법 메모를 리포트 말미에 남겼다(헤드리스 하네스 함정 포함)

### 이전 세션

- **🎚 로드맵 3a: 에디터 수치가 실제 발사에 반영** (2026-07-31)
  - `Player.gd` 가 `SkillDB.get_slot("Q")` 에서 이름·데미지·범위·색을 읽는다. 하드코딩은 `fallback_*`
  - `SkillDB.slot_changed` 구독 → 제작창에서 **저장하면 다음 발사부터 바로 반영**
  - (당시 "그림은 3b 예정" 안내 문구는 3b 에서 걷어냈다 — 이제 사실이 아니라서)
- **🔫 로드맵 2단계: 고정 스킬 1개 발사** (2026-07-31) — 크래시 세션이 건너뛴 자리를 되돌아와 채움
  - `Projectile.gd`(새 파일) — 속도·사거리·판정 반경·데미지를 스스로 정하지 않고 `Balance.derive()` 결과를 받아 씀
  - `Dummy.gd`(새 파일) — 허수아비 3개. 봇이 아니다(움직이지도 반격하지도 않음)
  - `Player.gd` — Q → 발동시간 → 발사 → 쿨타임. 셋 다 Balance 가 정한 값
  - `Player.tscn` — 마법봉 추가(기획서 12행). 투사체는 봉 끝에서 나간다
  - 🐞 **캐릭터가 뒤를 보고 달렸다** — `_face()` 가 몸의 +Z 를 진행 방향에 맞췄는데 Godot 의 앞은 -Z
  - 🐞 **Godot 함정**: `Projectile` 에 `monitorable = false` 를 주면 브로드페이즈가 그 Area 를
    정적 취급 → **StaticBody3D 와 아예 짝을 안 만든다** → 영영 안 맞는다. 상세는 `reports/developer.md`
  - 감시자 판정 **통과** (허수아비는 검증용 표적으로 사유 남기고 통과)
- **🩹 새 환경 Node 탐지 오탐 수정** (2026-07-31) — `C:\Windows\System32\node`(확장자 없는 0바이트 파일)
  때문에 PowerShell 이 버전을 빈 값으로 읽었다. `env-check.ps1`·`setup.ps1` 이 `node.exe` 로 찾도록 수정
- **🚦 새 세션 자동화 연결** (2026-07-31) — `.claude/env-check.ps1` + `SessionStart` 훅으로
  Node22+/CLI/Godot/`game/.godot` 점검 → 빠진 게 있으면 Claude 컨텍스트에 직접 경고.
  `CLAUDE.md` 에 「세션 시작 절차」·「담당 배정은 알아서 한다」·자동/수동 경계 명시
- **📦 새 환경 원클릭 셋업** (2026-07-31) — `README.md` + `setup.ps1`(6단계 자동 설치, Godot 무설치
  자동 다운로드) + `개발실 열기.cmd` / `게임 열기.cmd`. `.gitignore` 에 `/tools/` 추가
- **✅ 결정: 카메라 고정 각도** (사용자 확정) — 마우스룩·마우스 캡처 전부 제거.
  `cam_yaw_deg 0°` / `cam_pitch_deg -32°` / `cam_distance 7m`. 방향키는 고정 화면 기준
- **🛡 감시자(auditor) 에이전트 도입** — 기획 이탈 4종 검사, 판정은 `통과`/`반려` 둘 중 하나
- **✅ 결정: 3D 3인칭 확정 + 배그 요소 제거** (사용자 확정) — 2D 탑다운 폐기, 멧챠 카멜레온풍,
  방향키 전용. ADS·어깨너머 줌·조준 중 감속·스프린트 전부 제거. "배그처럼"은 자기장 한 곳만
- **개발실에서 직접 일 시키기** (`office/`) — 지시창에 적으면 `claude -p` 실행 →
  실시간 진행 표시 → `[역할]` 자동 커밋·푸시 → 담당자 판정 → 대표실 보고
- **로드맵 1단계** — Godot 4 프로젝트, 방향키 이동 + 스페이스 점프 (`move_speed 6.0` / `jump_height 1.2`)
- **개발 환경·GitHub 연결·프로젝트 셋업** — 저장소 https://github.com/Parksungchan1/mymakegame
- **결정: 그림의 역할 = 하이브리드** — 그림은 겉모습 + 형태 태그, 세기는 수치 슬라이더 전담.
  총 유효 판정 면적은 `범위` 값으로 정규화. 3단계를 3a(수치) / 3b(그림) 로 분리

## 🔧 진행 중
- **감시자 3차 감사** — 3b + 아트 + 밸런스 재조정에 대한 감사가 진행 중이다.
  결과는 `reports/auditor.md` 에 `[감시]` 커밋으로 남는다. **판정이 `반려`면 커밋 전에 조치할 것.**
  (2차 감사까지는 「통과」. 확인 필요 2건 — 허수아비 존치 여부 / 쿨타임 4.94초 → 쿨은 2.01초로 조정됨)

## ⏭ 다음 할 일 (우선순위 순)

> 표기: `[담당]` 은 코디네이터가 리포트 내용으로 추정한 것이다. 최종 배정은 PM/사용자 몫.

### 🅰 사용자 플레이 확인 직후 (게이트)
| # | 할 일 | 담당 | 근거 |
|---|---|---|---|
| A-1 | **사용자 직접 플레이 → 재미 확인** (위 📌 1번) | 사용자 | 기획서 87~88행 |
| A-2 | **QA 재검증** — S1·S2·S4·S7·S9 수정본을 실측으로 다시 확인.<br>특히 범위 10/50/100 둥긂이 설계 사거리만큼 나가는지 | qa | QA 요청 1번 |
| A-3 | 미커밋 작업물 커밋·푸시 | 아무나 | 위 📌 3번 |

### 🅱 높음 — 「그리는 재미」를 직접 죽이고 있는 것들
| # | 할 일 | 담당 | 출처 |
|---|---|---|---|
| B-1 | **[S3] 잡픽셀로 태그를 마음대로 고르는 것 차단** — `_count_blobs` 는 4px 미만을 노이즈로 버리는데 `fill_ratio`/`aspect` 는 버린 픽셀까지 포함한 바운딩박스로 계산한다. **구석에 4픽셀만 찍으면 원하는 태그 획득** | developer<br>(+기획 판단) | QA S3 / 요청 4번 |
| B-2 | **[D-1] 🐞 회전에 따라 태그가 달라진다** — 대각선 막대가 바운딩박스로는 정사각형이라 뾰족함으로 판정. 2차 모멘트 고유값 기반 **회전 불변 신장도**로 교체(공식은 리포트에 있음). 반환 키 `aspect` 는 유지하고 값만 교체 | developer | 기획 D-1 |
| B-3 | **[D-2] 🐞 대각선 점선의 덩어리 수가 0** — 4방향 연결이라 대각으로만 이어진 픽셀이 끊긴다. `NEIGHBORS` 에 대각 4방향 추가 → 8방향 (**코드 확인 결과 아직 4방향**) | developer | 기획 D-2 / QA S3 표 |
| B-4 | **[D-3] 에디터 실시간 미리보기 + 태그 경계 게이지** — 판정 도형 실비율 프리뷰 / 태그 전환 플래시 / "종횡비 2.1 → 길쭉함까지 0.4" 게이지. **밸런스 영향 0, 위험 0.** 기획이 꼽은 흥미 강화 1순위 | developer + designer | 기획 D-3 |
| B-5 | **[S5] 1픽셀만 그리면 "안 보이는데 판정은 최대"인 투사체** — 범위 100 둥긂에서 보이는 크기 0.122m vs 판정 지름 3.91m (**32배 차이**). 그림 창작 장려 취지와 정반대 | developer | QA S5 |
| B-6 | **[S8] 제작창 표시와 실제 전투가 다르다** — S1·S4 를 고쳤으니 **이제 맞는지 다시 재야 한다.** 에디터의 목적은 "예측 가능하게"(기획서 45행)인데 예측을 틀리게 하면 안 됨 | qa → developer | QA S8 |

### 🅲 중간
| # | 할 일 | 담당 | 출처 |
|---|---|---|---|
| C-1 | **[D-5] 태그별 미세 파라미터 배선** — 상수는 `Balance.gd` 에 **이미 확정**돼 있고 배선만 남았다.<br>⚠ **`damage_per_hit` 을 반드시 같이 고칠 것**(÷3·÷5 하드코딩 — 탄 수가 바뀌면 총 데미지가 어긋남. 선택 인자는 이미 추가됨)<br>⚠ **흩어짐 퍼짐각 `√(N/5)` 보정을 빼먹으면 덩어리를 많이 그릴수록 무조건 이득이 되어 형평성이 깨진다** | developer | 기획 D-5 |
| C-2 | **[D-4] 예산 초과 페널티를 화면에 표시** — `쿨타임 ×2.8 (예산 초과)` 식으로. `derive()` 가 `over_ratio`/`over_cooldown_mult`/`over_cast_mult` 를 이미 돌려준다(**값에 이미 반영돼 있으니 다시 곱하지 말 것**). 막지 않기로 했으니 **대가가 보이는 게 유일한 안전장치** | developer | 기획 D-4 |
| C-3 | **[S6] 슬롯을 옮기면 저장 안 한 편집이 경고 없이 사라진다** — 그림을 한참 그린 뒤 슬롯을 잘못 누르면 통째로 유실 | developer | QA S6 |
| C-4 | **[D-6] 다색 그림(팔레트 2~4색)** — `mask` 가 이미 `PackedByteArray` 라 `0=빈칸, 1~4=색`. **밸런스 코드 한 줄도 안 고쳐도 된다.** 색은 겉모습 전용, 판정에 절대 반영 금지 | developer | 기획 D-6 |
| C-5 | **스킬 에디터(그림판) UI/UX 아트** — "이게 이 게임의 얼굴인데 아직 손을 안 댔다"(디자인 담당). B-4 와 같이 하면 효율적 | designer | 디자인 리포트 |
| C-6 | **마법봉 끝 색을 스킬 색으로** — 지금 고정 주황. `_reload_skill()` 에서 `$Body/Wand/Tip` 재질 교체하면 "지금 든 스킬"이 손에서 보인다. **아트 쪽 준비는 끝났다** | developer | 디자인 리포트 |
| C-7 | **[디자인→QA] Muzzle 위치가 바뀌었다** `(0.3,1.0,-1.15)` → 대략 `(0.50,1.38,-0.90)`. ①허수아비 캡슐(0~1.8m)에 y=1.38 투사체가 정상 명중하는지 ②오른쪽 벽에 붙어 쏠 때 투사체가 벽 안에서 태어나지 않는지 | qa | 디자인 리포트 |

### 🅳 낮음
| # | 할 일 | 담당 | 출처 |
|---|---|---|---|
| D-a | **[S12]** 제작창으로 트리를 멈춰도 허수아비 부활 타이머가 계속 돈다 (`create_timer` 가 기본으로 일시정지를 무시) | developer | QA S12 |
| D-b | **[S11]** 마스크 크기가 틀리면 조용히 빈 그림 취급. `push_warning` 추가 | developer | QA S11 |
| D-c | **[S10]** `max_range_for(100)` 이 예산 초과 조합을 돌려줌(함수 계약 위반). **현재 호출처 없는 죽은 코드** | developer | QA S10 |
| D-d | 투사체 발사/피격 이펙트, 데미지 숫자, 화면 흔들림 등 도파민 연출 | designer | 디자인 리포트 |
| D-e | 캐릭터 애니메이션 (지금 T포즈 고정 — 걷기/점프 때 팔다리가 안 움직인다) | designer | 디자인 리포트 |
| D-f | `office/` 의 안 쓰는 원본 코드 정리 (`app/` `worker/` `db/` `drizzle/` `examples/` `build/` `tests/` `package*.json`) | — | 이전 세션 |
| D-g | 개발실: 작업 도중 취소 버튼 (지금은 15분 타임아웃뿐) | — | 이전 세션 |

### 🅴 로드맵
- **4단계**: QWER 4슬롯 + 봇. (여기서 진짜 봇이 들어오면 허수아비는 삭제 대상 — 아래 결정 5번)
- **5단계**: 자기장(안전구역 축소) + 경고 비주얼

## ⛔ 막힌 것 / 결정 대기

> **여기 있는 건 전부 사용자만 정할 수 있다.** 기획·개발이 임의로 정하면 기획 이탈 방지 규칙 위반이다.

### 🔴 1. **캐릭터 HP 100 → 150?** (기획 D-7 / 개발 대기 중)
지금 허수아비 HP 가 100 이라 **데미지 100 스킬이 원샷킬**이다. 5v5 에서 원샷은 과하다.
150 이면 최대 한 방이 HP 의 2/3 라 반드시 두 번 맞혀야 한다.
**기획서에 HP 수치가 정해진 적이 없어** 밸런스 담당 제안값으로만 기록했고, 코드는 안 건드렸다.
→ **결정: 100 유지 / 150 / 다른 값?**

### 🔴 2. **「잘 그릴 이유가 없다」 — 구조적 우려** (QA 제기, 이 게임 컨셉의 근간)
QA 실측 결과, **최적 플레이가 "대충 찍기"** 다.
- **빈 그림(아무것도 안 그림) = 둥긂 = 실측 기대 데미지 1위** (둥긂 1.00 / 흩어짐 0.46 / 길쭉함 0.29 / 뾰족함 0.19)
- **1픽셀만 그리면** 둥긂 + 거의 안 보이는 투사체(S5) — 상대는 사실상 보이지 않는 탄에 맞는다
- **구석에 점 4개** 찍으면 원하는 태그 획득(S3)

"내가 그린 스킬로 싸운다"가 컨셉인데 안 그리는 게 이득이면 컨셉이 죽는다.
QA 는 **"그림에 들인 정성이 보상받는 축"**(대칭성·꽉 찬 정도를 미세 파라미터로)을 제안했고,
**상수는 `Balance.gd` 103~140행에 이미 준비돼 있는데 아무도 안 쓰고 있다** — C-1(D-5) 배선이 상당 부분 해결한다.
→ **결정: 이 방향(정성 보상 축)으로 갈지, 다른 해법을 볼지.** B-1·B-5·C-1 의 방향이 여기에 달렸다.

### 🔴 3. **형평성 정규화 기준을 바꿀지 — 「정지 면적」 vs 「스윕 면적/기대 데미지」** (QA S2)
대원칙(기획서 20~24행)이 정규화하는 건 **정지 면적**이고, 코드는 그걸 오차 0 으로 지키고 있다.
그런데 투사체는 **날아가면서 면적을 쓸고 간다.** 범위 100 기준 스윕 면적은
둥긂 144.9 / 길쭉함 51.3 / 뾰족함 242.2 / 흩어짐 309.2 — **최대 6배 차이**다.
같은 데미지 60/범위 50 인데 둥긂이 뾰족함보다 **5.25배 강했다**(S2 수정 전 실측).
S2 는 고쳤지만 **정규화 기준 자체를 바꿀지는 기획서를 건드리는 일**이라 사용자 결정이 필요하다.
→ **결정: 기획서 20~24행의 "총 유효 판정 면적"을 스윕 면적/단일표적 기대 데미지로 재정의할지.**
   (재정의한다면 A-2 QA 재검증에서 태그별 기대 데미지를 다시 재야 한다)

### 🟠 4. **둥긂에 관통을 도입할지?** (기획 확인 요청 2번)
채움률이 낮으면(< 0.35) 최대 2명 관통, 대신 **총 데미지를 관통 수로 나눈다**(한 명만 맞히면 절반).
제로섬이라 형평성은 안전하지만 **새 메커니즘**이라 기획 이탈 방지 규칙상 승인이 필요하다.
→ **결정: 넣을지 / 나중으로 미룰지.**

### 🟠 5. **허수아비(`Dummy`)를 계속 둘지** (감시자 2차 확인 필요 1번)
허수아비 3개는 **기획서에 없다.** 4단계의 "봇"도 아니다.
다만 데미지가 들어가는지 확인할 대상이 없으면 검증 자체가 불가능해서 **검증용 표적**으로 판단해 통과시켰다.
움직이지도 반격하지도 않아 게임 규칙에는 영향이 없다. 4단계에서 진짜 봇이 들어오면 삭제 대상이다.
→ **결정: 4단계까지 유지 / 지금 걷어내기.**

### 🟡 6. **새 태그(설치·회전)를 언제 신설할지** (기획 확인 요청 3번)
기획서 47행이 "회전/부메랑/설치"를 예시로 들어뒀다. 특히 흥미로운 건 **"캔버스의 어디에 그렸는가"를 읽는 것** —
지금은 32×32 어디에 그리든 결과가 같아서 **캔버스 공간의 절반이 낭비**다(무게중심이 아래면 바닥 설치형 등).
기획 담당은 **로드맵 4단계(QWER+봇)가 끝난 뒤 재상정**을 권했다.
→ **결정: 4단계 이후로 미룰지 / 지금 볼지.**

### 🟡 7. **비용 공식을 곱셈으로 바꿀지** (기획 확인 요청 4번)
지금은 기획서 15행대로 `데미지 + 범위×0.6` (덧셈)이다. 엄밀히는 넓은 범위가 큰 데미지를 **증폭**하므로
곱셈이 더 정확하다. 다만 15행이 명시한 형태라 임의로 안 바꿨다.
**기획 담당 의견: 지금 수치로도 검산상 균형이 맞아 당장은 안 바꿔도 된다**
(D88/R20 유효 9.62 vs D40/R100 유효 8.69 — 17% 차이).
→ **결정: 유지(권장) / 곱셈 전환.**

### 🔵 8. (관찰 사항 — 결정은 아님) `데미지 100 / 범위 1` 이 순수 DPS 10.48 로 전 조합 최강
초과가 0.6% 뿐이라 페널티가 거의 안 붙는다. 사거리 8.3m / 반경 0.54m 짜리 **초근접기**라
"유효 지속딜(딜÷쿨×반경)" 기준으로는 합법 빌드가 이긴다 → QA 가 **버그로 판정하지 않고 관찰만** 남겼다.
**근접 난입 메타가 나오면 재검토 필요.**

### 🔵 9. (개발→QA/기획 질의) 뾰족함의 전탄 명중 거리가 너무 짧은 게 아닌가
개발이 3b 때 남긴 질문이다. 이후 S2 수정으로 퍼짐각이 90° → 11° 로 바뀌었으므로
**A-2 QA 재검증에서 다시 재면 답이 나온다.** 지금은 근거가 없어 판단 보류.

## 🗒 셋업 시 지시서와 다르게 처리한 것 (기록용)
1. **프로젝트 위치**: 지시서는 바탕화면을 루트로 쓰라고 했으나, 바탕화면에 신분증·통장 사본이 있어
   GitHub 유출 위험이 있었음. 사용자 확인 후 `C:\Users\k\SkillCraftBattle` 로 분리.
2. **`.claude/settings.json` 훅 형식**: 원본은 `"command"` + 별도 `"args"` 배열이었으나 Claude Code 훅은
   `args` 를 인식하지 않아 실행되지 않음. 한 줄 명령으로 병합:
   `"command": "powershell.exe -ExecutionPolicy Bypass -File .claude/checkpoint.ps1"`
3. **Git 사용자 정보**: 이 저장소에만(local) `wfs070809 / wfs070809@gmail.com` 설정.

## 🔎 환경 메모
- Git 설치됨, 시스템 PATH 등록됨
- 원격: `origin` → https://github.com/Parksungchan1/mymakegame.git (인증 완료 상태)
- Godot 4.7.1: `tools\Godot\Godot.exe` (setup.ps1 이 받아둔 무설치본. 저장소에는 안 올림)
- Node.js: v24.18.1

### 컴퓨터 2대에서 작업 중 (2026-07-31)
| | 경로 | 비고 |
|---|---|---|
| 1호기 | `C:\Users\k\SkillCraftBattle` | 처음 만든 곳 |
| 2호기 | `C:\Users\parks\SkillCraftBattle` | 클론 + `setup.ps1` 로 5분 만에 이어받음 |

- **2호기 주의**: `C:\Windows\System32\node` 에 확장자 없는 0바이트 파일이 있다.
  PowerShell 이 진짜 `node.exe` 대신 이걸 먼저 실행한다(cmd 는 영향 없음).
  점검 스크립트는 `node.exe` 로 찾도록 고쳐서 지금은 문제없지만,
  PowerShell 에서 `node` 를 직접 부르는 코드를 새로 쓸 거면 `node.exe` 로 쓴다.
  관리자 권한으로 그 빈 파일을 지우면 근본 해결: `Remove-Item C:\Windows\System32\node -Force`
- **Windows 주의**: 한글을 Node 에서 명령줄 인자로 넘기면 시스템 코드페이지로 인코딩돼 `???` 로 깨진다.
  git 커밋 메시지는 UTF-8 파일 + `-F`, claude 프롬프트는 stdin 으로 넘긴다.
  (커밋 `5ad68cb` 이 이 버그로 깨진 채 남아 있음)
- **체크포인트 훅**: `SKILLCRAFT_OFFICE_JOB=1` 이면 `checkpoint.ps1` 이 커밋을 건너뛴다.
  개발실 지시 작업은 서버가 `[역할]` 태그를 붙여 직접 커밋하기 때문.
- **과금**: API 키 없이 계정 로그인(OAuth) 방식이라 지시 실행은 구독 사용량을 소모한다.
  화면의 `$0.xx` 는 실제 청구액이 아니라 작업 무게를 가늠하는 환산값. 플랜 확인은 `/status`.
- **QA 헤드리스 검증 시 주의**: 검증 스크립트가 `SkillDB.set_slot` 을 부르면 `user://skills.json` 이
  덮어써진다. 끝나면 `Remove-Item "$env:APPDATA\Godot\app_userdata\SkillCraft Battle\skills.json"` 로 지울 것.
  또 비행 중인 `Projectile` 을 `free()` 로 강제 제거하며 반복 측정하면 그 뒤 생성한 Area3D 가
  이동 중 판정을 놓치는 현상이 있다(자연 소멸 방식으로 다시 재야 정확).
