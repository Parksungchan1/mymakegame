# 프로젝트 진행 현황판 (STATUS)

> 코디네이터가 관리. 각 `reports/*.md` 를 종합해 최신화. 이 한 장으로 전체 진행 파악.
> 최종 갱신: **2026-08-08** — **감사 7차 반영**(판정 반려, 위반 3건은 전부 기록 문제).
> QA **6차** · PM 로드맵 재설계(⛔ 승인 대기) · 커밋 `a206f55` 까지 반영.
> 이 문서에 적힌 숫자는 전부 리포트에 근거가 있다. 근거 없는 것은 「확인 필요」로 적었다.
>
> 🔴 **2026-08-03 최신화 때 발견한 것 — 이 문서는 22커밋(08-01 01:53 ~ 08-03 13:45) 동안 방치돼 있었다.**
> 그동안 「할 일」로 적혀 있던 것 중 **다섯 가지가 이미 끝나 있었다**: 4픽셀 익스플로잇 차단 ·
> 캐릭터 애니메이션 · 봇 대전 · 발사/피격 이펙트 · 미커밋분 커밋. **낡은 현황판은 없는 것보다 나쁘다** —
> 끝난 일을 다시 하게 만들기 때문이다. 이번 최신화는 **커밋 로그와 실제 코드를 대조**해서 썼다.
> **다음 담당도 리포트만 읽지 말고 `git log` 와 코드를 같이 볼 것.**

---

## 🔴 지금 살아 있는 문제 (다음 세션 최우선) — **경고를 갈아 끼웠다**

> ⚠ **종전 경고 「`aspect` 가 바운딩박스 종횡비라 회전에 민감하다」는 해소됐다.**
> [D-1] 로 **2차 모멘트 신장도(`elongation`)** 로 교체됐고, **명확히 길쭉한 그림은 회전에 안정적이다.**
> - 막대 8종 × 0~90° × 0.5° = **1,448지점: 오판 908 → 0** (QA 5차 1-A, 기획·감사와 독립 래스터라이저)
> - 손떨림 브러시 획 **285조합: 오판 177 → 0** (QA 5차 1-D). 「사선으로 그으면 부채꼴이 나간다」 소멸.
> **아래 세 가지가 그 자리를 대신하는 새 경고다.**

### 1. ✅ **[해소] 구석 4픽셀 익스플로잇 — 2026-08-03 `7bdf1d3` 에서 닫혔다**

> 이 항목은 08-01 ~ 08-03 내내 이 문서에 **1순위 🔴** 로 떠 있었지만 **이미 고쳐져 있었다.**
> 낡은 현황판이 만든 대표적 손해라 지우지 않고 기록으로 남긴다.

- **채택**: `T_BLOB_RELATIVE_MIN = 0.25` 신설. 하한 = `max(4, ceil(0.25 × 가장 큰 덩어리))`.
- **기각**: 절대 문턱 상향. 기각 이유가 이 결정의 전부다 — **문턱을 N 으로 올리면 유저는
  N 픽셀을 찍는다.** 비용이 그림 크기와 무관한 **상수**로 남는 한 「뭘 그리든 구석에 점」은
  안 사라지고 점이 커질 뿐이다. **어렵게 만드는 것과 닫는 것은 다르다.**
- **0.25 의 근거**: 흩어짐은 「덩어리 하나 = 탄 하나」인데 탄은 전부 같은 크기로 나간다.
  크기가 크게 다른 덩어리를 「같은 크기 탄 N발」로 바꾸는 것 자체가 거짓말이라
  기준을 **가장 큰 덩어리의 절반 지름**(= 면적 0.25)으로 잡았다.
- **실측 전→후**: 별5+구석2×2 흩어짐 41.235 → 뾰족함 25.630 / 원·막대도 동일하게 원복.
  **1.57배 → 1.000배(차이 0).** 익스플로잇 비용이 4px → 25~36px(본체의 28~32%)로,
  **상수에서 그림 비례로** 바뀐 것이 「닫혔다」의 실체다.
- **새 구멍이 안 열리는 이유**: 이 규칙은 **덩어리를 지우기만 한다.** 덩어리 수가 늘지 않으므로
  어떤 그림도 이 규칙 때문에 **새로 흩어짐이 되지 않는다.**
- 재검산 3건 완료(흩어짐 탄 수 매핑 · 기본 스킬 4개 태그 불변 · 정상 산탄 유지).
- ⚠ **남은 것 하나**: 뾰족함 정조준이 **QA 실측 26.222 대 기획 계산 25.630 으로 2.3% 다르다.**
  경계 거리에서 실사격이 격자 두 칸을 더 맞히기 때문. **QA 실사격 재확인 대기.**

### 2. 🟠 **문턱 2.5 근처(비율 1.9~2.8)는 지금도 회전으로 태그가 갈린다 — 범인은 지표가 아니라 32×32 래스터화다**

지표를 바꿔서 **흔들림 폭이 줄어든 것이지 없어진 것이 아니다.** 각도마다 픽셀이 재배치된다.

- 막대 **240조합 × 0~90° 0.5°**: 전 각도 한 태그 173 · **길쭉함 여부가 갈림 17** ·
  둥긂↔뾰족함만 갈림 50 → **합 67조합(28%)** 이 각도로 태그가 바뀐다 (QA 5차 1-C).
  (기획이 독립 측정한 값은 **254조합 × 91각도 기준 21조합(8.3%) / 77조합(30.3%)** — 표본이 달라 수가 다르다.
  세 사람(기획·감사·QA)이 **서로 다른 래스터라이저로 독립 측정했고 결론이 같다.**)
- 갈리는 17조합은 **전부 비율 2.25~2.80**. 감사관이 어림한 「2.2~2.9」와 같다.
- **타원·손떨림 획 같은 「사람이 실제로 그리는」 도형에서도 난다** (QA 5차 1-D):
  타원 12×5 는 91각도에서 신장 **2.020~2.884(변동 42.8%)** · 둥긂 82 / 길쭉함 8.
- **작게 그릴수록 심하다** — 비율 2.5:1 고정, 두께별 변동폭 [기획 실행]:
  두께 2 **66.0%** / 4 25.6% / 6 14.9% / 8 8.1% / 10 11.6% / 12 **3.6%**.
- 🔑 **격자 해상도를 올리면 정확히 그만큼 준다**: 32격자 **25.6% → 64격자 8.1% → 128격자 1.6%.**
  → 「격자 32→64」가 **원인을 직접 없애는 유일한 후보**이고, **사용자 결정 대기**(아래 ⛔ 3번).
- ⚠ **문턱이 있는 한 「갈리는 띠」는 0 이 안 된다.** 비율이 정확히 2.5 면 변동 3.6% 짜리도 갈린다
  (30×12: 길쭉함 39 / 둥긂 26 / 뾰족함 26). **「없앤다」는 도달 불가능한 목표**다.
- **유저에게 탈출구는 생겼다** — 비율 **2.9 이상은 100% 안정**이고, 제작창이 「신장 2.35」를
  실시간으로 띄운다([D-14]). 4차 때는 아무리 길게 그려도 각도가 지배해 탈출구가 없었다.

### 3. 🟠 **복잡도(둥긂↔뾰족함)는 아직 회전 불변이 아니다 — 그리고 이게 실제로 아픈 쪽이다**

- 막대 **240조합 중 63조합(26.3%)** 이 각도에 따라 뾰족함이 되었다 말았다 한다.
  최대 **43.6%** 의 각도가 뾰족함(19×8, 비율 2.38). 23×10·24×10 은 41.4%, 17×7 은 40.9%.
  → **기획서의 「50개·12%·짧고 뭉툭한 막대뿐」 서술은 사실보다 작다** (QA 5차 2-A).
- 심각도 [실측, QA 5차 2-B]:

| 그림 | 태그 | 정조준 평균 | 유효 명중 폭 | 쿨타임 |
|---|---|---|---|---|
| 막대 24×10 **@0°** | 둥긂 | **60.000** | 2.8546 | 7.697 |
| 막대 24×10 **@26°** | **뾰족함** | **30.864** | 2.8546 | 7.697 |

  → **같은 그림을 26° 돌리면 정조준 데미지가 1.94배 오간다.**
- **대원칙(「유효 명중 폭 = 기대 데미지」)은 한 자리도 안 깨진다** — 폭·쿨타임·발동·속도·`reach`
  소수점 끝까지 동일. **길쭉함↔둥긂 전환은 실측상 완전히 무해**(둘 다 60.000).
  **회전 흔들림 중 실제로 아픈 것은 「뾰족함으로 넘어가는 것」 하나뿐이다.**
- QA 의견: **[D-1] 잔여와 복잡도 흔들림은 사실상 같은 한 덩어리다**(같은 그림·같은 각도 축·같은 결과).
  나눠서 우선순위를 매기면 유저 체감으로는 하나도 안 고쳐진 것처럼 보인다. **[S3] 4픽셀과 묶어서 처리 권고.**

### 4. 🟠 **1픽셀 「꼬리」는 노이즈 제거로 못 막는다** (QA 5차 3-D, 신규)

`_denoised` 는 **끊어진** 작은 덩어리만 지운다. 본체에 **이어진** 가는 획은 아무리 얇아도 남는다.

| 그림 | 태그 | 복잡도 | 신장도 |
|---|---|---|---|
| 원 r6 (기준) | 둥긂 | 1.64 | 1.00 |
| 원 r6 + 오른쪽 1픽셀 꼬리 15칸 | **뾰족함** | 3.02 | 1.48 |
| 원 r4 + 좌우 1픽셀 꼬리 | **길쭉함** | 6.70 | 3.61 |

유저는 「원에 선 하나 그었을 뿐인데 왜 부채꼴이 되지?」를 알 수 없다.
**[S3] 의 잔여가 아니라 복잡도 지표(둘레²/면적) 자체의 성질**이다. 기획 판단 항목.

---

## 📌 다음 세션에서 가장 먼저 할 일

### 1️⃣ **사용자가 직접 플레이해서 「재미있는가」를 확인한다 — 다른 모든 것보다 먼저**

기획서 87~88행은 **「3a 가 돌아가고 재미가 확인된 뒤에 3b 를 붙인다」** 였는데, 사용자 승인으로
이 게이트를 건너뛰고 3b → 4단계(QWER)까지 왔다. **이번 세션 내내 미뤄졌다.**
코드·수치는 QA 가 다섯 번 쟀지만 **「재미있는지」는 아직 아무도 확인한 적이 없다.**

실행: `게임 열기.cmd` → **F5**. 아래를 순서대로 보고 느낀 걸 그대로 말해주면 된다.

- **① 그리는 게 재미있는가** (이게 이 게임의 전부다)
  - Tab → 제작창 → **원 / 세로 막대 / 대각선 막대 / 점 여러 개 / 별** 을 각각 그린다.
    태그(둥긂·길쭉함·뾰족함·흩어짐)가 **내가 그린 대로 나오는가.**
  - **같은 그림을 기울여 다시 그려 본다.** 명확히 긴 창은 이제 안 흔들려야 한다(고쳐진 것).
    **짧고 뭉툭한 몽둥이(길이 10~25 × 두께 4~10)는 각도에 따라 바뀐다**(안 고쳐진 것) —
    실제로 얼마나 거슬리는지가 다음 세션 우선순위를 정한다.
  - 제작창에 뜨는 **「채움 · 신장 · 종횡 · 복잡 · 덩어리」 지표 줄**이 이해되는가?
    「경계까지 얼마 남았는지」가 읽히는가? (안 읽히면 → 처방 1번 「경계 게이지」가 정답이다)
  - 그린 그림이 **그대로 투사체 모양으로 날아가는가** (3b 의 핵심)
  - **공들여 그릴 마음이 드는가?** 아니면 「대충 찍어도 되네」 싶은가 → 후자면 위 🔴 1번이 진짜 문제다.
- **② QWER 4개를 비교해 본다** (이번 세션에 새로 붙었다 — 이전 세션엔 Q 하나뿐이었다)
  - Q·W·E·R 를 각각 눌러 본다. **쿨타임이 슬롯마다 따로 도는가**(QA 실측상 독립).
  - 기본 4개 실측: Q 불꽃탄 쿨 **2.01초** / W 관통창 **4.61초** / E 산탄 **2.79초** / R 대폭발 **8.84초**.
    **이 쿨타임 체감이 적당한가? 아직 답답한가?**
  - 태그별 체감이 다른가 — 둥긂(구 1발) / 길쭉함(창) / 뾰족함(부챗살) / 흩어짐(산탄).
    ⚠ **길쭉함과 둥긂은 정지 표적 상대로 수치가 한 자리도 안 다르다**(N13). 실제로 구분되는가?
- **③ 움직임** — 멧챠 카멜레온풍인가? (`move_speed 6.0` / `jump_height 1.2`) 화면은 고정각.
- **④ 새 아트 확인** — 캐릭터 **2등신 마법사**, 아레나 **거대한 원룸**(침대·책상·머그컵·연필…).
  원룸으로 읽히는가? 캐릭터가 귀여운가?
  - 디자인이 QA 에 확인 요청한 것: **의자 등받이 옆을 지날 때 카메라가 확 당겨지는지** /
    **책 3권(1.1m)을 밟고 침대 위로 올라가지는지**
  - QA N5: 큰 둥긂(범위 100)은 총구(y 0.749)가 아니라 **y 2.034 — 캐릭터 키 1.7m 보다 높은 곳**에서
    태어난다. 3D 로 보면 봉이 아니라 **머리 위에서 공이 나온다.** 눈에 거슬리는지 확인.

> ⚠ **위 1️⃣ 목록은 08-01 에 쓰인 것이라 일부가 낡았다.** 그 뒤로 봇·HP·소리·이펙트·대쉬·
> 도안첩이 들어왔다. **지금 플레이하면 「④ 새 아트」에 텍스처 4장(08-03)이 추가로 보이고,
> 봇이 반격하므로 「혼자 허수아비를 때리는」 상황이 아니다.**

### 2️⃣ ✅ **[해소] 권위 모델 결정 — 사용자가 2026-08-03 에 답했다**

PM 이 「다음 가장 중요한 1가지」로 꼽았던 **❓2** 에 답이 나왔다.

> **사용자 결정 (2026-08-03)**: **「모르는 사람들끼리도 되고, 아는 사람들끼리는 방을 파고
> 초대해서 할 수도 있도록.」**

- 목적지는 PM 선택지의 **(B) 모르는 사람과도** 이고, 여기에 **친구용 방/초대**가 함께 붙는다.
  즉 **둘 중 하나가 아니라 둘 다**다 — 공개 매칭과 비공개 방이 같이 있어야 한다.
- **이 결정이 여는 것**: 효과 축(매혹·실드·반사)을 **서버 권위 전제로** 설계할 수 있다.
  PM 이 「나중에 못 바꾼다」고 한 것이 이것이고, 이제 안 막혀 있다.
- **이 결정이 부르는 대가** (PM 리포트 6장, 사용자에게 이미 전달됨):
  **서버비가 계속 들고 개발이 눈에 띄게 무거워진다.**
- 🔑 **그래도 5단계 계획은 안 바뀐다.** PM 의 (C) 경로(「판정 창구를 하나로 모아두면
  그 한 줄만 나중에 서버로 옮기면 된다」)는 (B) 를 **버리는** 길이 아니라 **거기로 가는 싼 길**이다.
  → **5-2(판정 단일 창구)가 이 결정의 직접적인 준비 작업이다.**
- 🆕 **이 결정으로 새로 생긴 할 일 — 로드맵에 없던 것**:
  **매치메이킹(모르는 사람을 붙여주는 것)** 과 **방 만들기/초대**. PM 의 5-7(타이틀→로비)이
  자리는 잡아두지만 내용은 비어 있다. **[확인 필요] PM 이 재검토해서 5단계에 넣을 것.**

### 3️⃣ ✅ **[해소] ❓3 제작창 시점 — 사용자가 2026-08-04 에 답했다**

> **사용자 결정 (2026-08-04)**: **「제작창은 게임하면서 열지 않는다. 게임 시작 전에 미리 세팅을
> 다 할 수 있도록 — 자신만의 창고·커스텀 창고 느낌으로, 매칭이나 시작 전에 미리 세팅해두고
> 하도록 할 것. 게임하면서도 그림을 그리고 적용하면 좋기는 하겠지만.」**

- **확정 = PM 선택지 (A) 매치 시작 전 로비에서만.** PM 이 「가장 안전하고 가장 만들기 쉽다」고
  적은 그 안이다. **온라인 설계에서 가장 어려운 문제 하나가 여기서 사라졌다** —
  제작창이 `get_tree().paused = true`(`SkillEditor.gd:83`)로 **세상을 멈추는데**,
  매치 전에만 열면 **멈출 세상이 아직 없다.**
- ⚠ **마지막 문장은 확정이 아니라 희망사항으로 기록한다.** 「게임하면서도 그리면 좋겠지」는
  PM 선택지 (B)/(C) 에 해당하고, **지금 만들 것에는 안 들어간다.**
  🔑 **다만 이것 때문에 지켜야 할 설계 제약이 하나 생긴다**:
  **나중에 전투 중 제작창을 열 수 있게 하려면 `get_tree().paused` 를 쓰면 안 된다.**
  온라인에서는 한 사람이 세상을 멈출 수 없기 때문이다. **로비 전용으로 만들 때부터
  「일시정지에 기대지 않는 제작창」으로 짜두면 나중이 공짜가 되고, 안 그러면 다시 짠다.**
- ✅ **「자신만의 창고」는 이미 절반 있다** — `134a8b2` 도안첩(보관함).
  `SkillDB.book()`/`book_save()`/`book_equip()`/`record()`(전적: 발사·명중·데미지·킬) +
  `SkillEditor._build_book()` 의 「그림 + 이름 + 전적 + 끼우기」 UI.
  **없는 것은 창고가 아니라 「매치 전에 그 창고를 여는 흐름」**이다 → A-2b.

### 4️⃣ 나머지 결정 대기 — **❓1(접속 방식)은 이제 성격이 달라졌다**

- **❓1 접속 방식**: 위 결정으로 **(A) 스팀 초대 단독은 답이 되기 어렵다** — 스팀 친구 초대는
  「아는 사람」쪽만 덮고 「모르는 사람과 매칭」을 안 덮는다. **[확인 필요 — PM 재검토]**
  다만 PM 판단대로 **급하지는 않다**(전송 방식은 나중에 갈아끼울 수 있다).
- **❓3 온라인에서 제작창을 언제 여나** — 제작창이 지금 **게임 전체를 멈춘다**
  (`SkillEditor.gd:83`). **온라인에서는 세상을 못 멈추므로 이건 반드시 답이 필요하다.**
  이 게임의 정체성 문제라 사용자만 정할 수 있다. **→ 다음으로 급한 결정.**
- **❓4 싱글 모드 · ❓5 맵** — 늦어도 굴러간다.
- 기존 「⛔ 막힌 것 7건」 중 **격자 32→64** 는 여전히 미결이고 다른 작업의 전제다.

---

### 5️⃣ 🔴 **감사 7차(2026-08-08) — 판정: 반려.** 상세는 `reports/auditor.md`

> **되돌릴 코드는 하나도 없다.** 위반 3건이 전부 **기록**의 문제다. 문서 세 곳을 적으면 끝난다.
> ⛔ 3건 · ⚠ 9건 · ✅ 8건.

| # | 위반 | 담당 | 조치 |
|---|---|---|---|
| ⛔1 | **미승인 PM 제안을 정본으로 선언** — 코디가 「종전 로드맵 폐기다」「이 표가 최신」이라 단정. PM 은 네 곳에서 「제안만」이라 못박았다 | coordinator | ✅ **2026-08-08 정정 완료** (🅴 절) |
| ⛔2 | **도안첩(보관함+전적)이 기준 문서에 0건** — 「도안첩」「보관함」「전적」「창고」 전문 검색 **전부 0건**. 승인 근거가 커밋 메시지 한 줄뿐. **감사 4차 위반 1과 같은 형태**이고, 그 뒤 더블점프·대쉬·발사형태·효과옵션 **4건이 연속으로 「원문 인용」 형식을 지켰는데 도안첩만 빠졌다** | game-designer | ⛔ **기획서에 승인 절 신설 — 사용자 승인 필요** |
| ⛔3 | **카메라 거리 `✅ 결정됨` 무단 변경** — 기획서 1717행 **7m** ↔ `Player.gd:57` **6.0**. 승인 기록 0건.<br>⚖ 감사관이 정직하게 남김: **1721행이 금지한 건 「각도」이고 거리는 엄밀히 각도가 아니다.** 디자이너는 7.0/6.2/6.0/5.6/5.2/5.0 을 전부 렌더 비교하고 「좌우 시야 −14% = 반응 0.04~0.10초 손해」까지 실측했다 | 사용자 | ⛔ **「거리≠각도」로 볼지 사용자 판단** |

**⚠ 그중 가장 급한 것 — 기준 문서 4곳이 거짓을 적고 있다**
- 🔴 **「봇은 대쉬를 안 한다」가 `기획서:552,617,1778~1779` + `Balance.gd:781~783` 에 적혀 있는데 사실이 아니다.**
  `Bot.gd:70~72,259~262` 에 대쉬가 **실제로 있다**(`f386688`).
  → 이 문장들이 **「4단계 재검증 6번(매혹 값)은 못 잰다」의 근거**라, 두면 **잴 수 있게 된 것을 안 재고 넘어간다.**
- 🔴 **대쉬 확정 4.0m 가 코드에 없다.** `Balance.DASH_*` 호출부 **0건**.
  `Player.gd:37` **22.0(→3.96m)** ↔ `Bot.gd:70` **22.2(→4.0m)** — **봇이 플레이어보다 멀리 피한다.**
  기획서 168행이 **바로 이 실패 방식을 경고해 뒀다.**
- 사용자 결정 2건(08-03 권위 모델 · 08-04 제작창 시점)이 **STATUS 에만 있고 기획서엔 0건.**
  기획서 1701행은 아직 「빈칸 7. 온라인 매칭(나중)」 — **두 기준 문서가 부딪친다.**
- 기획서 1731행 「스프린트 금지」 ↔ 156행 「대쉬(Shift)」 — 감사 판정은 **위반 아님**
  (`move_speed 6.0` 하나뿐, 대쉬는 0.18초 임펄스). 다만 PM 이 요구한 「둘은 다르다」 한 줄이 없어
  **다음 감사가 1731행만 보고 반려할 수 있다.**

**✅ 실측으로 확인된 것** — 배그 카메라 **안 움직였다**(ADS·줌·감속·스프린트·마우스룩 **0건**) ·
4픽셀 차단 **헤드리스 실사격으로 재확인**(구석 2×2 두 개를 붙여도 태그·수치 한 자리도 안 변함) ·
QA 6차 3대 지적 해소 4행 전부 사실 · **아트 색 규칙 지켜짐**(재질 27개 중 24개 |ΔS|≤5%,
전투가 벌어지는 마루는 채도가 오히려 **−20%**, 판정 노드 30→30 불변).

---

## ✅ 완료

### 2026-08-03 — 🎨 **AI 텍스처 4장을 붙였다. 이 프로젝트 최초의 에셋 파일이다.**

> 종전 `Arena.tscn` 주석의 **「이 프로젝트는 에셋 파일이 0개다(텍스처를 못 쓴다)」는 이제 틀렸다.**
> `game/art/` 에 PNG 4장이 있고, 절차적 `NoiseTexture2D` 는 노멀맵 용도로만 남았다.

- **막혀 있던 것 — Higgsfield MCP 인증**. 원인은 Claude Code 쪽이 아니라 **Higgsfield 서버**였다.
  Claude Code 의 OAuth 클라이언트 문서는 콜백을 **포트 없이**(`http://localhost/callback`) 선언하는데
  실제로는 랜덤 포트를 연다. 루프백은 포트를 무시하고 매칭하는 게 표준(RFC 8252 §7.3)인데
  Higgsfield 는 문자열 그대로 비교해서 거부했다 → `/mcp` → Authenticate 를 몇 번을 해도 같은 에러.
  **우회**: 같은 서버의 동적 등록(DCR, `/oauth2/register`)으로 포트 포함 콜백을 등록해 토큰을 받고,
  MCP 서버를 `Authorization: Bearer` 헤더로 재등록했다. ⚠ **토큰은 24시간짜리다**(갱신 토큰 있음).
  만료되면 `claude mcp list` 가 다시 인증 요구로 뜬다 — 그때 같은 절차를 반복하면 된다.
- **텍스처 4장** (Nano Banana 2, 1024², 크레딧 10 → 2.5 — 직물은 1회 재생성했다):
  `wood.png` 마루 · `fabric.png` 러그/침구/방석 · `paper.png` 책/판지 · `wall.png` 벽
  - 워터마크 없음 확인. **이음매는 수치로 검증**했다(이미지 내부 인접 행/열 평균차와 비교):
    나무 상하 **97.57 → 2.35**(아래 96줄을 위에 교차 페이드해서 직접 고쳤다) · 천 17.63 · 종이 6.05 · 벽 2.73
    — 뒤 셋은 생성 시점부터 이미 이어져 있어서 손대지 않았다.
  - 🔴 **직물 1차 생성물은 버렸다.** 킬림(마름모) 무늬가 강하게 나왔는데, 러그는 「여기가 싸우는
    자리」를 알려주는 차분한 표식이라 무늬가 있으면 투사체와 싸운다. 프롬프트에 `no motifs`
    `no geometric ornament` 를 명시해서 **민무늬 리넨 직조**로 다시 뽑았다.
- **씬 반영** — `albedo_color` 를 **전부 다시 계산했다.** 절차적 노이즈는 그라디언트가 0.82~1.06 즉
  거의 흰색이라 색은 사실상 `albedo_color` 혼자 냈다. 실제 텍스처는 자기 밝기를 갖고 있어서
  (나무 0.68 · 천 0.58 · 종이 0.89 · 벽 0.65) 그대로 곱하면 방이 어두워진다.
  `새 albedo = 옛 albedo × 옛노이즈평균 ÷ 새텍스처 채널평균` 으로 채널별 보정 —
  **방의 색 균형은 종전과 같게 두고 「표면」만 바꾸는 것**이 목적이다.
  - `uv1_scale` 중 **늘여 놓았던 값은 정사각으로 돌렸다**: 마루 (13,3)→(4,4) · 벽 (6,2)→(8,2.4) ·
    판지 (8,2)→(4,4). 늘인 값은 노이즈로 가짜 결·골을 만들던 것인데, 실제 텍스처를 늘이면
    구조가 찌그러진다. 마루는 60m ÷ 4타일 = 판자 폭 ≒ 1.7m ≒ 캐릭터 키 — 기획의
    「6m 방에 키 17cm 사람」 비율(마루 판자 15cm : 사람 170cm)과 맞는다.
  - `normal_scale` 은 절반 이하로 낮췄다. 결이 이제 알베도에 있어서 노멀까지 세면 표면이 울렁거린다.
  - 🔴 **이불·베개 — 여기만 규칙을 못 지켰다. 2026-08-08 감사 7차 ⚠6 이 잡았고, 사실이다.**
    보정값이 1.0 을 넘어 잘렸다(B 1.18 · 베개는 전 채널 1.16~1.39).
    종전에 이 자리에 **「침구는 방에서 가장 밝은 물건이라 밝기만 지키면 되고」**라고 적었는데,
    **그 밝기가 안 지켜졌다** — 베개 명도 **0.747 → 0.619 (−17%)** · 채도 0.039 → 0.134.
    albedo_color 가 이미 (1,1,1) 이라 **더 밝게 할 여지가 없는 것이 원인**이다.
    → **처방(크레딧 0)**: `fabric.png` 를 밝기·채도만 조정한 **`fabric_light.png` 사본**을 만들어
      침구 전용으로 쓴다. AI 재생성이 아니라 이미지 보정이라 비용이 없다. **아직 안 했다.**
- **⏭ 손대지 않은 것** — 침대틀·책상·의자·문의 나무는 **절차적 노이즈 그대로다.**
  이들 `uv1_scale` 은 (5,1.5)·(6,1.5) 처럼 늘어나 있어서 판자 텍스처를 그대로 넣으면 찌그러진다.
  가구용 나뭇결 텍스처를 따로 뽑든지, uv 를 재조정해야 한다. **다음 아트 작업 후보 1순위.**
- **검증**: Godot 로 전/후를 렌더해서 비교했다(1280×720, 스폰 지점 고정 카메라).
  사용자가 직접 켜서 확인했고 「일단 좋아」 판정.

### 2026-08-03 — 🧙 캐릭터 3D화: **컨셉 아트까지만 했다. 메시는 크레딧이 모자라 못 뽑았다.**

- 사용자 요청: **「AI가 3D 캐릭터 모델을 만들어주고, 나온 것 중에 골라 쓰는」** 방식(유튜브에서 봄).
- **`docs/concept/wizard.png`** — 지금 캐릭터의 색(남색 모자·파란 로브·금색 띠·흰 깃)을 그대로
  이어받은 정면 전신 컨셉. 이미지→3D 의 **입력**으로 쓰려고 배경을 민무늬 회색, 그림자 없이 뽑았다.
- 💰 **크레딧: 10 → 1. 🔴 그리고 08-04 에 확인한 결과 「하루 10 충전」은 사실이 아니었다.**
  - `transactions` 전체 내역이 **지출 6건(각 −1.5)뿐이고 충전(grant) 기록이 0건**이다.
    날짜가 08-04 로 바뀐 뒤에도 잔액은 **그대로 1**이었다.
    → **10 크레딧은 1회성 무료 체험분**으로 보인다. **[확인 필요 — 며칠 더 봐야 확정]**
  - 텍스트→3D 최저가가 **Tripo 5크레딧**(Hunyuan 7 · Meshy 25)이라
    **결제 없이는 3D 캐릭터를 못 뽑는다.** 후보 2개면 10 이 필요하다.
  - 유료 선택지(2026-08-04 조회): **3일 무료 Plus 체험 100크레딧 $0**
    — ⚠ **카드 등록 필요 · 3일 뒤 자동으로 $49/월 청구**(취소 안 하면). MCP 에서
    「cancel auto-renewal」로 해지 가능 / **PLUS 월 $49 (1,000크레딧/월)** · 연간 $39/월.
  - 🔑 **이건 돈 문제라 사용자만 정한다.** 결제 전까지 캐릭터 3D화는 **보류**다.
- 🔴 **통째 교체는 위험하다 — 캐릭터에 게임 로직이 붙어 있다.** `Player.tscn` 은 노드 49개짜리
  손수 조립품이고, 그중 다음은 **아트가 아니라 기능**이다:
  `Body/Wand/Muzzle`(투사체 발사 지점) · `Body/Wand/Rod/Tip/ChargeLight`(차지 표시) ·
  `Body/AimArrow`(조준 방향) · `Body/Wand/Muzzle/ChargeMotes`(파티클) ·
  `Body/Wand/Muzzle/Flash`(발사 섬광, 개발이 `albedo_color` 를 스킬 색으로 갈아끼운다).
  → **안전한 순서**: 새 메시를 `Body` 옆에 나란히 붙이고 → 위 노드들을 새 메시 손 위치로 옮기고 →
  둘을 번갈아 켜서 비교 → 괜찮으면 옛 `Body` 제거. **한 번에 갈아엎지 않는다.**
- ⚠ 그 밖에 확인해야 할 것: 고정 -32° 카메라에서의 실루엣(디자이너 리포트의 「얼굴이 화면에
  한 픽셀도 없다」 진단이 새 모델에서도 재발할 수 있다) · 캡슐 콜리전(반지름 0.35 / 높이 1.7)과
  메시 크기 정합 · 폴리곤 수.

### 2026-08-01 밤 ~ 2026-08-03 — **현황판에 한 줄도 안 적혀 있던 22커밋** (이번 최신화로 복원)

> 아래는 커밋 로그와 **실제 코드를 대조해서** 확인한 것이다. 커밋 메시지만 믿지 않았다.

- 🔴 **QA 6차(08-02 18:08) 판정 「이건 게임이 아니라 사격장이다」 — 그날 밤에 3건 다 해소됐다.**
  QA 가 잰 시점과 고쳐진 시점이 4시간 차이라, **QA 6차 리포트만 읽으면 지금 상태를 오해한다.**

  | QA 6차가 지적한 것 | 지금 (코드 확인) |
  |---|---|
  | 플레이어가 죽지 않는다 (`hp`·`take_damage` 없음) | ✅ `Player.gd:150 hp = 100.0` · `:303 take_damage` · `:321 _die` |
  | 투사체가 플레이어를 못 맞힌다 (레이어 겹침 0) | ✅ 마스크 `(1<<0)|(1<<1)|(1<<2)` — 플레이어 레이어 2 **포함** |
  | 적이 없다 (봇 스크립트 0개) | ✅ `Bot.gd` **429줄** — 같은 `Balance`·같은 `Projectile` 로 쏘고, 자기 탄은 무시 |
  | 소리가 하나도 없다 | ✅ `Sfx.gd` `AudioStreamPlayer` 풀 + 절차적 생성 |

- **[개발] 봇 대전 + 플레이어 체력/사망** `b6b999a` — 「드디어 상대가 생겼다」.
  ⚠ 이로써 **N13(길쭉함이 움직이는 표적에 관대한가)** 을 처음으로 잴 수 있게 됐다. **아직 안 쟀다.**
- **[개발] 봇 회피(대쉬)** `f386688` — 기획 [E-1] 1순위. `dash_speed 22.2` / `time 0.18` / `cool 3.0`.
  **봇이 회피를 못 하면 「회피를 뺏는」 효과(매혹)의 값을 못 정한다**는 이유였다.
- **[개발] 발사 손맛 1·2차** `17ef385` `57c0ff0` — 꼬리·회전·터짐·총구섬광·히트스톱 →
  폭발 파편·충격파 링·소리 4종. `Impact.gd` 신설.
- **[개발] 대쉬 / 걷기·시전 애니메이션 / 방향키 조준 / 화면 흔들림** `528ccc5` — `ScreenShake.gd` 신설.
  → **「D-b 캐릭터 애니메이션(지금 T포즈 고정)」은 이때 이미 끝났다.**
- **[개발] 타격감 있는 소리로 재작업** `6cf821c` · **도안첩(보관함) + 도안별 전적** `134a8b2`.
- **[아트]** 캐릭터 확대·재디자인 `fbad82d` · 발 박힘 수정 + 관절 피벗 구조 + 조준 화살표 `ec98955`.
- **[기획]** 원샷킬 제거(데미지 슬라이더 환율) `420db9a` · 재미 요소 조사 + 발사 형태 4종 `5802498` ·
  **스킬 효과 5종 설계 + 예산에 HP 통화 도입** `0d3b740` · **4픽셀 익스플로잇 차단** `7bdf1d3`.
- **[기획/신규 담당] 세계관·캐릭터성·싱글 모드 설계** `a75e827` — `reports/narrative.md` 신설.
- **[PM]** 사용자 2차 피드백 16건 분해 `4144621` · **온라인 진단 + 로드맵 재설계** `6f37fc1`.
  → **이 재설계가 아래 「⏭ 다음 할 일」의 근거다.**

### 이번 세션 (2026-07-31 밤 ~ 2026-08-01)

> ⚠ 담당 주의: **[D-1]·[S3]·[D-2]·홀수 탄·흩어짐 퍼짐각·`reach` 는 기획 담당이 `Balance.gd` 에 직접 구현했다.**
> 개발이 만진 것은 `Player.gd` · `SkillEditor.gd` · `SkillDB.gd` · `Projectile.gd` 쪽이다.
> 기획이 전 세션 통틀어 고친 파일은 `docs/게임기획.md` · `Balance.gd` · `reports/game-designer.md` **3개뿐**이다.

- **⚖ 대원칙 개정 — 정규화 기준이 「총 판정 면적」 → 「유효 명중 폭」** (사용자 승인 「바꾸는 쪽으로 가자」, 기획)
  - **왜 바꿔야 했나**: 면적을 고정하면 기대 데미지가 **1/√N**(N=탄 수)에 비례해서
    **둥긂이 영구 1위**가 된다 — 상수를 어떻게 만져도 수학적으로 회복 불가였다.
    새 기준의 닫힌 식은 `E = 2D × (탄반경 + 표적반경)` 으로 **N 이 상쇄되고 퍼짐각도 안 들어간다.**
    신설: `effective_width_for(면적) = 2√(면적/π)` · `WIDTH_MIN 1.00925` / `WIDTH_MAX 3.90882`.
  - 태그별 단일표적 기대 데미지 격차가 **5.25배(QA 1차) → 2.21배(2차) → 1.035배(3차)** 로 수렴.
    QA 3차 8,600발 실측, 점 표적 기준. 사람 크기(0.4m) 표적 기준은 **1.027배**.
  - 폭 정규화 자체는 **범위 1~100 × 태그 4종 400조합에서 최대 오차 0.0000000000 m** [QA 3차 1-D].
    QA 5차 재확인에서도 44조합 **오차 0.000000000000000 m**.
  - 남은 1~3%의 정체까지 규명됨 — 0.05m 스캔 격자 반올림(−1.5%) + 비스듬한 탄의 `1/cos θ`(+1~3%).
    ⚠ 그래서 **「퍼짐각은 기대 데미지를 1도 못 바꾼다」는 엄밀하게는 틀렸다**(≤4%, 형평성 문제는 아님).

- **🎮 로드맵 4단계 착수 — QWER 4슬롯** (사용자 승인: 「기획서에 한 줄 남기고 계속 진행해」)
  - 계기는 사용자 플레이 중 「WER 이 안써지긴해」. 승인 기록이 기획서 650~661행에 남았다.
  - **착수 범위는 QWER 4슬롯뿐이고 봇 대전은 착수하지 않았다.** 감사 5차가 `Dummy.gd`·`Arena.tscn`
    diff 로 「봇은 정말로 안 건드렸다」를 확인.
  - [실측, QA 5차] Q 입력 → 발사 → **Q 쿨 7.68 / W·E·R 0.00**(독립) → 곧바로 W 성공 →
    Q 7.16 / W 4.59. 쿨 중 재입력은 무시. **4슬롯 정상.**

- **🔧 미세 파라미터 배선 + 태그 정체성 확정** (기획 결정 → 개발 배선)
  - 종전 「상수는 `Balance.gd` 에 있는데 아무도 안 쓴다」 상태 해소. 그림의 지표가 탄 수·퍼짐각에 닿는다.
  - **홀수 탄 강제**(`to_odd_pellets`): 0/1→1 · 2/3→3 · 4/5→5 · 6/7→7.
    짝수는 정중앙 탄이 없어 **정조준 데미지가 0** 이 되는 구간이 있었다.
    [실측] 676지점 스캔에서 **0데미지 지점 0곳**.
  - **흩어짐 퍼짐각을 탄 수에 묶음**(`scatter_spread_for`): 덩어리 2·3 → **18°** / 4·5 → **24°** /
    6 이상 → **27°**. 종전 24° 고정일 때의 지배 전략(1.81배)이 **1.015~1.070배**로 소멸.
  - **뾰족함 하한 확정**: `CONE_ANGLE_MIN 32.0°` · `CONE_PELLETS_MIN 3`. 흩어짐 각도 상한 27° 와
    **분리 성립**(범위 1·18·50·100 전부).
  - 총 데미지 정합성 [실측]: 16조합 **전부 정확히 60.00**. 대원칙 무손상.

- **🔗 [D-2] 8방향 연결** — `NEIGHBORS` 에 대각 4방향 추가. 둘레(복잡도)는 **4방향 그대로**(의도적).
  - **「별을 그렸는데 산탄이 나간다」 소멸**: 별 576조합 중 흩어짐 판정 **223건 → 0건** [QA 4차].
  - ⚠ 부수 효과 **N14**: 「덩어리 수 = 탄 수」가 더 이상 성립하지 않는다(2·3→3발, 4·5→5발, 6↑→7발).
  - ⚠ 부수 효과 **N16**: 기획서 예시 「번개 = 뾰족함」이 사실과 다르다(덩어리 1 이 되며 **길쭉함**).

- **📏 `reach` 신설 (`distance` → `reach`)** — 「판정이 앞으로 뻗은 길이」를 사거리에 포함.
  - 종전엔 길쭉함 캡슐 절반이 사거리에 **덧붙어** 표시 34.0m ↔ 실제 41.1m(1.21배)였다(S8/N2).
  - [실측, QA 5차] 전방 도달점이 **네 태그 전부 20.75m**(표시 reach 21.00) — **태그 간 차이 0.00m**.
    항등식 `distance + forward_lead − reach` **최대 오차 0.000000000000000 m**. **S8/N2 해소.**

- **🔄 [D-1] 길쭉함 지표 교체 — 바운딩박스 종횡비 → 2차 모멘트 신장도** (미커밋)
  - `T_ASPECT_LONG` → **`T_ELONGATION_LONG = 2.500`**, `_FULL = 8.000`. `aspect` 키는 **뜻 그대로 유지**되고
    **`elongation` 키가 추가**됐다(화면이 조용히 거짓말하지 않게).
  - **「2.5:1 보다 길면 길쭉함」이라는 기획서 뜻이 한 글자도 안 바뀌었다** — 감사 6차가 항등식으로 증명:
    축에 나란한 직사각형에서 `√(l1/l2) = w/h` 는 **근사가 아니라 항등식**. 꽉 찬 직사각형 10종
    **오차 0.0000000000**(16×8→2.0 · 24×3→8.0 · 30×12→2.5 …).
  - 성과: 막대 8종 1,448지점 **오판 908 → 0** / 손떨림 획 285조합 **177 → 0**.
  - ⚠ **「회전 문제 해결됨」으로 읽지 말 것.** QA 판정은 **「명확히 길쭉한 그림(비율 2.9 이상)에 한해 해결」**.
    남은 것은 위 🔴 2번.

- **🧹 [S3] 잡픽셀 노이즈 제거** (미커밋) — 지표 5개가 전부 `_denoised` 마스크를 본다.
  - **「구석에 점을 찍어 종횡비·채움률·복잡도를 흔드는」 길은 완전히 막혔다** [QA 5차 3-A]:
    네 구석 1px(→뾰족함)·좌우 끝 1px(→종횡 2.46)·구석 3px 케이스가 **전부 기준 그림과 지표까지 동일**.
  - ⚠ **4픽셀 구멍은 그대로**이고, 이번에 **이득이 된다는 사실이 새로 밝혀졌다**(위 🔴 1번).
  - 부작용 기록: 3픽셀 이하만 그린 그림은 `filled` 0 · 덩어리 0 · 태그 둥긂이 되는데 **화면엔 점이 보인다.**
    거짓말은 아니지만 유저가 이유를 모른다 → **[D-16]** 로 접수(표시만 고치면 됨).

- **🖥 [D-14] 제작창 지표 표시 — 「화면이 거짓말한다」 다섯 번째 재발 없음**
  - `채움 · 신장 · 종횡 · 복잡 · 덩어리` 를 이름표와 값이 정확히 짝지어 띄운다.
  - QA 5차가 `SkillEditor` 를 실제로 세워 `_stat_label.text` 를 **문자 단위 대조** → **불일치 0 / 7**.
    대각 막대(신장 11.73 ↔ 종횡 1.00, 11배 차이)와 세로 막대(종횡 0.18)가 뒤바뀜 없이 뜬다.
  - **`aspect`(종횡)도 같이 띄우는 이유**: 대각선 막대는 종횡 1.00 인데 태그는 길쭉함이다.
    안 띄우면 유저가 판정 근거를 못 본다(기획서 87행 「예측 가능하게」).
  - 📌 **개발이 세운 규칙 — 지킬 것**: 「**제작창은 `box` 에서만 읽는다. 상수 직접 참조 금지.**」
    「화면이 실제와 다른 값을 띄운다」가 **네 번 재발**한 원인이 전부 같았다 —
    밸런스가 상수에서 **그림 의존 값**으로 바뀔 때마다 화면이 옛 상수를 계속 들고 있었다.

- **🔍 QA 2·3·4·5차 검증** (`reports/qa.md`) — 헤드리스 실측. 3차만 8,600발, 5차는 1,500발 +
  합성 마스크 약 20,000장. **1~4차 통과 항목 중 5차에서 새로 깨진 것 0건.**
  - 회귀 실측(5차): 자폭 **0/40조합** · 벽 차단 **12/12** · 투사체 누수 **210발 → 0개** ·
    저장/불러오기 **마스크 1024/1024 일치** · 예산 초과가 이득인 조합 **0** ·
    총 데미지 **12조합 전부 60.0000** · NaN 가드 정상 · 시체 방패 정상.
  - **기본 스킬 4개는 이번 개정 전후로 태그·지표·`derive` 값·실사격이 전부 불변**
    (Q 22.0 · W 45.0 · E 30.0 3발 전탄 · R 70.0). **저장된 유저 스킬이 바뀔 위험 없음.**

- **🛡 감시자 3·4·5·6차 감사** (`reports/auditor.md`) — 전부 **반려**였다가 사유가 하나씩 해소됐다.
  - 3차: 「S2 수정 뒤 아무도 재측정 안 함」 → QA 2차로 해소.
  - 4차: 「QWER 를 승인 기록 없이 착수」 → 기획서 인용문 블록으로 해소 / 형평성 → QA 3차로 해소.
  - 5차: 「리포트를 쓴 뒤 코드를 또 고쳤다(세 번째 재발)」 → 개발 리포트 보강으로 해소.
  - 6차: **「기획서가 남은 결함을 사실보다 작게 적었다」** — 아래 「🔧 진행 중」 참조.
    코드 쪽은 **8건 중 7건 통과**, 감사관이 「이번 묶음의 코드는 이 프로젝트에서 가장 깨끗한 편」이라고 적었다.
  - ✅ 절차 개선이 실제로 작동했다 — 「리포트를 안 쓴 채로 다른 담당에게 넘기지 않는다」가 지켜져
    세 번 재발했던 **작업 기록 누락이 6차에서 0건**.

- **🐞 그 밖에 이번 세션에 닫힌 것**
  - **[S5] 1픽셀 그림 익스플로잇 해소** — 그린 부분 bbox 를 판정 크기로 늘린다.
    1픽셀 그림의 보이는 크기 = 판정 지름 **3.909m**(종전 0.122m, 32배 차이). 「조금만 그리면 이득」 소멸.
  - **[S9] NaN 잠김** — `derive(NAN,NAN)` → 쿨 0.611 / 발동 0.130. 정상.
  - **[S6] 슬롯 전환 시 미저장 편집 소실** — `_dirty` 추적 + 슬롯 전환 시 `ConfirmationDialog`
    (「버리고 옮기기 / 돌아가기」) + 저장 버튼에 **`저장 ●`** 표시. **구현은 됐으나 미검증**(아래 C-2).
    ⚠ **창을 닫으면 편집은 그대로 사라진다** — 차단이 아니라 알림만 넣은 것이다.
  - **[S11] 마스크 크기 경고** — `SkillDB.gd:167` 에 `push_warning` 들어감(코드 확인).
  - **[S12] 일시정지 중 부활 타이머** — `create_timer(t, false)` 로 수정(감사 5차 diff 확인).
  - **[D-13] 제작창 산탄 퍼짐각 표시** — 17개 케이스 표시↔실제 **불일치 0건**.
  - **N1(흩어짐 「사거리 전체」 거짓말) · N7(흩어짐 태그 안 격차 1.81 → 1.029~1.053배) ·
    N8(짝수 탄 0데미지) · N9(전방 도달점)** 전부 조치 완료.

### 이전 세션 (요약)

- **로드맵 1 → 2 → 3a → 3b** 완료: 방향키 이동+점프 → 고정 스킬 1발 → 에디터 수치 반영 →
  **그린 32×32 마스크가 그대로 `Sprite3D` 투사체 겉모습**(판정은 태그가 고른 프리셋).
  `Player.gd` 는 저장된 태그를 믿지 않고 **매번 마스크에서 다시 뽑는다.**
- **QA 1차 치명 버그 5건 수정**: S1 스폰 자폭(Q·R 이 한 발도 안 나갔다) · S2 부채꼴 퍼짐각 분리 ·
  S4 캡슐이 등 뒤 5m 를 때림 · S7 시체 방패 · S9 NaN 잠김.
- **밸런스 재조정**: 예산 초과 = **③ 초과분 페널티**(쿨 ×(1+초과율×3.0) / 발동 ×(1+초과율×1.5)).
  10,000조합 전수 검산에서 **초과가 이득인 조합 0개.** 쿨타임 곡선 교정(`COOLDOWN_CURVE 1.6` 신설,
  DPS 최대/최소 비 **2.79 → 1.52배**). 길쭉함 캡슐 길이 폭주 차단(범위 100 에서 26.7m → 억제).
- **아트 1차**: 캐릭터 = **2등신 마법사**(색은 파랑 2톤 + 목도리 노랑만 — 목도리는 팀 구분색 자리).
  아레나 = **거대한 원룸 한 칸**(가구마다 전투 의도: 책 3권 = 유일한 고지대 진입로, 의자 등받이 =
  남북 시야 차단벽, 머그컵 = 원기둥 엄폐). 창문을 서쪽에 둔 이유 = 북쪽 벽은 카메라 고정각상 언제나 배경.
- **✅ 사용자 확정 사항**: 3D 3인칭 + 배그 요소 제거(ADS·줌·스프린트 없음, 「배그처럼」은 자기장 한 곳만) ·
  카메라 고정각(yaw 0° / pitch −32° / 7m) · 그림의 역할 = **하이브리드**(겉모습 + 형태 태그,
  세기는 수치 슬라이더 전담).
- **인프라**: `setup.ps1` 원클릭 셋업 · `SessionStart` 환경점검 훅 · 자동 체크포인트 훅 3종 ·
  `office/` 개발실(지시창 → `claude -p` → 자동 커밋·푸시·보고) · 감시자(auditor) 에이전트 도입.

---

## 🔧 진행 중

- **[기획] 감사 6차 차단 사유 해소 — `docs/게임기획.md` 「알려진 판정 구멍」 문단 수정**
  - 차단 사유: 기획서가 「[D-1] 은 길쭉함 축을 **회전 불변으로 만들었다**」·「길쭉함 여부는
    **안 흔들리는데** 둥긂↔뾰족함이 흔들리는 조합 50개」라고 적었는데 **사실이 아니다.**
    QA 5차 실측: **50 은 맞고 앞 절반이 틀렸다 — 길쭉함 여부가 갈리는 조합이 17개 더 있다.**
  - **코드는 되돌릴 것이 하나도 없다.** 기획서 한 문단 + 「해결」 표기 두 곳이 전부.
  - 상태: **기획 5회차 리포트에 「고쳤다」고 기록돼 있다**(절 제목을 「부분 해결」로, 「먼저 읽을 것」
    상자 추가, 「길쭉함 여부는 안 흔들리는데」 삭제, 표본의 한계 명시 등 7가지).
    ⚠ **감사 7차 재검사는 아직 없다.** 커밋 전에 확인할 것.
  - 기획이 `docs/게임기획.md` 와 `reports/game-designer.md` 를 쓰고 있다 — **다른 담당은 이 둘을 건드리지 말 것.**

---

## ⏭ 다음 할 일 (우선순위 순)

> 표기: `[담당]` 은 코디네이터가 리포트 내용으로 추정한 것이다. 최종 배정은 PM/사용자 몫.

> 🔄 **2026-08-03 최신화** — 아래 표에서 **끝난 것 5건을 걷어냈다**:
> `A-3` 미커밋 커밋(지금 미커밋 0) · `B-1` 4픽셀 구멍(`7bdf1d3`) · `D-a` 발사/피격 이펙트 ·
> `D-b` 캐릭터 애니메이션 · 🅴 「봇 대전 착수도 안 함」(`b6b999a`).
> 그리고 **PM 이 08-02 에 로드맵을 재설계**(`6f37fc1`)해서 🅴 를 통째로 갈아끼웠다.

### 🅰 게이트 — 이것부터
| # | 할 일 | 담당 | 근거 |
|---|---|---|---|
| A-1 | **사용자 직접 플레이 → 재미 확인** (위 📌 1번). **여섯 세션째 미뤄졌다.**<br>⚠ 08-03 에 사용자가 켜서 본 것은 **배경 아트뿐**이다(「일단 좋아」). **「그리는 게 재미있는가」는 아직 아무도 확인 안 했다** — 이게 이 게임의 전부인데도. **봇이 생겨서 이제 진짜로 잴 수 있다** | 사용자 | 기획서 87~88행 |
| ~~A-2~~ | ✅ **[해소 08-04] ❓3 제작창 시점 — 사용자가 답했다.** 아래 「📌 4️⃣」 참조. **온라인 설계에서 가장 어려운 문제 하나가 사라졌다** | 사용자 | 사용자 결정 08-04 |
| A-2b | 🆕 **로비/창고 화면을 만든다** — 결정에 따라 **매치 전 세팅 화면**이 필요해졌다. **도안첩(창고)은 이미 있다**(`134a8b2`) — 없는 건 **그걸 여는 매치 전 흐름**이다. PM 5-7(타이틀→로비)과 **같은 작업** | developer + designer | 사용자 결정 08-04 · PM 5-7 |
| A-3 | ⛔ **격자 32→64 결정** — 회전 흔들림(아래 🔴 2·3번)의 **유일한 근본 해법**이고 저장 포맷·`SkillCanvas`·스프라이트가 전부 걸려 있다 | 사용자 | 기획 처방 5번 |
| A-4 | 🆕 **PM 이 ❓2 답을 받아 5단계를 재검토** — **매치메이킹·방 만들기/초대**가 로드맵에 없다(위 📌 2️⃣). ❓1(접속 방식) 선택지도 (A) 스팀 단독으로는 안 덮인다 | pm | 사용자 결정 08-03 |

### 🅱 높음 — 「그리는 재미」를 직접 죽이고 있는 것
| # | 할 일 | 담당 | 근거 |
|---|---|---|---|
| ~~B-1~~ | ✅ **[해소 08-03 `7bdf1d3`] 4픽셀 구멍** — `T_BLOB_RELATIVE_MIN 0.25` 로 닫혔다. 상세는 위 「🔴 지금 살아 있는 문제」 1번.<br>⚠ **딸린 것 하나 남았다**: 뾰족함 정조준 **QA 실측 26.222 대 기획 계산 25.630 (2.3% 차)** 실사격 재확인 | qa | 기획 `7bdf1d3` 커밋 본문 |
| B-1b | 🆕 **N13 을 이제 잴 수 있다 — 「길쭉함이 움직이는 표적에 관대한가」.** 봇이 08-02 에 들어와서 5세션째 미뤄진 이 측정이 가능해졌다. **길쭉함과 둥긂은 정지 표적 상대로 수치가 한 자리도 안 다르다** — 움직이는 표적에서도 같으면 **길쭉함은 정체성이 없는 것이다**(⛔ 7번과 직결) | qa → game-designer | QA 5차 N13 · `b6b999a` |
| B-2 | 🔴 **제작창 「경계 게이지」**(= 기획 처방 1번 = **[D-3]**) — 신장도 + 문턱 + **흔들림 띠** 표시, 「기울이면 바뀝니다」 경고. **전투 수치 변경 0 · 위험 0.** 회전 구멍을 못 없애도 **예측 가능**하게 만드는 유일한 공짜 수단.<br>⚠ **[D-15]: 게이지는 새 상수 `T_ELONGATION_LONG` 을 볼 것**(`T_ASPECT_LONG` 은 코드에 한 글자도 안 남았다) | designer + developer | 기획 리포트 9장 **1순위** |
| B-3 | **[D-16] 제작창에 「잡픽셀(4픽셀 미만)은 판정에서 뺐다」 한 줄.** B-2 와 **같은 화면**이라 묶어서 | developer | 기획 [D-16] · 감사 ⚠5 |
| B-4 | 🟠 **복잡도 회전 흔들림** — 둘레 √2 보정 + `T_COMPLEXITY_SHARP` 재검산 + 뾰족함 정체성 재검증. ⚠ **혼자 하는 독립 묶음이어야 한다**(태그 판정 두 개를 같은 묶음에서 흔들지 마라) | game-designer → developer | QA 5차 2장 · 기획 9장 2번 |
| B-5 | 🟠 **1픽셀 꼬리** — 복잡도 지표 자체의 성질. 둘레를 대각 √2 로 세는 것과 함께 볼지 기획 판단 | game-designer | QA 5차 3-D |
| B-6 | **[QA-8] 문턱 근처 막대(10×4·15×6·13×5·17×7)를 0°/30°/45°/60° 로 그려 실사격.** 회전 흔들림 숫자는 세 사람 모두 **합성 마스크 계산**이고, 계산과 실측이 갈렸던 전례가 있다. **문서에 쓸 정확한 숫자가 여기서 나온다.**<br>⚠ 기획이 QA 에 넘긴 재측정 요청은 **5회차 7건 + [QA-8] = 8건 대기** 중이다 | qa | 기획 7장 · 감사 ⚠1 |

### 🅲 중간
| # | 할 일 | 담당 | 근거 |
|---|---|---|---|
| C-1 | **격자 32→64 이관** (⛔ 3번 승인 시) — 저장 포맷 · `SkillCanvas` · 스프라이트 · `Balance.GRID` 전부 걸린다 | developer | 기획 처방 5번 |
| C-2 | **[S6] 확인창 실제 검증** — 확인창·`_dirty`·`저장 ●` 은 **이미 들어갔는데 다섯 세션째 아무도 눌러보지 못했다**(헤드리스로 버튼 클릭 불가). **사용자 플레이 때 같이 확인.** 겸사겸사 「창을 닫으면 편집이 사라지는」 동작이 괜찮은지도 볼 것 | qa / 사용자 | QA 5차 7장 · 개발 리포트 |
| C-3 | **[N15] 흩어짐 5·7발의 두 번째 낙폭이 화면에 없다** — 5발 60→**36**→**12**, 7발 60→43→26→9 인데 화면은 첫 문턱만. 거짓말은 아니고 **정보 부족** | developer | QA 4·5차 |
| C-4 | **[D-4] 예산 초과 페널티 배수 표시** — 지금은 `예산 X / Y — 초과!` 만 뜨고 **`쿨타임 ×2.8` 같은 배수가 안 보인다**(코드 확인). `derive()` 가 `over_cooldown_mult` 를 이미 돌려준다(**값에 반영돼 있으니 다시 곱하지 말 것**) | developer | 기획 D-4 |
| C-5 | **[N5] 큰 둥긂이 머리 위 2.034m 에서 태어난다** — 기획서 12행 「마법봉으로 마법을 쓰는 연출」과 어긋남. 디자인 확인 | designer | QA N5 |
| C-6 | **[N14] 「덩어리 수 = 탄 수」 문구 정정** — 홀수 강제로 2·3→3발, 4·5→5발, 6↑→7발이 됐다. 덩어리를 8·12개 그려도 7발에서 잘려 **그림의 표현이 전투에 안 닿는다**. 세기 영향 없음(문서만) | game-designer | QA 4차 N14 |
| C-7 | **[S10] `max_range_for(100)` 이 예산 초과 조합을 돌려줌**(함수 계약 위반). 현재 호출처 없는 죽은 코드 | developer | QA S10 |
| C-8 | **[N6] 접선에 가까운 명중이 이산 이동에 걸려 통과한다** — 지금 규모(0.1~0.2m 손실)에선 무시 가능. **4단계에서 빠르고 작은 투사체가 들어오면 커진다** | developer | QA N6 |
| C-9 | **[D-6] 다색 그림(팔레트 2~4색)** — `mask` 가 이미 `PackedByteArray` 라 `0=빈칸, 1~4=색`. **밸런스 코드 한 줄도 안 고쳐도 된다.** 색은 겉모습 전용, 판정 반영 금지 | developer | 기획 D-6 |
| C-10 | **스킬 에디터 UI/UX 아트** — 「이게 이 게임의 얼굴인데 아직 손을 안 댔다」. B-2 와 같이 하면 효율적 | designer | 디자인 리포트 |
| C-11 | **마법봉 끝 색을 스킬 색으로** — 지금 고정 주황. 아트 쪽 준비는 끝났다 | developer | 디자인 리포트 |

### 🅳 낮음
| # | 할 일 | 담당 | 근거 |
|---|---|---|---|
| ~~D-a~~ | ✅ **[해소 08-02]** 발사/피격 이펙트·화면 흔들림 — `17ef385`(꼬리·회전·터짐·총구섬광·히트스톱) + `57c0ff0`(폭발 파편·충격파 링·소리 4종) + `528ccc5`(`ScreenShake.gd`).<br>**남은 것: 데미지 숫자만 아직 없다** | designer | 커밋 확인 |
| ~~D-b~~ | ✅ **[해소 08-02 `528ccc5`]** 걷기·시전 애니메이션 — 「T포즈 고정」은 더 이상 사실이 아니다 | designer | 커밋 확인 |
| D-b2 | 🆕 **AI 3D 캐릭터 교체 (진행 중)** — 컨셉 아트 완료(`docs/concept/wizard.png`), 메시는 크레딧 대기. ⚠ **통째 교체 금지** — `Muzzle`·`AimArrow`·`ChargeLight`·`Flash` 는 아트가 아니라 기능이다. 위 「캐릭터 3D화」 항목의 안전한 순서를 따를 것 | designer | 사용자 요청 08-03 |
| D-c | 길쭉함 4픽셀 가로선의 **보이는 크기 7.036m ↔ 판정 지름 3.909m(1.8배)** — 거짓말은 아니나 겉모습 손볼 때 기억할 것 | developer | QA 5차 6장 |
| D-d | `office/` 의 안 쓰는 원본 코드 정리 / 개발실 작업 도중 취소 버튼 | — | 이전 세션 |

### 🅴 로드맵 — ⛔ **PM 제안 · 사용자 승인 대기** (08-02 `6f37fc1`)

> 🔴 **감사 7차 위반 1 — 2026-08-08 정정.**
> 종전에 이 절은 「종전 선형 1~6단계는 **폐기다**」「기획서와 이 표가 다르면 **이 표가 최신**」이라고
> 단정했다. **틀렸다.** PM 본인이 네 곳(`pm.md:559,571,581,716`)에서
> 「**기획서는 내가 못 고친다 — 제안만**」이라고 못박았는데, 코디(메인 세션)가 그것을
> 정본으로 승격시키고 5-1~5-7 에 담당까지 배정했다.
> `CLAUDE.md` 세션 시작 절차 2 가 「STATUS 를 읽고 거기서 이어서 한다」이므로,
> 다음 세션은 기획서가 아니라 이 표를 따라갔을 것이다 — **미승인 제안이 사실상 정본이 될 뻔했다.**
> 2026-07-31 로드맵 건너뜀 사고와 같은 계열이고, 이번에는 코드가 아니라 문서 형태였다.
>
> **정본은 여전히 `docs/게임기획.md:1736~1741` 의 선형 로드맵이다.**
> 아래 표는 **승인받기 전까지 참고 자료**다. **이 표를 근거로 착수하지 마라.**
> (5단계 코드는 아직 0줄이라 되돌릴 것은 없다.)
> ⛔ **사용자 결정 대기**: 이 재설계를 채택할 것인가 — 특히 **자기장을 5단계 → 8단계로 미루는 것.**

**왜 바꿨나** — 새로 들어온 요구 6개 중 **어느 하나도 옛 5단계(자기장)에 안 들어간다.**
그리고 **자기장은 5v5 가 성립해야 의미가 있는 장치**(강제 교전)인데 5v5 가 없다.
→ **자기장이 5단계에 있는 것 자체가 순서 오류였다.**

| 단계 | 이름 | 상태 |
|---|---|---|
| **4** | QWER + 봇 | 4a 슬롯 ✅ · 4b 손맛 ✅ · 4c 조작 ✅ · **4d 발사 형태 배선 🟡** · 4e 봇 1마리 ✅ → **거의 끝** |
| **🆕 5** | **「한 판(Match)」을 만든다** = **네트워크 준비 리팩터.** 온라인·5v5·싱글·맵5개의 **공통 선행조건** | **다음** |
| **🆕 6** | **효과 축** — 대폭발·매혹·실드·반사. **5 뒤여야 한다** | 대기 |
| **🆕 7** | **온라인** — 5 가 끝나 있으면 **얹는 일**이 된다 | 대기 |
| **8** | 자기장 + 라운드 타이머 (옛 5단계) — **5v5 가 성립한 뒤에 의미가 생긴다** | 뒤로 밀림 |
| **병렬 C** | **콘텐츠 축** — 스토리·세계관·캐릭터성 → 배경 아트 → 맵 5개. **코드 위험 0** | **지금 병렬 가능** |

#### 5단계 쪼개기 — 이게 다음 실행 계획이다
| # | 할 일 | 담당 | 무엇을 여는가 |
|---|---|---|---|
| **5-1** | **Match 관리자 신설.** 투사체 스폰·봇 스폰·부활 지점을 `Player.gd` 에서 여기로 | developer | 온라인·5v5·팀봇·라운드 **전부** |
| **5-2** | **판정을 한 곳으로.** `take_damage` 호출을 단일 창구로 → **나중에 그 한 줄만 RPC 로 바꾸면 서버 권위** | developer | 🔑 **사용자 결정 ❓2(B)의 직접 준비 작업** |
| **5-3** | **로직 / 연출 분리** — `Engine.time_scale` 히트스톱, `Bot._die()` 의 `shake_kill()`, `Sfx.play` | developer | 온라인 (아니면 호스트가 전원을 멈춘다) |
| **5-4** | 🎨 **맵 씬 계약 확정** — 맵은 **지형과 스폰 지점만**. `Player`·`SkillEditor`·`SkillHud` 를 맵에서 뺀다 | developer + designer | ⚠ **이게 나오기 전에 맵을 그리면 5개를 다시 만든다** |
| **5-5** | **팀 개념** — 소속·팀 스폰·아군 오사 규칙·판정 레이어에 팀 추가 | game-designer → developer | 3v3 · 5v5 |
| **5-6** | **승패·라운드·타이머·스코어** ⛔ **기획서 빈칸 5번(승리 조건/리스폰) 미정 — 사용자 결정** | 사용자 → game-designer | 싱글 · 온라인 |
| **5-7** | **타이틀 → 로비 → 매치 흐름 + 일시정지/종료** (QA 1-F 「창 닫는 것 말고 나갈 방법이 없다」) | developer | 🆕 **매치메이킹·방 코드가 붙을 자리** |

---

## ⛔ 막힌 것 / 결정 대기 — **사용자만 정할 수 있는 7건 (+ 작은 것 3건)**

> 🔄 **2026-08-03 — 이 목록 말고 PM 리포트에 별도로 ❓1~5(온라인)가 있다. 헷갈리지 말 것.**
> 아래 7건은 **기획(밸런스·태그) 결정**이고, PM 의 ❓1~5 는 **구조(온라인·권위·로비) 결정**이다.
> - ✅ **PM ❓2(권위 모델) 해결** — 사용자 답 「**모르는 사람들끼리도 되고, 아는 사람들끼리는
>   방을 파고 초대해서도**」. 상세와 파급은 위 「📌 2️⃣」 참조.
> - ✅ **PM ❓3(제작창 시점) 해결** — 사용자 답 「**게임 전에 창고에서 미리 세팅**」(= 선택지 A).
  상세와 설계 제약(`paused` 를 쓰지 말 것)은 위 「📌 4️⃣」 참조.
> - 아래 7건 중에서는 **3번(격자 32→64)** 이 가장 많은 것을 막고 있다 → 🅰 A-3.
>
> 기획·개발이 임의로 정하면 기획 이탈 방지 규칙 위반이다.
> 감사 6차가 **「코드가 이 항목들을 앞지르지 않았다」를 전체 검색으로 확인**했다 —
> 관련 상수는 **정의만 있고 호출부 0건**이다.

### 🔴 1. **형태 태그 표 「체감」 칸을 고칠지**
낡은 서술 **2건**:
- **길쭉함 「조준 어려움」** — 폭 정규화 개정으로 **네 태그 좌우 폭이 전부 2.8546m 로 같아졌다.** 사실 아님.
- **흩어짐 「견제형」** — 덩어리 2~3개면 **3발**이라 2단 계단이 아니라 **1단 절벽**이다.

기획은 사실만 기록하고 표는 안 고쳤다(감사 5차·6차가 「절차대로다」로 확인).
→ **결정: 표 문구를 사실에 맞게 고칠지 / 그대로 둘지.**

### 🔴 2. **형태 태그 표 「판정 조건」 칸 수정 추인** (감사 ⚠3, 신규)
[D-1] 로 「종횡비 ≥ 2.5 또는 ≤ 0.4」 → 「신장도 ≥ 2.5」로 바뀌었다.
감사 6차는 **기획 담당 권한 안**이라고 판정했다(그 숫자를 정한 것이 사용자가 아니고, 축에 나란한
직사각형에서 두 지표가 같은 수라 **뜻이 안 바뀌었다**). 다만 **기획서 118행이 「표 문구 갱신은
사용자 승인 사항」이라고 적어 뒀고 그 줄은 안 고쳤다** — 문서 안에 부딪치는 두 문장이 남는다.
→ **결정: 아래 한 줄을 추인해 주면 끝난다.**
> 「2026-08-01 판정 조건 칸은 지표 개정에 따른 기록 정정으로 기획 담당이 수정」

### 🔴 3. **격자 해상도 32×32 → 64×64** (신규 — 위 🔴 2번 경고의 유일한 근본 해법)
- 얻는 것: 래스터화 흔들림 **25.6% → 8.1%**(128 이면 1.6%). **원인을 직접 없애는 유일한 후보.**
- 대가: **저장 포맷 · `SkillCanvas` · 스프라이트 · `Balance.GRID` 가 전부 걸리고, 그리는 손맛이 달라진다.**
- 기획이 검토한 다른 후보 5개의 결론: 데드존/히스테리시스는 **이 문제에 안 듣고**(유저는 회전 버튼을
  누르는 게 아니라 기울여 새로 그린다), AA 마스크는 **문턱 2.5 의 근거인 항등식을 깬다**(16×8 → 1.915),
  태그 절벽 제거는 **태그 4종이 사용자 확정 구조**라 4단계 이후.
→ **결정: 64 로 올릴지 / 32 유지하고 「경계 게이지」(B-2)로 예측 가능하게만 만들지.**

### 🔴 4. **대원칙에 「정조준 데미지는 균등화 대상이 아니다」 문안을 넣을지**
개정된 대원칙이 균등화하는 것은 **유효 명중 폭 = 기대 데미지**이고, **정조준 데미지는 태그마다 다르다.**
0~사거리 평균 [기획 검산]: 둥긂·길쭉함 **60.0** / 흩어짐 41.3~42.0 / 뾰족함 27.2~32.1 = **2.21배**.
[QA 5차 실측] 둥긂 60.000 ↔ 별5 뾰족함 **26.222** = 2.29배.
기대 데미지 격차는 **1.035배**로 지켜지고 있다. 이게 **설계 의도인지 결함인지가 문서에 없다.**
기획이 문안 초안만 「⏳ 사용자 확인 필요」 절에 적어 뒀고 **대원칙 블록·`Balance.gd` 주석 어디에도 안 넣었다**
(감사 5차·6차 확인).
→ **결정: 이 문장을 대원칙에 넣을지.** 넣으면 위 🔴 3번 경고의 「1.94배」가 「알려진 사양」이 되고,
   안 넣으면 「고쳐야 할 결함」이 된다. **다음 세션 우선순위가 여기서 갈린다.**

### 🟠 5. **둥긂에 관통을 도입할지**
채움률이 낮으면(< 0.35) 최대 2명 관통, 대신 **총 데미지를 관통 수로 나눈다**(한 명만 맞히면 절반).
제로섬이라 형평성은 안전하지만 **새 메커니즘**이라 승인이 필요하다.
상수 `T_FILL_PIERCE` · `PIERCE_MAX` 는 **정의만 있고 호출부 0건**(감사 확인).
→ **결정: 넣을지 / 미룰지.**

### 🟠 6. **캐릭터 HP 100 → 150?**
지금 허수아비 HP 100 이라 **데미지 100 스킬이 원샷킬**이다. 5v5 에서 원샷은 과하다.
150 이면 최대 한 방이 HP 의 2/3 라 반드시 두 번 맞혀야 한다.
**기획서에 HP 수치가 정해진 적이 없어** 제안값으로만 기록했고 `Dummy.gd` 는 안 건드렸다(감사 확인).
→ **결정: 100 유지 / 150 / 다른 값.**

### 🟠 7. **[N13] 길쭉함의 정체성 — 「창을 그리면 뭐가 달라지나」에 답이 없다**
[실측] 정지 표적 상대로 **둥긂과 한 자리도 다르지 않다**:

| | 둥긂 | 길쭉함 |
|---|---|---|
| 유효 명중 폭 | 2.8546m | 2.8546m |
| 좌우 밴드(10m) | −1.40 ~ +1.40 | −1.40 ~ +1.40 |
| 기대 데미지 적분 | 171.00 | 171.00 |
| 정조준 데미지 | 전 거리 60 | 전 거리 60 |
| 전방 도달점 | 20.75m | 20.75m |

기획이 남겨둔 유일한 차별점은 **체류 시간**이다 — 범위 50 에서 캡슐 5.14m ÷ 20m/s = **0.26초**,
6m/s 로 움직이는 표적 상대로 **실효 폭 약 +1.5m**. **봇이 들어오기 전에는 확인 불가 [미검증]**.
이번 개정으로 **길쭉함 판정을 받는 그림이 오히려 늘어서**(대각선 획이 전부 길쭉함) 「4가지 유형이
실질 3가지」인 상태가 더 눈에 띈다.
→ **결정: 길쭉함에 고유 성질을 줄지 / 4단계(봇)까지 판단 보류할지.**

### 🟡 8. **작은 것 3건 — 한 번에 같이 답해 주면 된다** (기획 8장 3번·5번에 묶여 있음)
- **(a) 새 태그(설치·회전)를 언제 신설할지** — 기획서 47행이 「회전/부메랑/설치」를 예시로 뒀다.
  특히 **「캔버스의 어디에 그렸는가」**를 읽으면 지금 낭비되는 캔버스 공간 절반을 회수한다.
  **새 메커니즘**이라 승인 필요. 기획 담당은 **4단계(봇) 이후 재상정** 권고.
  → **결정: 4단계 이후로 미룰지 / 지금 볼지.**
- **(b) 예산 초과 페널티 추인** — `쿨 ×(1+초과율×3.0)` / `발동 ×(1+초과율×1.5)` 로 이미 굴러가고 있다.
  10,000조합 전수 검산에서 **초과가 이득인 조합 0개**. 사후 추인만 받으면 된다.
  → **결정: 추인 / 다시 볼지.**
- **(c) [N16] 기획서 예시 「번개 = 뾰족함」 정정** — 세로로 긴 번개는 신장도 **5.07** 이라
  **길쭉함이 먼저 걸린다.** 판정 순서(덩어리 → 길쭉함 → 복잡도) 때문이지 코드 버그는 아니다.
  → **결정: 예시를 바꿀지 / 판정 순서를 바꿀지(후자는 대원칙 검산 재실행이 붙는다).**

### 🔵 (참고 — 결정 사항 아님)
- **허수아비(`Dummy`) 존치**: 기획서에 없지만 감사가 **검증용 표적**으로 사유를 남기고 통과시켰다.
  움직이지도 반격하지도 않아 게임 규칙에 영향 없음. **4단계에서 봇이 들어오면 삭제 대상.**
- **비용 공식 곱셈 전환**: 기획 의견 「지금 수치로도 검산상 균형이 맞아 당장은 안 바꿔도 된다」.
  기획 6회차 승인 대기 목록에서는 빠졌다.
- **`데미지 100 / 범위 1`** 이 순수 DPS 10.48 로 전 조합 최강(초과가 0.6% 뿐이라 페널티가 거의 안 붙는다).
  사거리 8.3m 짜리 **초근접기**라 QA 가 버그로 판정하지 않고 관찰만 남겼다. **근접 난입 메타가 나오면 재검토.**

---

## 🗒 셋업 시 지시서와 다르게 처리한 것 (기록용)
1. **프로젝트 위치**: 지시서는 바탕화면을 루트로 쓰라고 했으나, 바탕화면에 신분증·통장 사본이 있어
   GitHub 유출 위험이 있었음. 사용자 확인 후 `C:\Users\k\SkillCraftBattle` 로 분리.
2. **`.claude/settings.json` 훅 형식**: 원본은 `"command"` + 별도 `"args"` 배열이었으나 Claude Code 훅은
   `args` 를 인식하지 않아 실행되지 않음. 한 줄 명령으로 병합.
3. **Git 사용자 정보**: 이 저장소에만(local) `wfs070809 / wfs070809@gmail.com` 설정.

## 🔎 환경 메모
- Git 설치·PATH 등록됨. 원격: `origin` → https://github.com/Parksungchan1/mymakegame.git (인증 완료)
- Godot 4.7.1: `tools\Godot\Godot.exe` (setup.ps1 이 받아둔 무설치본. 저장소에는 안 올림)
- Node.js: v24.18.1

### 컴퓨터 2대에서 작업 중
| | 경로 | 비고 |
|---|---|---|
| 1호기 | `C:\Users\k\SkillCraftBattle` | 처음 만든 곳 |
| 2호기 | `C:\Users\parks\SkillCraftBattle` | 클론 + `setup.ps1` 로 5분 만에 이어받음 |

- **⚠ 2호기 주의**: `C:\Windows\System32\node` 에 **확장자 없는 0바이트 파일**이 있다.
  PowerShell 이 진짜 `node.exe` 대신 이걸 먼저 실행한다(cmd 는 영향 없음).
  점검 스크립트는 `node.exe` 로 찾도록 고쳐서 지금은 문제없지만, **PowerShell 에서 `node` 를 직접
  부르는 코드를 새로 쓸 거면 `node.exe` 로 쓴다.**
  관리자 권한으로 그 빈 파일을 지우면 근본 해결: `Remove-Item C:\Windows\System32\node -Force`
- **Windows 주의**: 한글을 Node 에서 명령줄 인자로 넘기면 시스템 코드페이지로 인코딩돼 `???` 로 깨진다.
  git 커밋 메시지는 UTF-8 파일 + `-F`, claude 프롬프트는 stdin 으로 넘긴다. (커밋 `5ad68cb` 이 깨진 채 남아 있음)
- **체크포인트 훅**: `SKILLCRAFT_OFFICE_JOB=1` 이면 `checkpoint.ps1` 이 커밋을 건너뛴다.
- **과금**: API 키 없이 계정 로그인(OAuth) 방식이라 지시 실행은 구독 사용량을 소모한다.
  화면의 `$0.xx` 는 실제 청구액이 아니라 작업 무게 환산값. 플랜 확인은 `/status`.

### QA 헤드리스 검증 시 주의 (1~5차 누적)
- 실행: `.\tools\Godot\Godot.exe --headless --fixed-fps 60 --path "<repo>\game" --script "res://_qa_xxx.gd"`
- `--script` 로 돌리면 오토로드를 전역 이름으로 못 쓴다. `root.get_node("Balance")` 로 받고,
  `_initialize()` 시점엔 오토로드가 아직 없으므로 `_process` 첫 호출까지 기다린다.
- **`preload()` 가 깨진다** — 오토로드 이름을 참조하는 스크립트는 컴파일 단계에서 `Identifier not found`.
  **`load()` 로 첫 `_process` 이후에 읽으면 정상.**
- **비행 중인 `Projectile` 을 `free()` 로 강제 제거하며 반복 측정하면** 그 뒤 생성한 Area3D 가
  이동 중 판정을 놓친다. **자연 소멸 방식으로 재야 정확하다.**
- 검증 스크립트가 `SkillDB.set_slot` 을 부르면 `user://skills.json` 이 덮어써진다. 끝나면 지울 것:
  `Remove-Item "$env:APPDATA\Godot\app_userdata\SkillCraft Battle\skills.json"`
- **「화면이 거짓말한다」는 이 방법으로만 잡힌다**: `SkillEditor.gd` 를 `load().new()` 로 세워
  `_canvas.set_mask()` → `_refresh()` → `_stat_label.text` 를 읽고 `Balance.analyze_mask()` 기대값과
  **문자 단위 비교**. 헤드리스에서 잘 돈다.
- **회전 스윕은 막대의 「비율」로 표본을 골라야 한다.** QA 4차가 쓴 8종은 전부 비율 4.4 이상이라
  문턱 근처를 한 번도 안 건드렸고, 그 표본만으로 「해결」을 결론낼 뻔했다.
  **다음에 회전을 잴 때는 비율 1.8 ~ 3.2 를 0.1 간격으로 반드시 포함할 것.**
- `derive()` 반환에 `forward_lead` 는 **없다.** `hitbox` 안에 있다.
- `tag_from_metrics(...)` 를 `:=` 로 받으면 파스 에러(오토로드는 타입 추론 불가). `var tg: String =` 로 명시.
