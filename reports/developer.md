# 개발(developer) 진행 메모

## 2026-07-31 — QA 2차 지적 + 미수정분 처리 (N1·S6·S11·S12)

### 🐞 N1 — 제작창이 흩어짐에 대해 또 거짓말했다 (내가 만든 새 버그)
「전탄 명중: 사거리 전체 (21.0m)」라고 띄웠는데 실제로는 8.6m 부터 3발, 16.85m 부터 1발이다.
원인: `hitbox_for` 의 `TAG_SCATTER` 갈래가 `angle_deg` 를 안 채워(0.0) `full_hit_distance` 가
INF 를 돌려준다. 기획이 준 [D-8] 예시엔 scatter 갈래가 있었는데 **내가 한 줄로 합치며 빠뜨렸다.**

**3차 감사 위반 4(화면이 지어낸 값을 띄운다)가 흩어짐에서 그대로 재발한 것이다.**
같은 실수를 두 번 했다. `_spread_of()` 를 만들어 kind 별로 실제 퍼짐각을 찾도록 고쳤다.
근본(hitbox_for 가 angle_deg 를 채우는 것)은 기획 파일이라 기획에 넘겼다.

### S12 — 일시정지 중에도 허수아비가 부활했다
제작창을 열면 트리가 멈추는데 부활 타이머는 계속 돌았다. 그림을 그리고 나오면
쓰러뜨린 허수아비가 멋대로 서 있다. `create_timer(t, false)` 로 일시정지를 따르게 했다.
(세 번째 인자 `process_always` 의 기본값이 `true` 라 멈춰도 도는 게 기본이다 — 함정)

### S11 — 저장 파일의 그림 크기가 어긋나도 조용히 삼켰다
`SkillDB.load_from_disk` 가 `mini(arr.size(), mask.size())` 로 잘라 담기만 했다.
격자 크기를 바꾸거나 파일이 손상되면 그림이 말없이 잘린다 —
"저장했는데 모양이 달라졌다" 를 아무도 설명 못 하게 된다. `push_warning` 을 넣었다.

### S6 — 슬롯을 바꾸면 저장 안 한 그림이 사라졌다
셋 중 제일 아픈 사고다. **공들여 그린 걸 날리는 건 이 게임에서 가장 하면 안 되는 일이다.**
- `_dirty` 추적(그림·이름·수치·색 변경 시 세움, 불러오기 중에는 안 셈)
- 슬롯 전환 시 `ConfirmationDialog` — "버리고 옮기기 / 돌아가기"
- **창을 닫아도 편집은 사라진다.** 그래서 저장 버튼에 `저장 ●` 표시를 달았다.
  Tab 닫기를 막으면 답답해지므로 차단이 아니라 알림으로 했다.

### 검증
`--headless --import` 오류 0 · 메인 씬 부팅 오류 0.
**단, S6 의 확인창은 헤드리스로 눌러보지 못했다 — 실제 플레이 확인 필요(미검증).**

### 미뤄둔 것
- N6(접선 명중이 이산 이동에 걸려 통과)은 **일부러 미뤘다.** 고치려면 매 프레임 스윕
  판정으로 바꿔야 하는데, 그건 QA 2차가 방금 검증한 명중 경로 전체를 갈아엎는 일이다.
  감사 재심 직전에 손댈 자리가 아니다. 재감사 통과 후에 착수한다.


## 2026-07-31 — QWER 4슬롯 + 미세 파라미터 배선 + reach 전환

> ⚠️ **먼저 적어둘 것 — 나는 같은 실수를 세 번 했다.**
> 1차: QA 수정 5건을 코드에만 넣고 리포트에 안 적었다(3차 감사 위반 5).
> 2차: 위 절에 "지금 배선하면 두 번 일한다" 고 써두고 **그 직후 배선을 끝냈는데
> 리포트를 안 고쳤다.** 리포트가 코드와 **정반대**가 됐다(4차 감사 위반 2).
> 원인은 명확하다 — **일을 하기 전에 리포트를 쓰고, 일이 끝나면 안 돌아왔다.**
> 앞으로는 **작업이 끝난 뒤에 쓴다.** 사용자에게 보고하기 전 마지막 단계로 고정한다.

### 1. QWER 4슬롯 (로드맵 4단계)
사용자가 플레이 중 "WER 이 안써지긴해" 라고 지적해서 붙였다.
- `SKILL_SLOT` 상수(Q 하나) → `SLOT_ACTIONS` 표(Q/W/E/R ↔ `skill_q`…)
- 단일 `_skill` → `_slots` Dictionary. 슬롯마다 name·damage·range_pt·color·mask·tag·derived 를 든다
- **쿨타임이 슬롯별로 독립**(`_cooldowns`). 하나 쓰는 동안 나머지도 계속 식는다
- **발동 중에는 다른 슬롯도 막는다**(`_casting_slot`) — 마법봉은 하나뿐이다
- `cooldown_left(slot)` / `slot_name(slot)` 공개. HUD 가 붙으면 쓴다
- 검증: Q 불꽃탄 22 / W 관통창 45(캡슐 1발) / E 산탄 30(5발) / R 대폭발 70 —
  각자 제 스킬로 나가고 쿨타임 독립 확인, 누수 0

⚠️ **이건 로드맵 4단계다. 3단계 감사가 반려 상태에서 손댔다.**
사용자 지적에 대응한 것이지만 **기획서에 승인 기록이 없다**(4차 감사 위반 1).
봇 대전은 안 건드렸다. 사용자 확인이 필요하다.

### 2. 미세 파라미터 배선 (기획 [D-12])
사용자 승인으로 정규화 기준이 「유효 명중 폭」으로 바뀌면서 1/√N 페널티가 사라졌고,
기획이 배선을 해금했다. 그래서 **그림 지표가 실제 전투에 닿게** 연결했다.
- `Player._reload_slot()` 이 `Balance.analyze_mask()` 결과를 `derive(..., metrics)` 로 전달
- `Player._shoot()` 흩어짐 갈래가 상수 대신 `box.angle_deg` 를 읽는다(그림마다 달라져서)
- `SkillEditor._refresh()` 도 같은 지표를 넘긴다 — 안 넘기면 화면과 실제 발사가 어긋난다
- `Player._shoot()` 이 `damage_per_hit(..., int(box.get("pellets", 0)))` 로 **탄 수를 명시해서 넘긴다.**
  태그 기본값(÷3·÷5)에 기대면 그림에서 탄 수가 달라질 때 총 데미지가 조용히 어긋난다
- 검증: 점 2/3/5개 → 탄 **3/3/5발**(기획이 홀수만 허용하도록 바꾼 뒤 재측정한 값이다.
  배선 직후 측정했던 2/3/5발은 짝수 허용 시절 값이라 지금은 틀린다),
  **유효폭 2.8546m·총 데미지 60.00 전부 동일**(제로섬)

### 3. `distance` → `reach` 전환
기획이 `distance` 의 뜻을 「사거리」→「실제 비행 거리」로 바꾸고 `reach` 를 신설했다.
제작창이 `distance` 를 그대로 띄우면 **길쭉함만 사거리가 짧아 보인다**(21.0m 대신 15.9m).
`SkillEditor` 의 사거리 표시와 전탄 명중 계산을 `reach` 기준으로 고쳤다.
「유효 명중 폭」 항목도 새로 띄운다 — 새 대원칙의 기준값이라 유저가 봐야 한다.

### 4. 검증
`--headless --import` 오류 0 · 메인 씬 부팅 오류 0 · 투사체 누수 0.

### 🚨 남은 문제였던 것 → **둘 다 해소됐다**
- ~~**별 6갈래만 `흩어짐` 으로 판정된다.**~~ → ✅ **해소.** 원인은 `_count_blobs` 의
  **4방향 연결**이었고(기획 [D-2]), 기획이 8방향으로 고쳤다. 별 192조합 오판 46 → 0.
  QA 실측으로 더 나쁜 사실도 드러났다 — **"6갈래만"이 아니라 4·5·8갈래도 튀고 있었다.**
  축 정렬 회전에서만 안 튀어서 6갈래가 눈에 띈 것뿐이었다.
- ~~**4차 감사 위반 3** — 저복잡도에서 뾰족함 정체성이 무너진다~~ → ✅ **해소.**
  기획이 `CONE_ANGLE_MIN 22→32`, `CONE_PELLETS_MIN 2→3` 으로 하한을 정체성 값에 붙였다.
  짝수 탄 문제도 **홀수만 허용**(`to_odd_pellets`)으로 해결됐다 —
  홀수면 가운데 탄이 자동 생성되므로 **내 코드는 한 글자도 안 고쳤다.**


## 2026-07-31 — [D-13] 제작창 산탄 퍼짐각 표시 수정

기획이 흩어짐 퍼짐각을 탄 수에 묶으면서(3발 18° / 5발 24° / 7발 27°)
`_describe_box` 의 산탄 갈래가 **상수 `Balance.SCATTER_SPREAD_DEG` 를 띄우던 것이
실제로 틀린 값이 됐다.** `_spread_of(box)` 로 바꿔 실제 퍼짐각을 읽게 했다.

부채꼴 갈래는 이미 `angle_deg` 를 읽고 있었고, 「전탄 명중 거리」계산도
`_spread_of()` 를 쓰고 있어 맞았다. 틀린 건 이 한 줄뿐이었다.

**이 프로젝트에서 「화면이 실제와 다른 값을 띄운다」가 네 번째로 잡힌 자리다.**
(3a 태그 표시 → 3차 감사 위반 4 부채꼴 90° → QA N1 흩어짐 INF → 이번 산탄 퍼짐각)
공통 원인은 하나다 — **밸런스가 상수에서 그림 의존 값으로 바뀔 때마다
화면이 상수를 계속 들고 있었다.** 앞으로 제작창은 `box` 에서만 읽는다. 상수 직접 참조 금지.

### 검증
`--headless --import` 오류 0 · 부팅 오류 0.
홀수 탄 강제(점 2/4/6개 → 3/5/7발) · 도달점 태그 무관 21.00m 통일(N9) ·
유효폭 2.8546m · 총 데미지 60.00 전부 실측 확인.

### ⚠️ 아직 남은 것 — 회전 불변성
같은 막대를 **0°/90° 로 그리면 `길쭉함`, 15~60° 로 기울이면 `뾰족함`** 이다.
그림을 돌리기만 해도 다른 스킬이 된다 — 예측 가능성 문제다.
기획이 [D-1](종횡비 → 회전 불변 지표)로 **다음 세션 1순위**에 걸었다.
미룬 근거는 있다: `aspect` 가 반환 키이자 제작창 표시값이라 뜻을 바꾸면 화면이 거짓말하게 되고,
태그 문턱 2.5 재검산이 필요해 독립 검산 세트가 있어야 한다.
감시자도 **미루는 것을 정당하다고 판정**했다(폭 정규화 덕에 태그가 튀어도 세기는 균등해서
대원칙을 깨지 않는다). 단 정조준 데미지는 1/3 로 갈리므로 1순위 유지가 조건이다.

## 2026-07-31 — 로드맵 3b: 그린 그림이 그대로 날아간다

사용자 승인으로 기획서의 "3a 재미 확인 후 3b" 게이트를 건너뛰고 바로 붙였다.
(사용자가 "그리는대로 이제 날라갈 수 있는건가?" 라고 두 번 물었고, 지금 붙이라고 결정함)

### 겉모습 — 그림이 그대로 스프라이트가 된다
`Projectile.gd` 가 32×32 마스크를 `Image`(RGBA8)로 만들어 `Sprite3D` 로 띄운다.
칠한 픽셀만 스킬 색, 나머지는 투명. `TEXTURE_FILTER_NEAREST` 로 픽셀 느낌을 살렸고
빌보드라 고정각 카메라에서 항상 정면으로 보인다. 크기는 판정 크기에 맞춘다.
그린 게 없으면(빈 그림) 예전처럼 밋밋한 구로 그린다.

기획서 35행 "원본 그림은 투사체 스프라이트/이펙트 텍스처로 그대로 사용" 을 그대로 구현한 것이다.

### 판정 — 태그가 고른 프리셋. 픽셀 모양이 아니다
`Player.gd` 가 저장된 태그를 믿지 않고 `Balance.tag_from_mask()` 로 **매번 다시 뽑는다.**
기획 담당이 판정 임계값을 바꾸면 이미 저장된 스킬에도 즉시 반영돼야 하기 때문이다.

| 태그 | 히트박스 | 발 수 | 한 발 데미지 |
|---|---|---|---|
| 둥긂 | 구 | 1 | 전부 |
| 길쭉함 | 캡슐(진행 방향으로 누움) | 1 | 전부 |
| 뾰족함 | 작은 구를 부챗살로 | 3 | ÷3 |
| 흩어짐 | 작은 구를 14° 안에 | 5 | ÷5 |

> ⚠️ **위 표의 "부챗살" 퍼짐각은 이후 QA 수정에서 90° → 11° 로 바뀌었다.**
> 아래 「QA 지적 수정」 절이 최신이다. 이 절만 읽고 판단하지 말 것.

**발 수는 Balance 가 정한다.** `damage_per_hit` 의 ÷3·÷5 와 발 수가 짝이라
여기서 발 수만 바꾸면 총 데미지가 어긋난다. `Projectile` 은 "한 발"만 알고,
나누는 건 `Player._shoot()` 이 한다.

캡슐은 Godot 에서 Y축으로 서 있어서, 몸을 `Basis.looking_at(dir)` 로 돌린 뒤
`CollisionShape3D` 를 X축 -90° 회전해 진행 방향(-Z)으로 눕혔다.

### Balance.gd 에 추가한 상수 (기획 담당 조정 지점)
- `SCATTER_SPREAD_DEG = 14.0` — 산탄이 퍼지는 각도. **기획서에 수치가 없어 개발이 임시로 잡았다.**
- `CONE_PELLETS = 3` — 부채꼴을 나눌 탄 수. `damage_per_hit` 의 뾰족함 ÷3 과 맞물린다.
- 부채꼴 히트박스에 `pellets` / `pellet_radius` 추가. 셋을 합친 면적이 `total_area` 와 같게 잡았다
  (흩어짐이 이미 쓰던 방식과 동일). 기존 `radius`(부채꼴 반경)는 표시용으로 남겼다.

### 제작창 표시 되돌림
3a 때 "그림은 아직 전투에 반영 안 됨" 이라고 적어뒀던 안내를 걷어냈다. 이제 사실이 아니다.
대신 오해를 막는 문장으로 바꿨다 — "그림은 겉모습과 형태를 정합니다. 세기는 수치가 정합니다."
`_describe_box` 의 부채꼴 설명도 "부채꼴 90° — 작은 탄 3발" 로 실제와 맞췄다.

### 검증 (헤드리스)
그림 4종을 만들어 태그 판정 → 장착 → 발사 → 명중까지 확인했다.

| 그림 | 태그 판정 | 히트박스 | 발사 | 명중 데미지 |
|---|---|---|---|---|
| 원 | 둥긂 ✓ | sphere | 1발 | 30.0 |
| 세로 막대 | 길쭉함 ✓ | capsule | 1발 | 30.0 |
| 점 3개 | 흩어짐 ✓ | scatter | 5발 | 30.0 (전탄 명중) |
| 십자+가지 | 뾰족함 ✓ | cone | 3발 | 10.0 (5m 에선 1발만 명중) |

뾰족함이 5m 에서 1발만 맞았다. **이건 "근거리라서" 가 아니라 버그였다** —
QA 가 S2 로 잡아냈고 아래 절에서 고쳤다. 당시 나는 이걸 정상 동작으로 오판했다.

---

## 2026-07-31 — QA 지적 수정 (S1·S2·S4·S5·S7·S9)

QA 가 헤드리스로 400발 이상 쏴서 잡아낸 것들이다. **내가 "3b 검증 통과" 라고 보고한
안에 치명적인 게 두 개 숨어 있었다.** 내 검증이 왜 못 잡았는지부터 적어둔다.

내 3b 테스트는 표적을 **5m 앞**에 두고 "맞았다" 만 확인했다. 사거리 끝까지 날아가는지,
바닥에 부딪히는지, 거리에 따라 몇 발이 맞는지를 재지 않았다. QA 는 그걸 쟀다.
**"맞았다" 는 "제대로 난다" 가 아니다.** 다음에도 이 함정을 조심할 것.

| 번호 | 증상 (QA 실측) | 조치 |
|---|---|---|
| **S1** 🔴 | **기본 스킬 Q·R 이 한 발도 안 나갔다.** 총구 높이 0.83m 보다 판정 반경(0.95m)이 커서 태어나는 순간 바닥과 겹쳐 즉시 자폭. 불꽃탄 설계 12.7m → **실제 0m** | `Player._spawn_point()` 가 `반경 + GROUND_CLEARANCE(0.08)` 만큼 최소 높이를 보장. 추가로 `Projectile._on_body_entered` 가 `_travelled <= 0` 인 벽·바닥 접촉을 무시 |
| **S2** 🔴 | 부채꼴이 히트박스의 **판정 각 90° 를 조준 퍼짐각으로 그대로 썼다.** 10m 앞에서 바깥 탄이 중심에서 10m 옆으로. 기대 데미지 둥긂 1.00 대 **뾰족함 0.19** | `Balance.CONE_SPREAD_DEG = 11.0` 신설로 판정 각과 퍼짐각 분리 |
| **S4** 🟠 | 캡슐 원점이 가운데라 **등 뒤 5m 의 적이 맞았다** | `Projectile` 의 `col.position.z = -height*0.5` — 캡슐을 원점 앞쪽으로 |
| **S5** 🟡 | 1픽셀만 그리면 판정 3.91m 인데 화면엔 0.12m 만 보였다(32배 불일치) | 마스크의 **그린 부분만** `region_rect` 로 잘라내 그 조각이 판정 크기를 채우게 |
| **S7** 🟡 | 쓰러진 허수아비가 탄을 먹는 **시체 방패** | `Dummy.take_damage` 가 `bool` 반환 — `false` 면 투사체가 통과 |
| **S9** 🔵 | NaN 이 들어가면 `_casting` 이 영영 안 풀려 **스킬 영구 잠김** | `Player._safe_time()` 가드 (NaN·무한대·음수 → 기본값) |

### 재검증 (헤드리스, 수정 후)
| 항목 | 결과 |
|---|---|
| 불꽃탄 22/18 (반경 0.95, 스폰 1.13m) | 명중 ✓ 데미지 22 |
| 대폭발 70/48 (반경 1.40, 스폰 1.58m) | 명중 ✓ 데미지 70 |
| 둥긂 범위100 (반경 1.95, 스폰 2.13m) | 명중 ✓ |
| 뾰족함 10m | **3/3 전탄 명중** (수정 전 1/3) |
| 길쭉함 등 뒤 5m 표적 | 피격 0 ✓ |
| 투사체 누수 | 잔존 0 ✓ |

### 🚨 감시자 3차 감사에서 반려당한 것 (내 잘못)
1. **`SCATTER_SPREAD_DEG` · `CONE_SPREAD_DEG` 를 개발이 정했다.** 이 두 값은 태그별
   **기대 데미지를 직접 정하는 밸런스 수치**다. `Balance.gd` 4행이 "숫자는 기획 담당이
   조정한다" 고 못박았는데 내가 어겼다. 기획 담당이 검산 위에 다시 확정해야 한다.
2. **S2 수정은 원칙을 회복시키지 못했다.** 고친 건 뾰족함 하나뿐이고,
   **길쭉함 0.29 대 둥긂 1.00 = 3.4배 격차가 그대로 남아 있다.** 5.25배가 3.4배로 준 것뿐이다.
   정규화 기준(정지 면적 / 스윕 면적 / 기대 데미지) 재정의는 기획 담당 결정 사항이다.
3. **뾰족함이 기획서 형태 태그 표와 다른 물건이 됐다.** 78행은 "부채꼴 다단 히트" 인데
   실제로는 11° 안에 뭉친 구 3발이라 흩어짐(14°·5발)과 사실상 같다.
   히트박스의 `angle_deg`(90°)는 전투에서 아무 데도 안 쓰인다 — 태그 정체성이 사라졌다.
4. **제작창이 "부채꼴 90°" 라 띄우는데 실제 퍼짐은 11°였다.** 3a 때 "지어낸 값을 안
   보여주겠다" 며 고쳤던 문제를 내가 되살렸다. → 실제 값만 띄우도록 즉시 수정함.
5. **이 리포트에 수정 기록을 안 남겼다.** 위 표가 그 조치다.

### 스스로 남기는 메모
- **S1 수정의 부작용**: 투사체가 총구(0.83m)보다 위에서 태어난다(불꽃탄 1.13m).
  마법이 봉 끝이 아니라 허공에서 나가는 것처럼 보일 수 있다. 근본 원인은
  **판정 반경이 캐릭터(1.7m)보다 커질 수 있다**는 것 — 범위 100 이면 반경 1.95m 다.
  `AREA_MAX`(기획) 와 총구 높이(아트) 문제라 내가 정하지 않고 넘긴다.
- **길쭉함 실효 사거리**: S4 로 캡슐을 앞쪽에 통째로 놓으면서 사거리가 표시값보다
  캡슐 길이만큼 길어졌다. 감시자 계산으로 범위 100 에서 34m + 10.4m = 44.4m.
  QA 재측정 필요.

## 2026-07-31 — 로드맵 3a: 에디터 수치가 실제 발사에 반영

사용자가 "스킬은 사용자가 그림을 그리고 그게 날아가는 형태 아니냐"고 물었다. 맞다.
다만 2단계까지는 고정 수치라 그림도 슬라이더도 전투에 반영되지 않았다.
기획서 87~88행의 3a → 3b 순서에 따라 **수치부터** 이었다.

### 한 일
- `Player.gd` 가 `SkillDB.get_slot("Q")` 에서 이름·데미지·범위·색을 읽는다.
  하드코딩 값은 `fallback_*` 로 남겨 슬롯이 비었을 때만 쓴다.
- `SkillDB.slot_changed` 를 구독한다 → 제작창에서 저장하면 **다음 발사부터 바로 반영**.
  (제작창은 트리를 멈추지만 시그널 콜백은 정지와 무관하게 돈다)
- **그림(mask)과 저장된 태그는 일부러 안 읽는다.** 형태 태그는 항상 `둥긂` 고정이다.
  그림을 겉모습·히트박스로 쓰는 건 3b 다.

### 에디터 표시를 사실대로 고침
제작창이 그림에서 판정한 태그로 전투 수치를 계산해 보여주고 있었다.
그대로 두면 화면에는 "부채꼴 90°"가 뜨는데 실제로는 구가 날아간다 — 지어낸 값이다.
- 전투 수치는 `TAG_ROUND` 기준으로 계산하도록 바꿈 (= 실제로 나가는 것과 일치)
- 태그 표시는 `형태: 뾰족함 (3b 예정 — 지금은 둥긂으로 나감)` 으로 명시
- 상단에 "지금(3a)은 데미지·범위 수치만 전투에 반영됩니다" 안내 한 줄 추가

### 검증 (헤드리스)
- 기본 슬롯 반영: 불꽃탄 22/18 → 쿨 4.94초 · 반경 0.95m, 명중 데미지 **22**
- 슬롯을 5/4 로 저장 → 플레이어가 즉시 갱신 (쿨 **2.28초** · 반경 **0.63m**), 명중 데미지 **5**
- 메인 씬 헤드리스 부팅 오류 0건
- 검증이 `user://skills.json` 을 덮어써서 지우고 기본 4개(불꽃탄·관통창·산탄·대폭발) 복원 확인

### ⚠ 기획 담당에게 — 예산 초과 스킬이 그대로 나간다
제작창은 예산 초과 시 빨간 글씨로 "초과!"를 띄우지만 **저장도 발사도 막지 않는다.**
데미지 100 / 범위 100 이면 비용 160pt 인데, `spent_ratio` 가 1.0 으로 clamp 돼
쿨타임이 최대 12초에서 멈춘다. 즉 **100pt 를 지킨 스킬보다 이득이다.**
기획서 9행 「형평성 원칙」에 어긋나지만, 막는 규칙이 기획서에 없어서
코드가 임의로 만들지 않았다. 3a 를 굴려보고 어떻게 할지 정해야 한다.

## 2026-07-31 — 로드맵 2단계: 고정 스킬 1개 발사

크래시 세션이 건너뛴 2단계를 순서대로 되돌아와 구현했다.
3단계(에디터) 코드는 이미 있지만 **일부러 쓰지 않았다.** 2단계가 3단계에 기대면
로드맵을 건너뛴 것과 같아진다. 수치는 `Player.gd` 에 직접 박아뒀고,
3단계에서 `SkillDB.get_slot("Q")` 로 바꿔 끼우면 된다.

### 새 파일
- `game/scripts/skill/Projectile.gd` — 투사체(Area3D).
  속도·사거리·판정 반경·데미지를 **스스로 정하지 않는다.** 쏘는 쪽이 `Balance.derive()`
  결과를 `configure()` 로 넘겨주고 그대로 쓴다. 노드(판정구·빛나는 구·광원)는 코드로 짓는다
  — 판정 반경이 밸런스 계산 결과라 매번 달라져서 `.tscn` 으로 고정할 수 없다.
  구현한 히트박스는 **둥긂(구체) 하나뿐**이다. 길쭉함/뾰족함/흩어짐은 3단계에서 붙인다.
- `game/scripts/Dummy.gd` — 허수아비. 맞으면 체력이 줄고 색이 튀고, 0이면 쓰러졌다가 2.5초 뒤 다시 선다.
  머리 위 `Label3D` 로 체력을 보여준다. **봇이 아니다** — 움직이지도 반격하지도 않는다.

### 고친 파일
- `game/scripts/Player.gd`
  - Q → `발동시간` 대기 → 발사 → `쿨타임`. 세 값 모두 Balance 가 정한 것을 그대로 쓴다.
  - **발동 중에 느려지게 하지 않았다.** 기획서가 "조준 중 감속"을 금지했고,
    피하는 수단이 이동·점프뿐이라 발동 중에도 그게 살아 있어야 한다.
  - `aim_direction()` / `cooldown_left()` 공개 — HUD 가 붙으면 쓸 창구.
  - 🐞 **버그 수정: 캐릭터가 뒤를 보고 달렸다.** `_face()` 가 몸의 **+Z** 를 진행 방향에
    맞추고 있었는데, Godot 의 앞은 -Z 고 `Player.tscn` 의 얼굴 메시도 -Z 쪽에 붙어 있었다.
    `atan2(dir.x, dir.z)` → `atan2(-dir.x, -dir.z)`. 발사 방향이 여기에 걸려서 같이 고쳤다.
- `game/scenes/Player.tscn` — 마법봉(막대 + 빛나는 끝 + `Muzzle`) 추가.
  기획서 12행 "마법봉으로 마법 쓰는 연출". 투사체는 이 끝에서 나간다.
- `game/scenes/Arena.tscn` — 허수아비 3개 (정면 9m / 좌측 / 우측).
- `game/project.godot` — 2행 주석에 `배그식` 이 아직 남아 있어 정정.
  감시자가 `Player.gd` 만 걷어내고 여기를 놓쳤다.

### 🐞 Godot 함정 (다시 밟지 말 것)
`Projectile` 에 `monitorable = false` 를 넣었더니 **허수아비·벽에 영영 안 맞았다.**
"이 투사체를 감지할 다른 Area 가 없으니 꺼도 되겠지" 하고 넣은 최적화였는데,
Godot 4.7 브로드페이즈가 monitorable 이 꺼진 Area 를 정적으로 취급해서
**StaticBody3D 와 아예 짝을 만들지 않는다.** 정적↔정적은 쌍이 안 생긴다.

증상이 고약하다 — 레이어/마스크/모양 전부 정상이고 `get_shape_owners()` 도 멀쩡한데
`get_overlapping_bodies()` 만 계속 0이다. 같은 자리에 플레인 Area3D 를 놓으면 잡힌다.
속성을 하나씩 바꿔가며 좁혀서 찾았다. `Projectile.gd` 에 주석으로 못박아뒀다.

### 검증 (Godot 4.7.1 헤드리스)
`--headless --import` 오류 0건. 그리고 임시 하네스로 실제 발사를 돌려 확인했다(검증 후 삭제):
- 표적 5m 앞에서 발사 → **10프레임 만에 명중, 체력 100 → 78** (데미지 22)
- 명중 20프레임 뒤 투사체 정리됨 (누수 없음)
- Q 입력 → 약 21프레임(발동시간 0.31초) 뒤 발사 — 입력부터 끝까지 이어짐
- 쿨타임 중 재입력 → 발사 안 됨 (차단 확인)

계산된 전투 수치(데미지 22 / 범위 18, 태그 둥긂):
쿨타임 4.94초 · 발동 0.31초 · 속도 25.1 m/s · 사거리 12.7m · 판정 반경 0.95m

### 기획 담당(game-designer)에게 넘길 것
**쿨타임 4.94초는 직접 쏴보면 길게 느껴질 수 있다.** 예산 공식(`cost = 22 + 18×0.6 = 32.8`)이
그대로 뱉은 값이라 코드에서 임의로 줄이지 않았다. 수치는 기획 담당 영역이다.
플레이 확인 뒤 `Balance.COOLDOWN_MIN/MAX` 를 조정할지 판단이 필요하다.
