# 스킬 크래프트 배틀 (가제) — 게임 기획서

## 한 줄 컨셉
"내가 그린 스킬로 싸운다." 코어 루프 = 그림판에서 스킬 창작 → QWER 4칸에 형평성 있게 배치 → 5분 안에 맞히고 피하는 손맛.

## 코어 설계
- 5v5, 3인칭, PC. 이동 방향키 / 점프 스페이스바 / 스킬 QWER 4슬롯. 5분×3라운드.
- 스킬 제작(심장): 스킬 세팅 창(그림판)에서 모양 그리기 + 발사 형태 + 데미지(1~100) + 범위 설정.
- 형평성 원칙: 데미지 약할수록 쿨 짧게, 셀수록 쿨 길게. 범위 크고 세면 쿨 길고 발동 느림(피하기 쉬움). 범위 작고 세면 발동 빠름(피하기 어려움).
- 맵: 거대한 원룸 한 칸, 소형 인간들이 싸움. 캐릭터/속도는 멧챠 카멜레온풍.
- 자기장: 시간 지나면 배그처럼 안전구역 축소 → 강제 교전.
- 이펙트: 마법봉으로 마법 쓰는 연출. 회피: 이동 + 점프 + **공중 2단 점프(더블 점프)**.
  ↳ 더블 점프는 **2026-08-01 사용자가 직접 플레이한 뒤 요구해 승인된 항목**이다. 상세는 아래 「더블 점프」 절.

## 밸런스 공식
공통 예산 100pt. 비용 = 데미지 + (범위 × 0.6). 남은 예산으로 쿨/발동속도 자동 결정.
실제 수치는 전부 `game/scripts/skill/Balance.gd` 에 있다. 아래는 그 근거만 적는다.

### 쿨타임은 예산에 **곡선**으로 걸린다 (2026-07-31, 밸런스 담당 확정)
`쿨타임 = 0.6초 + 8.4초 × (예산소모율 ^ 1.6)`

지수 1.6 을 쓰는 이유: 선형(지수 1.0)이면 데미지가 클수록 지속 딜(데미지÷쿨)이
계속 좋아져서 **약한 스킬이 일방적으로 손해**였다. 9행 형평성 원칙이 무너진다.
범위 20 고정, 데미지 10~88 의 지속 딜 최대/최소 비 — 지수 1.0 은 2.39배, 1.6 은 1.52배.
(2.0 이면 1.43배로 더 평평하지만 중간 데미지가 최강이 되는 역전이 생겨 1.6 을 골랐다)

### ✅ 결정 — 예산을 넘겨도 만들 수 있다. 대신 대가가 붙는다 (2026-07-31, 밸런스 담당)
예산 초과 시 **저장·발사를 막지 않는다.** 초과분에 비례해 쿨타임과 발동시간이 늘어난다.
- 쿨타임 × (1 + 초과율 × 3.0) · 발동시간 × (1 + 초과율 × 1.5)
- 예: 데미지 100 / 범위 100 = 160pt → 60% 초과 → 쿨 9.0초 × 2.8 = **25.2초**

막지 않는 이유:
1. 8행이 데미지 1~100 을 명시했는데, 초과를 막으면 데미지 100 이 사실상 존재할 수 없다.
   데미지 100 + 최소 범위 1 = 100.6pt 라 무조건 초과이기 때문이다.
2. 9행 형평성 원칙이 "셀수록 쿨 길게"다. 초과분에 쿨을 더 붙이는 건 새 규칙을 만드는 게
   아니라 **기존 원칙을 끝까지 적용하는 것**이다.
3. 거부당하는 것보다 대가를 치르는 쪽이 만드는 재미가 산다.

형평성 검산: 데미지·범위 1~100 전 조합 10,000개를 훑어 `(데미지÷쿨) × 판정반경` 을 비교했다.
| 초과 페널티 | 100pt 이내 최강 | 초과 조합 최강 | 판정 |
|---|---|---|---|
| 없음(종전) | 11.24 | **21.72** | 초과가 2배 이득 — 붕괴 |
| ×1.0 | 11.24 | 13.57 | 아직 이득 |
| ×2.0 | 11.24 | 11.09 | 여기서 뒤집힘 |
| **×3.0 (채택)** | **11.24** | 11.07 | 초과가 이득인 조합 **0개** |

### ✅ 원샷킬 제거 — 데미지 슬라이더에 **환율**을 넣었다 (2026-08-01, 사용자 플레이 피드백)

**세션 내내 미뤄졌던 「직접 해 보고 재미있나」 확인이 이날 이뤄졌다.** 사용자 원문:
> "지금 100 hp 가 문제가 아니라 **데미지가 너무 쎄서 한방에 죽는건 말이 안됨.
> 그니까 hp가 아닌 스킬을 패치 해야할듯**"

#### ❌ 기각 — 종전의 「캐릭터 HP 150」 제안
이 자리에는 「HP 를 100 → 150 으로 올리자」는 밸런스 담당 제안이 있었다.
**사용자가 「hp 가 문제가 아니다」로 명시 기각했다.** 고칠 곳은 스킬 쪽이다.
사용자 판단이 옳다 — HP 100 은 **곧 백분율**이라 「한 방이 HP 의 몇 %냐」가 바로 읽힌다.
그 가독성을 버리고 HP 를 부풀리는 것은 같은 문제를 덜 읽히게 푸는 것이다.

#### 조치 — `DAMAGE_SCALE = 0.40`
슬라이더 값(1~100)은 그대로 두고, **실제로 들어가는 데미지 = 슬라이더 × 0.40** 으로 바꿨다.
슬라이더는 이제 「이 스킬에 예산을 얼마나 넣었나」이고, 실데미지는 그 예산의 **환율**이다.
(8행의 「데미지 1~100」 표기는 안 바뀐다 — 그건 슬라이더의 눈금이다)

> **보장: 실데미지 상한이 40 이므로 HP 100 에서 어떤 스킬도 최소 3대다.**
> 다탄 태그도 같다 — 탄 N발의 합이 40 이라 전탄 명중해도 40 을 못 넘는다.
> 2대 = 80(HP 20 남음) · 3대 = 120.

**왜 3대인가.** 2대면 첫 방을 맞은 뒤 회피 기회가 한 번뿐이라 「맞았다 → 끝」에 가깝다.
3대면 기회가 두 번이고, HP 바가 100 → 60 → 20 으로 꺾여 **「위험하다」는 상태가 화면에 생긴다.**
사용자가 더블 점프를 요구한 이유가 「스킬을 피하면서 해야 하기 때문」이므로,
**피할 기회를 몇 번 주느냐**가 이 값의 기준이다.

**왜 하필 0.40 인가.** HP 100 에서 3대가 되는 캡 구간은 **(33.34, 50.0)** 이다
(2대 합 < 100 이려면 캡 < 50, 3대 합 ≥ 100 이려면 캡 > 33.34).
그 구간의 **위쪽**을 쓰되 2대 경계에서 **20% 여유**를 남긴 값이 40 이다.
위쪽을 쓰는 것은 「센 스킬은 세다」는 체감을 최대한 남기려는 것이고,
여유를 두는 것은 나중에 HP 나 다단히트가 조금 움직여도 **2대 즉사로 조용히 넘어가지 않게** 하려는 것이다.

#### 왜 「수확체감 곡선」이 아니라 비례 상수인가
위쪽만 눌러 깎는 오목 곡선(예: `실데미지 = 50s/(s+50)`)도 계산해 보고 **버렸다.**
1. **예산 공식이 데미지를 1차로 값매김한다** — `비용 = 데미지 + 범위×0.6`.
   출력만 오목하게 만들면 슬라이더 위쪽 절반이 **「예산은 정가로 내고 데미지는 덜 받는 함정」**이 된다.
   값매김과 출력은 같은 차수여야 한다.
2. **곡선을 넣으면 지금까지의 검산이 통째로 무효가 된다.** 실제로 재보니
   `COOLDOWN_CURVE 1.6` 이 안 버텼다 — 지속딜 비가 **1.52 → 2.01** 로 벌어지고
   **약한 스킬이 이득**인 쪽으로 순위가 뒤집힌다(9행 형평성 원칙 위반).
   쿨타임 곡선까지 같이 다시 잡아야 하는데, **사용자가 확인해 준 결함은 「한 방에 죽는다」 하나뿐**이다.
   보고된 것보다 넓게 손대지 않는다.

#### 연쇄 영향 — **기존 검산은 전부 그대로 유효하다** [실행 확인 2026-08-01]
`DAMAGE_SCALE` 은 `cost_of` 에도, 쿨타임·발동·속도·사거리·면적·히트박스 계산에도 **안 들어간다.**
그 계산들은 종전과 똑같이 슬라이더 값만 본다. 데미지가 들어가는 형평성 지표는 전부
데미지에 **1차 비례**하므로 0.40배로 같이 줄고 **비율과 순위가 안 바뀐다.**

| 검산 | 종전 | 개정 후 | 판정 |
|---|---|---|---|
| 지속딜 비(범위 20 · 데미지 10~88) | 1.5162 (최강 D38 · 최약 D10) | **1.5162 (D38 · D10)** | 완전 동일 → `COOLDOWN_CURVE 1.6` 유지 |
| 10,000 조합 합법 최강 | 11.2356 @D48/R33 | **4.4942 @D48/R33** | 위치 동일 |
| 10,000 조합 초과 최강 | 11.0661 @D69/R52 | **4.4264 @D69/R52** | 위치 동일 |
| **초과가 이득인 조합 수** | 0개 | **0개** | `OVER_COOLDOWN_PENALTY 3.0` 유지 |
| 태그별 총 데미지(슬라이더 60) | 60.0000 | **24.0000** (탄 1·3·5·7발 전부) | 합 = `damage_out` 정확히 유지 |
| 유효 명중 폭(범위 50) | 2.8546m | **2.8546m** | 데미지와 무관 |
| D100/R100 쿨타임 | 25.2초 | **25.2초** | 초과 페널티 예시 그대로 |

**체감 확인** — 기본 스킬 4개(Q22·W45·E30·R70)의 한 방이 **8.8 / 18.0 / 12.0 / 28.0**.
QWER 를 한 바퀴 다 맞혀도 **66.8** 이라 안 죽는다. 전탄 명중 기준 TTK 는 **2.5초 → 6.4초**.

⚠ **개발 주의**: 변환은 `Balance.damage_per_hit()` 안에서 **한 번만** 일어난다.
`Player._shoot()` 과 제작창은 원래부터 그 함수를 거치므로 **배선을 고칠 것이 없다.**
어디서도 다시 곱하지 마라. 표시용 명목 데미지는 `derive()` 의 새 키 `damage_out` 을 쓴다.

📌 **이 문서의 기존 데미지 표를 읽는 법.** 아래쪽 검산표들이 「데미지 60」 「정조준 60.000」
「41.235」 처럼 적은 숫자는 **전부 슬라이더 단위**다(그때는 1:1 이었다).
실데미지로 읽으려면 **0.40 을 곱하면 된다** — 슬라이더 60 → 24.0, 정조준 60.000 → 24.000.
**배수로 적은 결론(1.57배 · 1.94배 · 2.21배 등)은 손댈 필요가 없다.** 비율이라 환율에 안 흔들린다.
표를 일괄 재작성하지 않은 이유가 이것이다 — 숫자를 옮겨 적다 틀리는 위험이
「0.40 을 곱해라」 한 줄보다 크다.

### ✅ 더블 점프 — 공중에서 한 번 더 뛴다 (2026-08-01, 사용자 승인)

**기획서에 없던 기능인데 사용자가 직접 플레이한 뒤 요구했다.** 원문:
> "스페이스바 두번 누를시에 캐릭터가 공중에서 덤블링하면서 두번 점프하는 게 필요할듯
> **스킬을 피하면서 해야하기 때문에**"

- **승인 경로**: 사용자 직접 요구 → 즉시 승인. 12행 「회피: 이동 + 점프」를
  「이동 + 점프 + 공중 2단 점프」로 정정했다.
- **이유가 중요하다** — 「스킬을 피하면서 해야 하기 때문」은 **회피 수단이 부족하다는 체감 보고**다.
  기능 요청이 아니라 밸런스 신호로 읽어야 한다.
- 연출은 「공중에서 덤블링」. 시각 처리는 디자인 담당 항목이다.

#### 밸런스 영향 판단 — **지금은 스킬을 안 건드린다**
회피 수단이 늘면 스킬은 상대적으로 약해진다. 그러나 이번에는 **조정하지 않는다.**
1. **같은 세션에 완화를 두 개 넣었다.** 원샷킬 제거(한 방 0.40배)가 이미
   「맞아도 안 죽는다」로 회피 압박을 크게 낮췄다. 여기에 발동 시간까지 줄이면
   다음 플레이에서 **어느 쪽이 효과였는지 가를 수 없다.**
2. **더블 점프는 순수한 회피 강화가 아니다.** 공중에서는 이동 제어가 약하고 궤적이
   포물선이라 예측하기 쉽다 — 「피하기 쉬워지는 만큼 읽히기도 쉬워진다」.
   방향과 크기를 실측 없이 단정할 수 없다.
3. 투사체 속도(`SPEED_MIN/MAX`)는 **더 나중이다.** 속도는 사거리·전탄 유지 거리와
   얽혀 있어 건드리면 태그 정체성 검산이 통째로 흔들린다.

**다음 플레이에서 「스킬이 안 맞는다」가 나오면 손댈 순서**:
`CAST_MAX 1.10 → 0.90` (큰 스킬의 발동만 빠르게) → 그래도 안 되면 `CAST_MIN` →
투사체 속도는 마지막. **한 번에 하나씩** 바꾼다.

## ✅ 결정됨 — 그림의 역할: 하이브리드 (2026-07-31)
그림은 **겉모습 + 히트박스 "형태" 태그**까지 담당한다. 세기는 절대 담당하지 않는다.

### 대원칙 (밸런스 안전장치) — **2026-07-31 사용자 승인으로 개정됨**
> **그림은 모양을 바꾸고, 수치는 세기를 바꾼다.**
> 스킬의 **유효 명중 폭**(진행 방향에 수직인 단면의 기대 폭)은 에디터의 `범위` 슬라이더
> 값으로 **정규화**된다.
> 어떤 그림을 그리든 같은 범위 값이면 **한 명을 상대로 한 기대 데미지가 같다.**
> 그림은 그 폭을 몇 조각으로 나눌지, 어느 각도로 벌릴지, 앞뒤로 얼마나 길게 늘일지만 정한다.
> → 「얇은 선 하나로 맵 관통」은 **길이 상한 3중 장치**로 막는다(바로 아래).

**개정 이력 — 2026-07-31, 사용자 승인.**
종전 문장은 「스킬의 **총 유효 판정 면적**을 범위 값으로 정규화한다」였다.
`✅ 결정됨` 항목이라 사용자만 바꿀 수 있고, 밸런스 담당이 (다)안으로 상정한 것을
**사용자가 "바꾸는 쪽으로 가자"로 명시 승인**했다(2026-07-31). 그 승인에 따라 고친다.

**왜 바꿨나 — 면적 정규화로는 대원칙이 수학적으로 성립하지 못한다.**
1. 밸런스 담당이 유도한 닫힌 식:
   `기대 데미지 = damage × (유효 명중 폭 + 표적 지름)`
   투사체는 스스로 진행 방향을 쓸고 지나가므로 **진행축에 배분한 면적은 명중 확률을
   1도 올리지 못한다.** 명중을 정하는 것은 진행축에 수직인 폭 하나뿐이다.
2. 그래서 면적 A 를 고정하면 N 발로 쪼갤 때 탄 반경 √(A/Nπ) · 한 발 D/N →
   기대 데미지 ∝ **1/√N**. 캡슐도 ∝ 1/날씬함. **어떤 상수를 넣어도 단일 원(둥긂)을
   못 따라잡는다 — 둥긂이 영구 1위**라는 것이 증명됐다.
3. **QA 2차 실측이 이 식을 검증했다**(2,200발 이상 발사, 오차 최대 3.4%·대부분 1% 이내).
   N 상쇄와 퍼짐각 무관도 실측으로 확인됐다. 그리고 면적 정규화 상태의 실제 격차는
   **2.21배**(점 표적) / 1.78배(0.4m 표적)로 남아 있었다. 「같다」가 아니었다.
→ 정규화 대상을 **면적 → 유효 명중 폭**으로 옮기면 격차가 정의상 **1.00배**가 된다.
   (개정 후 검산: 범위 1~100 × 태그 4종 전 조합에서 `effective_width` 오차 **0.0000000000 m**)

**어떻게 구현되나** — 유효 명중 폭 = 그 범위의 기준 면적을 가진 원의 지름
`W = 2√(면적/π)`. 범위 1 에서 **1.0776m**, 범위 50 에서 2.855m, 범위 100 에서 3.909m.
(🐞 **정정 2026-08-01** — 종전 판본은 「1.009m」였다. 그것은 `WIDTH_MIN`(= 범위 **0** 의 값)이고,
슬라이더의 최솟값인 **범위 1** 은 `range_ratio = 1/100` 이라 면적 0.912㎡ → 폭 **1.0776m** 이다.
4차·5차·6차 감사가 연속으로 지적한 미결 항목을 여기서 닫는다. **상수·코드는 안 바뀌었다** —
`WIDTH_MIN` 은 하한 표시용으로 맞는 값이고, 계산의 권위는 `effective_width_for()` 에 있다.)
- 둥긂: 지름 W 인 구 하나 **(개정 전과 한 자리도 안 바뀐다)**
- 흩어짐·뾰족함: **탄 하나하나가 둥긂과 같은 크기**(지름 W). 한 발 데미지는 D/N
- 길쭉함: 폭 W 인 캡슐. 길이만 모양 값이고 상한이 걸린다

기준을 「둥긂 그대로」로 잡은 이유: 둥긂은 종전 기준의 **최강 태그**였으므로 나머지
셋을 거기까지 끌어올리는 개정은 **세기의 상한을 움직이지 않는다.** 덕분에 예산 초과
페널티 3.0 검산(10,000 조합)과 쿨타임 곡선 1.6 검산이 재계산 없이 유효하다
(재실행 확인: 합법 최강 11.24 D48/R33 · 초과 최강 11.07 D69/R52 — 종전과 동일).

### 「얇은 선 하나로 맵 관통」을 이제 무엇이 막는가 — 길이 상한 3중 장치
종전에는 **면적 상한**이 길이를 묶었다(길이 = 상수 × √면적). 폭을 정규화하면 그 족쇄가
풀리므로, 차단 근거를 **길이에 직접 거는 상한**으로 옮긴다. 세 겹이고 셋 다 코드에 있다.

| # | 장치 | 값 | 무엇을 막나 |
|---|---|---|---|
| 1 | **폭을 그림이 못 정한다** | 폭 = `범위` 가 정한 W (고정) | 얇게 그려도 폭이 안 줄어든다. 종전에는 얇게 그리면 면적 보존 때문에 **길이가 대신 늘어났다** — 관통 버그의 원동력이 이것이었다 |
| 2 | **길이 절대·비례 상한** | `LONG_LENGTH_MAX_ABS 8.0m` · `LONG_LENGTH_MAX_FRAC 0.30`(사거리의 30%) | 어떤 범위·어떤 그림에서도 캡슐이 8m 와 사거리의 30% 를 동시에 못 넘는다. 실제 길이는 범위 100 에서 7.04m(20.7%) |
| 3 | **전방 도달점을 사거리에 고정** | 비행 거리 = 사거리 − 캡슐 길이 | **길게 그려도 더 멀리 못 간다.** 「맵 관통」의 본질은 사거리 연장인데 그것을 직접 차단한다. 네 태그의 도달점이 전 범위에서 동일함을 검산으로 확인했다 |

3번은 QA N2(길쭉함 표시 34.0m ↔ 실측 41.1m)의 해결책이기도 하다. 상세는 아래 「N2」 절.

### 개정의 대가 — 명시하고 4단계에서 재검증한다
1. **총 판정 면적이 태그마다 달라진다.** 탄 하나가 둥긂과 같은 크기가 되므로 다탄 태그의
   면적 합이 N 배다(범위 50 기준 둥긂 6.40㎡ · 길쭉함 12.92㎡ · 뾰족함 19.20㎡ · 흩어짐 32.00㎡).
   **한 명 상대 기대 데미지는 같지만 여러 명이 겹친 난전에서는 갈린다** —
   다탄 태그는 여러 명에게 D/N 씩 흩뿌리고, 단발 태그는 한 명에게 D 를 몰아준다.
   → **4단계(QWER + 봇 대전)에서 다중 표적 위력을 반드시 재검증한다.**
2. **캡슐 길이의 2차 효과.** 길이는 투사체가 한 지점에 머무는 시간(길이 ÷ 속도)을 늘려
   **움직이는 표적**에 대한 관용을 키운다(범위 50 에서 0.26초 체류 → 실효 폭 약 +1.5m).
   닫힌 식이 못 잡는 효과라 길이 상한으로 묶어 두고, 신장도 → 길이 배선은 4단계 실측
   전까지 보류한다. → **4단계 재검증 항목.**
3. **큰 투사체가 늘어난다.** 다탄 태그의 탄 반경이 커져(범위 50 흩어짐 0.638m → 1.427m)
   스폰 높이도 같이 올라간다(QA N5). 디자인 담당 확인 항목이 넓어졌다.
4. **형태 태그 표의 「체감」 칸이 일부 낡았다.** 길쭉함의 「조준 어려움」은 이제 사실이
   아니다 — 네 태그의 좌우 명중 폭이 같아졌기 때문이다. 판정 조건·히트박스 형태 칸은
   그대로 맞다. **표 문구 갱신은 사용자 승인 사항**이라 이 문단으로 사실만 기록해 둔다.

### 파이프라인
1. 유저가 그림판(32×32 그리드)에 그림 → 픽셀 마스크
2. **잡픽셀을 먼저 턴다** — 4픽셀 미만짜리 덩어리는 그림이 아니라 노이즈로 보고 **지표 계산에서
   통째로 뺀다**(2026-08-01 개정, QA S3). 종전에는 덩어리 수만 노이즈를 버리고 나머지 지표는
   버린 픽셀까지 세어서, **구석에 1픽셀만 찍으면 태그를 마음대로 고를 수 있었다.**
   투사체 그림 자체는 원본 마스크를 그대로 쓰므로 잡픽셀도 화면에는 보인다 — 판정에만 안 쓴다.
3. 마스크에서 **지표 4개** 계산
   - 채움률 = 칠한 픽셀 / 바운딩박스 면적
   - **신장도** = 칠한 픽셀 분포의 2차 모멘트 고유값 비 `√(l1/l2)`. 항상 ≥ 1.0.
     1.0 = 완전 등방(원·정사각형·십자·별), 클수록 한 방향으로 길다.
     **종횡비보다 회전에 훨씬 덜 흔들린다** — 이것이 종전 「종횡비 = 바운딩박스 가로/세로」와의
     차이다 (2026-08-01 개정, [D-1]. 종횡비는 45° 막대에서 1.00 이 되어 길쭉함을 놓쳤다).
     축에 나란한 꽉 찬 직사각형에서는 **신장도 = 가로/세로 픽셀 수**와 정확히 같다.
     ⚠ **「회전 불변」이 아니다.** 32×32 이진 격자에 다시 그리면 각도마다 픽셀이 재배치돼
     값이 흔들리고(문턱 근처에서 **8~26%**, 전체로는 3.6~95.7% — QA 5차 실측),
     **비율 대략 1.9~2.8 인 그림은 지금도 회전으로 태그가 갈린다.**
     반드시 아래 「⚠ 알려진 판정 구멍」의 회전 항목 두 개를 같이 읽을 것.
   - 복잡도 = 둘레² / 면적 (뾰족할수록 큼)
   - 덩어리 수 = 연결 요소(connected components) 개수 — 8방향 연결
   - (종횡비도 계속 계산해서 제작창에 띄운다. **판정에는 안 쓴다.**)
4. 지표 → **형태 태그 1개** 판정 (아래 표)
5. 태그 → 히트박스 프리셋 선택, 크기는 `범위` 값으로 정규화
6. 원본 그림은 투사체 스프라이트/이펙트 텍스처로 그대로 사용 (겉모습)

### 형태 태그 표 (판정 조건 수치 확정 — 2026-07-31 밸런스 담당)
판정은 위에서부터 순서대로 본다. 먼저 걸리는 태그가 이긴다.

| 순위 | 태그 | 판정 조건 | 히트박스 형태 | 체감 |
|---|---|---|---|---|
| 1 | 흩어짐 | 덩어리 ≥ 2 | 산탄 다발(작은 원 N개) | 견제형 |
| 2 | 길쭉함 | **신장도 ≥ 2.5** ⚠사용자 확인 필요 | 직선 관통형(캡슐) | 조준 어려움, 맞히면 시원함 |
| 3 | 뾰족함 | 복잡도 ≥ 2.5 **그리고** 채움률 < 0.50 | 부채꼴 다단 히트 | 근거리 난사형 |
| 4 | 둥긂 | 나머지 전부(폴백) | 원형 폭발 | 무난, 기본값 |

**길쭉함 조건이 「종횡비 ≥ 2.5 또는 ≤ 0.4」에서 「신장도 ≥ 2.5」로 바뀐 이유**
(2026-08-01 [D-1], 상세 근거는 아래 「[D-1] 길쭉함 지표 — 종횡비 → 신장도」 절)
- 종횡비는 **축에 나란한 바운딩박스**로 재는 값이라 그림을 기울이면 값이 무너진다.
  같은 막대가 0°/90° 에서는 길쭉함인데 **19~70° 구간은 통째로 뾰족함**이었다(QA 4차 실측).
- **문턱 숫자 2.5 는 안 바꿨다.** 꽉 찬 직사각형에서는 신장도 = 가로/세로 픽셀 수와 정확히
  같아서(증명은 `Balance.gd` 주석), 「2.5:1 보다 길면 길쭉함」이라는 **뜻이 그대로다.**
- **역수 조건(≤ 0.4)이 사라진 이유**: 신장도는 정의상 항상 ≥ 1 이라 세로로 긴 그림도
  똑같이 큰 값이 나온다. 두 갈래로 나눠 볼 필요가 없어졌다(가로 막대 6×32 = 5.333,
  같은 막대를 눕혀도 5.333).
- **기본 스킬 4개 태그는 안 바뀐다** — Q 둥긂 · W 길쭉함 · E 흩어짐 · R 둥긂 (검산 완료).

> **⚠ 사용자 확인 필요 — 이 표의 「판정 조건」 칸을 기획 담당이 고쳤다 (2026-08-01).**
> 고친 것은 길쭉함 한 칸(「종횡비 ≥ 2.5 또는 ≤ 0.4」 → 「신장도 ≥ 2.5」)이고, **뜻은 안 바뀌었다**
> (축에 나란한 직사각형에서 두 지표가 같은 수라는 항등식이 근거다). **「체감」 칸 · 태그 이름 ·
> 순서 · 히트박스 형태 칸은 한 글자도 안 건드렸다.**
> 6차 감사는 이것을 「기획 담당 권한 안」으로 판정했다(그 숫자를 정한 것이 사용자가 아니라
> 밸런스 담당이고, 뜻이 안 바뀌었다는 근거 넷). **그러나 위 「개정의 대가」 4번이 「표 문구 갱신은 사용자
> 승인 사항」이라고 적어 두었다.** 문서 안에 서로 부딪치는 두 문장이 남으므로,
> 감사 권고대로 **사용자 추인 한 줄을 받는다.** 받으면 이 상자를 지우고 아래 한 줄로 바꿀 것:
> 「2026-08-01 판정 조건 칸은 지표 개정에 따른 기록 정정으로 기획 담당이 수정(사용자 추인 완료)」.
> **추인 전까지**: 코드는 이미 신장도로 판정하고 있고(6차 감사 실행 확인), 이 칸은 그 사실의
> **기록**이다. 추인이 안 나면 바꿔야 하는 것은 표가 아니라 **「개정의 대가」 4번 쪽 규칙의 범위**다.

**뾰족함에 채움률 조건을 더한 이유** — 복잡도(둘레²/면적)는 둘레를 제곱하므로 손떨림에
2차로 반응한다. 검산 결과 손으로 그린 원은 테두리가 한 칸만 울퉁불퉁해도 복잡도가
1.90 → 5.22 로 뛰어 **"원을 그렸는데 부채꼴이 나가는"** 일이 생겼다. 같은 손떨림에서
채움률은 0.67 → 0.61 밖에 안 움직인다. 별·번개·십자는 채움률 0.24~0.48 이라
0.50 문턱으로 원(0.60~0.73)과 깨끗하게 갈린다. 예측 가능해야 그리는 재미가 산다.

애매하면 **둥긂(기본값)** 으로 폴백. 에디터에서 판정된 태그를 실시간으로 보여줘 유저가 예측 가능하게 한다.

### 퍼짐각 2개 확정 (2026-07-31, 밸런스 담당 — 3차 감사 위반 1 해소)
개발이 근거 없이 잡아 둔 `SCATTER_SPREAD_DEG` · `CONE_SPREAD_DEG` 를 검산 위에 다시 확정한다.

**먼저 — 퍼짐각은 형평성 값이 아니다. [증명]**
표적이 좌우 어디에 있을지 모를 때의 기대 데미지는
`Σ(탄 i 의 명중 폭) × (데미지 ÷ N) = N × 2(탄반경 + 표적반경) × D/N = 2D(탄반경 + 표적반경)`
— **탄을 어느 각도로 벌리든 합이 같다.** 퍼짐각은 기대 데미지를 1도 못 바꾼다.
퍼짐각이 정하는 유일한 것은 **전탄이 다 맞는 거리**다.
> 전탄 유지 거리 = (탄 반경 + 표적 반경) ÷ tan(퍼짐각 ÷ 2)

그래서 퍼짐각은 **태그 체감(76·78행)** 으로 정한다. 기준: 표적 반경 0.4m(QA 실측에 쓴
사람 크기 표적), 범위 50(기준 면적 6.40㎡ · 사거리 21.0m).

**정하는 기준은 「사거리 대비 전탄 유지 %」다** — 각도 자체가 아니다. 각도는 탄 반경에
따라 달라지는 종속 변수다. QA 2차가 이 기준을 실측으로 검증했다
(뾰족함 계산 6.30m ↔ 실측 6.35m · 흩어짐 8.46m ↔ 8.35m · 16.98m ↔ 16.60m — 전부 일치).

| 상수 | 1차 확정 | **폭 정규화 후 재계산** | 전탄 유지 거리 | 사거리 대비 | 근거 |
|---|---|---|---|---|---|
| `SCATTER_SPREAD_DEG` | 14.0 | **24.0** | 5발 8.60m / 3발 17.39m | 40.9% / 82.8% | 76행 「견제형」 = 완만한 2단 낙폭. 절벽이 없다 |
| `CONE_SPREAD_DEG` | 22.0 | **32.0** | 3발 6.37m | 30.3% | 78행 「근거리 난사형」 = 흩어짐보다 짧은 유지 거리 |

⚠ **2026-07-31 (QA 3차 N7) — 흩어짐 퍼짐각은 이제 탄 수에 묶여 있다.**
위 24.0° 는 **탄 5발(기본)** 값이고, 탄이 적으면 좁게 · 많으면 넓게 나간다
(3발 18° · 5발 24° · 7발 27°). 이유와 검산은 아래 「흩어짐 퍼짐각을 탄 수에 묶는다」 절.
`CONE_SPREAD_DEG` 32.0 은 뾰족함 퍼짐각의 **하한이자 기본값**이다(32°~46°).

**왜 다시 넓혔나 — 기준(%)은 안 바뀌었고 탄 반경이 바뀌었다.**
폭 정규화로 범위 50 의 탄 반경이 뾰족함 0.818m · 흩어짐 0.638m 에서 **둘 다 1.427m**
로 커졌다. 같은 각도를 두면 유지 거리가 뾰족함 6.30 → 9.40m(44.8%), 흩어짐
8.46 → 14.88m(70.9%) 로 늘어 **「근거리 난사형」과 「견제형」의 구분이 흐려진다.**
목표 %(30% / 40%·80%)를 그대로 맞추는 각도가 32.0° 와 24.0° 다. 1%p 안에서 일치한다.
- 32° > 24° → **부채꼴이 산탄보다 넓다.** 「부채꼴」이 이름값을 한다(1차 확정과 같은 판정).
- 30.3% < 40.9% → **뾰족함이 더 붙어야 한다.** 「근거리」가 성립한다.
- 기대 데미지는 위 증명대로 **각도와 무관**하다. 형평성은 여전히 안 건드린다.

범위별 드리프트(전탄 유지 ÷ 사거리):

| 범위 | 1 | 18 | 50 | 100 |
|---|---|---|---|---|
| 뾰족함 3발 | 39.6% | 37.0% | 30.3% | 24.1% |
| 흩어짐 5발 | 53.5% | 50.0% | 40.9% | 32.6% |
| 흩어짐 3발 | (사거리 밖) | (사거리 밖) | 82.8% | 65.9% |

큰 스킬일수록 더 붙어야 한다 = 9행 「범위 크고 세면 피하기 쉽다」와 같은 방향이라 그대로 둔다.
저범위에서 흩어짐 3발 유지가 사거리를 넘는 것은, 작은 스킬은 애초에 근접용이라 문제없다.

### ✅ 해소 — 「형평성」의 정의 (3차 감사 위반 2) · **2026-07-31 사용자 승인으로 (다) 채택**

> **결론: (다) 단일 표적 기대 데미지 정규화로 간다.** 사용자가 2026-07-31 에
> "바꾸는 쪽으로 가자"로 명시 승인했다. 개정된 대원칙 문장은 이 문서 위쪽
> 「대원칙 (밸런스 안전장치)」 절에 들어가 있고, 코드(`Balance.gd`)도 그 기준으로 다시 짰다.
> 아래는 **어떤 판단으로 그 결론에 이르렀는지의 기록**이다. 되돌아볼 때 읽을 것.

**사실**: 총 판정 면적은 네 태그 모두 정확히 같았다(QA 실측 오차 0). **그런데 실전 기대 데미지는 안 같았다.**

**원인 [계산]**: 투사체는 **스스로 진행 방향을 쓸고 지나간다.** 그래서 진행축(깊이)에
배분된 면적은 명중 확률을 **1도 올리지 못한다.** 명중을 정하는 것은 진행축에 **수직인 폭**뿐이다.
총 면적을 정규화하는 것은 **쓸모없는 축까지 예산으로 쳐주는 제도**여서, 깊이에 면적을 많이
배분한 그림(=길쭉함, 다탄 태그)이 구조적으로 손해를 본다.

**[증명] 면적을 고정하는 한 형평성은 수학적으로 회복 불가능하다.**
- 면적 A 를 N 발로 쪼개면 탄 반경 √(A/Nπ), 한 발 데미지 D/N → 기대 데미지 ∝ **1/√N**
- 캡슐은 폭 = √A ÷ `LONG_LEN_RATIO` → 기대 데미지 ∝ **1/`LONG_LEN_RATIO`**
- 어떤 상수를 골라도 **단일 원(둥긂)을 못 따라잡는다.** 둥긂이 영구 1위다.

**[계산] 실전 지표는 「완벽 조준」이 아니라 「빗맞힘 포함 기대값」이다.**
범위 50 기준 투사체 속도 20m/s, 10m 비행에 0.50초. 표적 이동속도 6.0m/s → 그 사이 최대 **3.0m** 이동한다.
어떤 히트박스 폭(0.84~2.85m)보다 크다. 그래서 QA 의 「좌우 스캔 기대값」이 실전 지표로 옳다.

#### 선택지와 대가
| 안 | 내용 | 대가 |
|---|---|---|
| **(가) 정지 면적 유지** | 지금대로. 태그별 위력 차를 「개성」으로 받아들인다 | 둥긂이 영구 1위(위 증명). 「어떤 그림을 그리든 같은 범위면 같다」가 **실전에서 거짓말**로 남는다. 그림 잘 그릴 이유가 계속 없다(QA 재미 평가 참조) |
| (나) 스윕 면적 | 날아가며 쓸고 지나가는 면적을 같게 | **기각 권고.** QA 실측 스윕 순위(흩어짐 309 > 뾰족함 242 > 둥긂 145 > 길쭉함 51)는 기대 데미지 순위(둥긂 1위, 흩어짐 0.46)와 **상관이 거의 없다.** 스윕을 균등화해도 형평성은 안 생기고, 사거리(=범위 값)와 면적이 얽혀 공식만 복잡해진다 |
| **(다) 단일 표적 기대 데미지** ⭐추천 | 「총 면적」 대신 **「유효 명중 폭」**(진행축에 수직인 단면 폭)을 범위 값으로 정규화 | 20~24행 대원칙의 문장이 바뀐다 → **사용자 승인 사항.** 화면에 보이는 판정 크기가 태그마다 달라진다(다탄 태그는 총 면적이 N배까지 커진다). 다중 표적 위력이 태그마다 갈려 5v5 난전 재검증 필요. 「얇은 선으로 맵 관통」 차단 근거(58행)가 면적이 아니라 **길이 상한**으로 옮겨간다 |

#### 개정 전(면적 정규화)의 실제 수치 — 무엇을 버렸는지 기록

| 태그 | 유효 명중 폭 | 기대 데미지(점) | **QA 2차 실측(점)** | 기대 데미지(0.4m) | **QA 2차 실측(0.4m)** |
|---|---|---|---|---|---|
| 둥긂 | 2.855m | 1.000 | 855 (1.000) | 1.000 | 1095 (1.000) |
| 뾰족함 | 1.648m | 0.577 | 505 (0.591) | 0.670 | 745 (0.680) |
| 흩어짐 | 1.277m | 0.447 | 387 (0.453) | 0.568 | 621 (0.567) |
| 길쭉함 | 1.265m | 0.443 | 405 (0.474) | 0.565 | 615 (0.562) |

**격차 2.21배(점) / 1.78배(0.4m).** 「같다」가 아니다. 개정 후에는 정의상 **1.00배**다.
(정정: 이전 판본이 길쭉함 예측치를 316 으로 적었던 것은 오기다. `유효폭 1.2649 × 300 =
**379.5**` 가 맞고, 316 은 `LONG_LEN_RATIO 2.4` 를 넣었을 때의 값이었다. QA N4 지적 반영.)

#### 개정 후 태그 4종 (범위 50 · 데미지 60 · 표적 반경 0.4m)

| 태그 | 유효 명중 폭 | 기대 데미지(점) | 기대 데미지(0.4m) | 판정 면적 합 | 조준 명중 데미지 |
|---|---|---|---|---|---|
| 둥긂 | **2.855m** | **1.000** | 1.000 | 6.40㎡ | 60 (전 거리) |
| 뾰족함 | **2.855m** | **1.000** | 1.000 | 19.20㎡ | 60(≤6.4m) → 20 |
| 흩어짐 | **2.855m** | **1.000** | 1.000 | 32.00㎡ | 60(≤8.6m) → 36(≤17.4m) → 12 |
| 길쭉함 | **2.855m** | **1.000** | 1.000 | 12.92㎡ | 60 (전 거리) |

세기는 같아지고 **개성은 「낙폭 모양」과 「판정 부피」로 남는다.** 그게 개정의 요점이다.

⚠ **「조준 명중 데미지」 칸은 기본 탄 수 기준이다** (뾰족함 3발 · 흩어짐 5발).
그림 미세 파라미터가 탄 수를 바꾸면 계단의 개수와 높이가 같이 바뀐다. 다만
**마지막 계단은 언제나 「가운데 탄 1발 = 데미지÷탄 수」이지 0 이 아니다** —
탄 수를 홀수로만 뽑기 때문이다(아래 「홀수 탄 원칙」). 실제 값은 제작창이 띄운다.
(2026-07-31 정정: 종전에는 짝수 탄이 나올 수 있어 이 칸이 **0** 이 되는 구간이 있었다.
4차 감사 위반 3 으로 잡혔고, 홀수 탄 원칙으로 막았다.)

### ✅ 미세 파라미터 배선 — 일부 해금 (2026-07-31, 사용자 승인 뒤)
「나중 확장 여지」 우선순위 2 를 다시 판정한다. 종전에 전면 보류한 이유는
**면적 정규화에서는 탄 수가 곧 세기(∝1/√N)** 였기 때문이다. 폭 정규화에서는
`기대 데미지 = Σ(2r+2R)·(D/N) = D·(W+2R)` 로 **N 이 상쇄**되므로 그 이유가 사라진다.

| 축 | 판정 | 근거 |
|---|---|---|
| 흩어짐 **탄 수**(덩어리 수 → **3·5·7발**) | ✅ **해금** | N 상쇄. 폭·기대 데미지 불변. 검산으로 확인(3·5·7발 전부 폭 2.8546 · 총 데미지 60.00). **홀수만** — 아래 「하한 재확정」 절 |
| 뾰족함 **탄 수**(복잡도 → **3·5발**) | ✅ **해금** | 같은 근거. 역시 홀수만 |
| 흩어짐 **퍼짐각**(탄 수 → **18°·24°·27°**) | ✅ **해금** | 기대 데미지와 무관. 탄 수에 묶어 **정조준 데미지 지배 전략을 없앤 값**이다(QA N7) |
| 뾰족함 **퍼짐각**(복잡도 → **32°~46°**) | ✅ **해금** | 원래부터 기대 데미지와 무관. 순수 제로섬(붙어야 하는 대신 밴드가 넓어진다). 하한은 흩어짐 24° 를 침범하지 않게 32° 로 올렸다 |
| 길쭉함 **날씬함**(신장도 → 길이) | ⛔ **보류 유지** | 정지 표적엔 무해하지만 캡슐 길이는 체류 시간(길이÷속도)을 늘려 **움직이는 표적**에 대한 관용을 키운다(범위 50 에서 +1.5m 상당). 닫힌 식이 못 잡는 2차 효과라 **4단계 실측 뒤 재상정**. 최악값은 길이 상한이 이미 묶고 있다 |
| 둥긂 **관통**(채움률 → 최대 2명) | ⛔ **사용자 확인 대기** | 새 메커니즘. 변동 없음 |

`SCATTER_SPREAD_REF_PELLETS`(퍼짐각을 √N 배로 넓히던 보정)는 **폐기**한다.
기대 데미지가 애초에 N 과 무관해져 보정할 것이 없고, 넣으면 조준 명중 데미지만 깎여
순손해가 된다. 상수는 과거 문서 대조용으로만 남기고 어디서도 호출하지 않는다.

### ✅ 미세 파라미터 **하한 재확정** + 홀수 탄 원칙 (2026-07-31, 밸런스 담당 — 4차 감사 위반 3 해소)

배선을 해금하자 **하한 구간에서 뾰족함의 정체성이 무너졌다.** 기본값(3발·32°)만
보면 안 보이고, 복잡도가 문턱(2.5) 바로 위일 때만 나타난다. 감사가 실행으로 잡았다.

#### 무엇이 깨졌나
| 복잡도 | 종전 퍼짐각 | 종전 탄 수 | 전탄 유지 | 사거리 대비 | 흩어짐(24° · 40.9%) 대비 |
|---|---|---|---|---|---|
| 2.50 | **22.0°** | **2발** | 9.40m | **44.8%** | 더 **좁고** 더 **멀리** 유지 ⛔ |
| 2.64 (십자) | 22.4° | 2발 | 9.23m | 44.0% | 같은 문제 ⛔ |

이 두 줄은 기획 담당이 `CONE_SPREAD_DEG 11.0` 을 폐기할 때 든 사유 두 개
(「부채꼴이 산탄보다 좁다」·「근거리인데 산탄보다 멀리 유지된다」)와 **글자 그대로 같다.**

#### 왜 한 줄의 부등식으로 정리되나
폭 정규화 이후 **뾰족함과 흩어짐의 탄 반경은 서로 같다**(둘 다 유효 명중 폭 ÷ 2).
그래서 `전탄 유지 = (탄반경 + 표적반경) ÷ tan(퍼짐각÷2)` 에서 탄 반경이 약분되고,
두 태그를 가르는 것은 **오직 퍼짐각**이 된다:

> **퍼짐각 > 27.0°  ⟺  「부채꼴이 산탄보다 넓다」 이면서 「전탄 유지가 산탄보다 짧다」**

즉 두 정체성 조건이 하나다. 그리고 탄 반경이 약분되므로 이 부등식은 **범위 1~100
어디서나 동시에 성립**한다(실행 검산으로 확인).

(🐞 **정정 2026-08-01, 6차 감사 ⚠6** — 종전 판본은 이 부등식을 **24.0°** 로 적었다. 24.0° 는
흩어짐 **5발(기본)** 값이고, 흩어짐 퍼짐각은 탄 수에 묶여 **18°·24°·27°** 로 움직인다.
**모든** 흩어짐 판을 이기려면 기준은 그 상한인 **27.0°** 여야 한다.
**확정값 32.0° 는 그래도 통과한다**(여유 5°). 아래 「흩어짐 퍼짐각을 탄 수에 묶는다」 절이 이미 27° 기준으로 다시 적어 놓았고,
이 줄만 옛 기준으로 남아 있었다 — 한 문서 안의 두 기준을 하나로 맞춘다. **상수 변경 없음.**)

#### 확정값
| 상수 | 종전 | **확정** | 근거 |
|---|---|---|---|
| `CONE_ANGLE_MIN` | 22.0 | **32.0** | 기본값과 같은 값 = 뾰족함의 정체성 값(유지 = 사거리의 30%). **미세 파라미터는 정체성을 진하게만 할 수 있고 묽게 할 수는 없다** |
| `CONE_ANGLE_MAX` | 46.0 | 46.0 유지 | 유지 20.5%. 「복잡할수록 더 붙어야 한다」 |
| `CONE_PELLETS_MIN` | 2 | **3** | 홀수 탄 원칙 |
| `CONE_PELLETS_MAX` | 5 | 5 유지 | 홀수. 뾰족함은 3발 또는 5발 |
| `SCATTER_PELLETS_MIN` | 2 | **3** | 홀수 탄 원칙 |
| `SCATTER_PELLETS_MAX` | 6 | **7** | 하한이 올라간 만큼 상한도 올려 「많이 그릴수록 손해」를 막는다 |

**경계 바로 위(28° 등)로 잡지 않은 이유**: 여유가 1~2°·3%p 뿐이라 앞으로
`SCATTER_SPREAD_DEG` 를 1° 만 건드려도 정체성이 다시 뒤집힌다. 하한을 정체성 값에
붙여 두면 그 종류의 재발이 구조적으로 불가능하다.

**[검산] 범위 50 · 데미지 60 · 표적 반경 0.4m (흩어짐 기준 = 탄 5발 24.0° · 8.60m · 40.9%)**

| 복잡도 | 퍼짐각 | 탄 수 | 전탄 유지 | 사거리 대비 | 넓은가 | 짧은가 |
|---|---|---|---|---|---|---|
| 2.50 (문턱) | 32.0° | 3발 | 6.37m | 30.3% | O | O |
| 2.64 (십자) | 32.2° | 3발 | 6.33m | 30.1% | O | O |
| 5.50 | 36.4° | 3발 | 5.55m | 26.4% | O | O |
| 8.00 | 40.1° | 5발 | 5.01m | 23.8% | O | O |
| 12.00 이상 | 46.0° | 5발 | 4.30m | 20.5% | O | O |

범위 드리프트(전탄 유지 ÷ 사거리) — **전 구간에서 흩어짐보다 짧다:**

| 범위 | 1 | 18 | 50 | 100 |
|---|---|---|---|---|
| 흩어짐 | 53.5% | 50.0% | 40.9% | 32.6% |
| 뾰족함 하한(c=2.5) | 39.6% | 37.0% | 30.3% | 24.1% |
| 뾰족함 상한(c=12) | 26.8% | 25.0% | 20.5% | 16.3% |

#### 홀수 탄 원칙 — **다탄 태그의 탄 수는 언제나 홀수다**

`Player._spawn_spread()` 는 탄을 `-퍼짐/2 ~ +퍼짐/2` 에 고르게 편다. **짝수면
정중앙 탄이 없다.** 그래서 전탄 유지 거리를 넘는 순간 **완벽하게 조준한 표적이
받는 데미지가 0** 이 된다 — 탄이 전부 좌우로 비켜 간다.

- [실행] 탄 2발·24°·범위 50: 8.60m 까지 60, 그 뒤 **0**. 살짝 빗맞히면 30.
  → **잘 조준할수록 손해.** 게임 규칙으로 낼 수 없는 값이다.
- 짝수 4발도 최대 범위(100)에서 안쪽 두 발의 벌어짐이 33.66m 라 사거리 34.0m
  끝자락에 같은 구멍이 생긴다. 그래서 「2발만 금지」가 아니라 **홀수만 허용**이다.

**덩어리 수 → 탄 수 (홀수로 올린다)**: 2→3 · 3→3 · 4→5 · 5→5 · 6 이상→7.
홀수로 그린 덩어리는 그대로 맞아떨어지고, 짝수는 한 발이 더 붙는다.
복잡도 → 뾰족함 탄 수: 3발 또는 5발.

**다른 선택지를 버린 이유**
| 안 | 기각 사유 |
|---|---|
| (나) 짝수면 가운데 한 발을 더 둔다 | 결국 홀수가 된다. 같은 계산을 두 곳에서 하게 되고 「탄 N발」 표시가 또 어긋난다 |
| (다) 퍼짐 배치를 바꾼다 | 짝수에 중앙 탄을 넣으려면 좌우 비대칭이 된다 — 부채꼴이 한쪽으로 기운다. `Player` 코드도 고쳐야 한다 |
| (라) 그대로 두고 문서를 고친다 | 「정조준 0 데미지」를 사실로 인정하는 것이라 기각 |

**(가) 를 고른 결과 `Player._spawn_spread()` 는 한 글자도 안 고쳐도 된다.**
홀수면 `i=(N-1)/2` 에서 `t=0.5` → 각도 0 인 중앙 탄이 자동으로 생긴다(실행 확인).
제작창의 「그 뒤로는 일부만 맞는다」 문구도 이제 언제나 참이다.

**[실행] 정조준 데미지 프로필 (범위 50 · 데미지 60 · 표적 0.4m)**

| 그림 | 탄 수 | 4m | 8m | 12m | 16m | 21m |
|---|---|---|---|---|---|---|
| 흩어짐 덩어리 2 | 3발 | 60 | 60 | 20 | 20 | 20 |
| 흩어짐 덩어리 5(기본) | 5발 | 60 | 60 | 36 | 36 | 12 |
| 흩어짐 덩어리 6 | 7발 | 60 | 42.9 | 25.7 | 25.7 | 25.7 |
| 뾰족함 c=2.5(하한) | 3발 | 60 | 20 | 20 | 20 | 20 |
| 뾰족함 c=12(상한) | 5발 | 60 | 36 | 12 | 12 | 12 |

**0 이 사라졌다.** 그리고 뾰족함이 흩어짐보다 항상 먼저 떨어진다(=근거리).

#### 남은 대가 — 4단계 재검증에 추가
1. **탄 7발 = 판정 부피가 기준의 7배**(범위 50 에서 44.8㎡). 한 명 상대 기대 데미지는
   그대로지만 **난전 위력**이 종전 상한(6발·38.4㎡)보다 커졌다. 「개정의 대가 1번」과
   같은 항목이므로 함께 재검증한다.
2. **덩어리 2~3개짜리 흩어짐은 낙폭이 「완만한 2단」이 아니라 「1단 절벽」이다**
   (3발이라 60 → 20). 태그 표의 「견제형」은 기본값 5발 기준 서술이다.
   덩어리를 적게 그릴수록 굵고 급해지는 것은 의도된 기울기지만, **형태 태그 표의
   「체감」 칸을 손봐야 할 수도 있다 — 표 문구 변경은 사용자 승인 사항**이라
   여기에 사실만 적어 둔다.

### ✅ 흩어짐 퍼짐각을 **탄 수에 묶는다** (2026-07-31, 밸런스 담당 — QA 3차 N7 해소)

**문제(QA 3차 8,600발 실측)**: 퍼짐각이 24° 로 고정이면 **탄이 많을수록 정조준 데미지가 크다.**
기대 데미지는 탄 수와 무관한데(N 상쇄) 조준 데미지만 커지므로,
**「덩어리를 많이 그리는 것」이 아무 대가 없는 지배 전략**이 됐다.
유저가 체감하는 것은 기대값이 아니라 조준 데미지다 — 창작이 아니라 정답 맞히기가 된다.

**해법**: 탄이 적으면 **좁게 모아** 쏜다. 유저 직관과도 맞는다 — 덩어리를 두어 개
그리면 뭉쳐 날아가고, 잔뜩 그리면 넓게 흩뿌린다. 각도는 「0~사거리 구간의 정조준
데미지 평균」이 기본값(5발)과 같아지는 값으로 잡았다.

| 탄 수 | 퍼짐각 | 전탄 유지(범위 50) | 사거리 대비 |
|---|---|---|---|
| 3발 (덩어리 2~3) | **18.0°** | 11.54m | 54.9% |
| 5발 (덩어리 4~5) | **24.0°** (기본·종전 그대로) | 8.60m | 40.9% |
| 7발 (덩어리 6 이상) | **27.0°** | 7.61m | 36.2% |

**[실행] 정조준 데미지 평균 — 지배 전략이 사라졌다** (데미지 60 · 표적 0.4m · 0.05m 간격)

| 범위 | 3발 | 5발 | 7발 | 격차 |
|---|---|---|---|---|
| 1 | 48.61 | 48.80 | 46.08 | 1.059배 |
| 18 | 46.88 | 47.95 | 44.82 | 1.070배 |
| 50 | 41.96 | 41.67 | 41.34 | **1.015배** |
| 100 | 37.50 | 35.65 | 36.12 | 1.052배 |

**종전 1.81배**(QA 3차 실측, 짝수 탄 포함) **→ 홀수 탄 원칙으로 1.193배 → 이 조치로 1.015~1.070배.**
남은 5~7% 는 탄이 이산적이라 생기는 반올림 잔차다(연속으로 만들 방법이 없다).

**[실행] 태그 정체성은 안 깨진다** — 범위 1·18·50·100 전부에서
흩어짐 각도 상한 **27° < 뾰족함 각도 하한 32°**, 흩어짐 유지 하한 **28.8% > 뾰족함 유지 상한 24.1%**
(범위 100 기준). 두 태그의 구간이 어디서도 겹치지 않는다.

#### 남은 것 — **태그 사이의 조준 데미지 격차 2.21배는 그대로다** (⏳ 사용자 확인 필요)
같은 범위·데미지에서 0~사거리 평균 정조준 데미지는
**둥긂·길쭉함 60.0 · 흩어짐 41.3~42.0 · 뾰족함 27.2~32.1** 로 **최대 2.21배** 갈린다.
이것은 「전탄 유지 거리 뒤에 계단식으로 떨어진다」는 다탄 태그의 정의 그 자체이므로
**없앨 수 없다**(없애려면 다탄 태그를 없애야 한다).

- 개정된 대원칙 문장은 **「한 명을 상대로 한 기대 데미지」** 만 균등화 대상으로 못박고 있으므로
  문자 그대로는 위반이 아니다. 실측 기대 데미지 격차는 1.035배로 지켜지고 있다.
- 그래도 **「정조준 데미지는 균등화 대상이 아니라 태그 정체성이다」를 대원칙에 명시하는 편이
  정직하다.** 대원칙 블록은 `✅ 결정됨` 이라 **사용자만 고칠 수 있어** 여기에 초안만 남긴다:
  > 추가 문안(초안): 「균등화되는 것은 **좌우 위치를 모르는 표적에 대한 기대 데미지**다.
  > 완벽히 조준했을 때의 데미지는 태그마다 다르며, 그 차이가 태그의 정체성이다 —
  > 단발은 전 거리 풀데미지, 다탄은 붙을수록 세다.」
- **같은 태그 안에서의 격차(그림이 세기를 정하는 부분)는 이 조치로 1.07배까지 좁혔다.**
  기획이 막아야 했던 것은 그쪽이고, 그건 막았다.

### ✅ [N9] 전방 도달점을 네 태그에서 맞췄다 (2026-07-31, 밸런스 담당 — QA 3차)

`forward_lead`(판정이 원점보다 앞으로 뻗은 길이)를 **캡슐에만** 걸고 구에는 0 으로 뒀던 것이
버그였다. 구도 판정이 **반경만큼 앞으로** 뻗어 있다. 그래서 캡슐만 사거리에 맞고 나머지
셋이 표시보다 멀리 닿았다 — **N2 의 부호만 뒤집힌 재발**이다.

| 범위 100 | 표시 사거리 | 종전 실측(QA 3차) | **조치 후(계산)** |
|---|---|---|---|
| 둥긂·뾰족함·흩어짐 | 34.00m | 35.95m (+5.7%) | **34.00m** |
| 길쭉함 | 34.00m | 33.85m | **34.00m** |

구·부채꼴·산탄의 `forward_lead` 를 **탄 반경**으로 채웠다(캡슐은 종전대로 길이).
[실행] 범위 1~100 × 태그 4종 × 미세 파라미터 5종 전 조합에서 `distance + forward_lead − reach`
오차 **0.0000000000 m**. 「사거리는 오직 범위 값이 정한다」가 이제 네 태그 전부에서 성립한다.
대가: 비행 거리가 탄 반경만큼 짧아진다(범위 100 둥긂 34.00 → 32.05m). 도달점은 그대로다.

### ✅ [N11] 닫힌 식 정정 — `1/cos θ` 항이 빠져 있었다 (2026-07-31, 밸런스 담당)

비스듬히 나는 탄은 좌우 스캔축에 대해 밴드가 `2r ÷ cos θ` 로 **넓어진다**. 정확한 식은

> **기대 데미지 = 데미지 × (평균 1/cos θᵢ) × (유효 명중 폭 + 표적 지름)**

이고, 종전 식은 `평균 1/cos θᵢ = 1` 로 놓은 근사였다. 실제 크기(계산 = QA 실측):

| 구성 | 평균 1/cos θ | 실제 이득 | QA 3차 실측 잔차 |
|---|---|---|---|
| 흩어짐 24°·5발 | 1.0111 | +1.1% | **+1.1%** |
| 뾰족함 32°·3발 | 1.0269 | +2.7% | **+2.7%** |
| 뾰족함 46°·5발 (현행 최대) | 1.0428 | +4.3% | – |
| (가정) 60° | 1.0760 | +7.6% | – |
| (가정) 90° | 1.1986 | +19.9% | – |

QA 가 「잔차 3.5% 의 출처」로 지목한 값과 **소수 첫째 자리까지 일치한다.** 식이 맞다.

**보정하지 않는다.** 보정하려면 탄 반경을 cos 항만큼 줄여야 하는데, 그러면
`effective_width(box) == effective_width_for(total_area)`(오차 0)라는 **검증 가능한 불변식**이
깨진다. 4.3% 는 그 불변식을 흐리면서까지 살 값이 아니다.
**대신 규칙으로 남긴다 — `CONE_ANGLE_MAX` 를 46° 위로 더 열려면 이 항을 먼저 보정하라.**

### ✅ [D-2] 연결 판정 4방향 → **8방향** (2026-07-31, 밸런스 담당)
「별을 그렸는데 산탄이 나간다」(개발·QA 3차 지적)의 원인이다.

픽셀 격자에서 **대각선 획은 계단 모양**이라 위·아래·좌·우로는 안 닿고 모서리로만
닿는다. 4방향으로 연결 요소를 세면 그 획이 **끊어진 것으로 잡혀** 한붓그리기로 그린
별·번개가 「덩어리 여러 개」가 되고, 덩어리 ≥ 2 라 흩어짐으로 튄다.

**[실행] 스포크 별 192 조합**(갈래 4·5·6·8 × 회전 0~75° × 반지름 4종 × 굵기 2종):

| 연결 판정 | 흩어짐으로 오판된 별 |
|---|---|
| 종전 4방향 | **46 조합** (6갈래만이 아니었다 — 4·5·8갈래에도 있었다) |
| 현행 8방향 | **0 조합** |

사람 눈에 「이어져 보이면 한 덩어리」가 맞으므로 8방향이 유저 직관과 같은 쪽이다.
**둘레(복잡도) 계산은 4방향 그대로 둔다** — 둘레는 「면과 면이 맞닿는 길이」라
대각선 이웃을 세면 값이 부풀어 복잡도 문턱(2.5)이 통째로 흔들린다.

**[실행] 기본 스킬 4개 태그는 안 바뀐다**: 원 r6 둥긂 · 막대 길쭉함 ·
점 3개 흩어짐(덩어리 3 그대로) · 원 r13 둥긂.

### 「뾰족함」의 정체성 — 부채꼴 유지 (2026-07-31, 밸런스 담당 — 3차 감사 위반 3 해소)
**결정: 78행 형태 태그 표를 고치지 않는다.** 「부채꼴 다단 히트 / 근거리 난사형」 그대로 두고
**코드를 문서에 맞춘다.** (문서를 고치는 쪽은 사용자 승인 사항이고, 규칙 3 「코드 먼저·문서
나중 금지」에도 어긋난다. 표를 지키는 쪽이 승인 없이 갈 수 있는 유일한 길이다.)

문서대로 만들기 위해 바꾸는 것 3가지 (괄호 안은 폭 정규화 개정 후 값):
1. **퍼짐각 32°** (1차 확정 22°) — 흩어짐(24°)보다 **넓다.** 「부채꼴」이 이름값을 한다.
2. **전탄 유지 거리 = 사거리의 30%** — 흩어짐(41%/83%)보다 **짧다.** 「근거리」가 성립한다.
   그 뒤로는 가운데 탄 1발(데미지 ÷3)만 남는 **절벽**이다. 흩어짐의 완만한 2단 낙폭과 대비된다.
   QA 2차 실측으로 확인됐다: 6.35m 까지 3발 · **6.60m 부터 곧장 1발**(2발 구간이 없다).
3. **히트박스 `angle_deg` 를 90° 고정에서 퍼짐각과 같은 값으로** — 지금까지 `angle_deg` 는
   전투에서 아무 데도 안 쓰이면서 제작창에는 「부채꼴 90°」로 떴다(위반 4). 문서·표시·전투를
   **한 숫자로 묶는다.** 같은 이유로 흩어짐에도 `angle_deg` 를 채운다(QA N1 — 아래).

**흩어짐과 무엇이 다른가 (한 줄로 구분된다)**
| | 뾰족함 | 흩어짐 |
|---|---|---|
| 퍼짐 | 32° (넓다) | 24° (좁다) |
| 탄 | 3발 · 한 발 데미지 D/3 (굵다) | 5발 · 한 발 데미지 D/5 (잘다) |
| 낙폭 | 30% 지점에서 60 → 20 **절벽** | 41% 60 → 83% 36 → 12 **완만** |
| 판정 부피 | 탄 3발 = 기준의 3배 | 탄 5발 = 기준의 5배 (난전에 강하다) |
| 체감 | 붙어서 한 번에 쏟아붓는다 | 거리를 두고 야금야금 깎는다 |

**사거리·쿨타임으로 보상하지 않는 이유**: 기대 데미지는 퍼짐각과 무관하게 이미 정해져
있고, 사거리를 깎으면 **대가만 늘고 보상은 안 생긴다.** 쿨타임을 깎아 보상하는 안도
기각한다 — 조준 정확도의 손해를 발사 횟수로 갚으면 **잘 조준하는 사람만 두 배로 이득**을 보아
형평성이 더 나빠진다. (뾰족함의 실전 세기는 폭 정규화 개정으로 이미 둥긂과 같아졌다)

### 🐞 QA 2차 지적 처리 — N1 · N2 (2026-07-31, 밸런스 담당)

**N1 — 제작창이 흩어짐에 대해 「전탄 명중: 사거리 전체」라고 거짓말했다.**
원인은 `Balance.hitbox_for()` 의 흩어짐 갈래가 `angle_deg` 를 안 채운 것이었다
(0.0 → `full_hit_distance` 가 INF → 「사거리 전체」). **근본 자리인 `hitbox_for` 에서
`angle_deg = SCATTER_SPREAD_DEG` 를 채웠다.** 검산: `angle_deg 24.0 · 전탄 유지 8.60m`.
개발이 에디터에 넣어 둔 임시 폴백(`_spread_of`)은 이제 안 타지만 남겨 둬도 무해하다.

**N2 — 길쭉함의 실효 사거리가 표시값의 1.21배였다(표시 34.0m ↔ 실측 41.1m).**
캡슐이 원점 **앞쪽**에 통째로 놓이므로 전방 도달점 = 비행 거리 + 캡슐 길이였다.
「길쭉함을 그리면 사거리가 20% 는다」는 대원칙 정면 위반이다.
**결정: 표시를 늘리는 쪽이 아니라 비행 거리를 줄이는 쪽으로 간다.**
사거리는 오직 `범위` 값이 정해야 하므로, 도달점을 `범위` 가 정한 값에 고정하는 것이 옳다.
- `Balance.derive()` 가 **비행 거리 = 사거리 − 캡슐 길이**로 내려준다.
- 새 반환 키 **`reach`** = 전방 실효 도달 거리(= `범위` 가 정한 값). 네 태그 전부 같다.
- 검산: 범위 1/18/50/100 에서 길쭉함 도달점 8.26 / 12.68 / 21.00 / 34.00m — 둥긂과 완전 일치.
- ⚠ **제작창이 「사거리」로 보여줄 값은 이제 `distance` 가 아니라 `reach` 다.**
  안 고치면 길쭉함만 사거리가 짧게(21.0 → 15.9m) 표시된다. 개발 요청에 올렸다.

### [D-1] 길쭉함 지표 — 종횡비 → 신장도 (2026-08-01, 밸런스 담당 · **부분 해결**)

> **⚠ 먼저 읽을 것 — 「회전 문제 해결」이 아니다 (2026-08-01, 6차 감사 반려로 정정 · QA 5차 실측으로 확정).**
> 이 절의 종전 판본은 제목과 본문에 「**해결**」·「회전 불변」이라고 적었다. **사실이 아니다.**
> 해결된 것: **명확히 길쭉한 그림에 한해** — 즉 신장도가 **최악의 각도에서도** 문턱 2.5 위에
> 남는 그림(실측 표본은 전부 **비율 4.4 이상**)은 기울여도 길쭉함을 유지한다.
> 아래 오판 **908 → 0** 은 진짜 성과이고 QA 가 **독립 래스터라이저로 0/1,448 을 재현**했다.
> **다만 그 표본은 문턱 2.5 를 한 번도 안 건드린다.** 그래서 그 표가 증명하는 것은
> 「**문턱에서 먼 그림은 안 흔들린다**」이지 「회전 불변」이 아니다 (QA 5차 1-A 판정).
> **안 된 것: 문턱 2.5 근처(비율 대략 1.9 ~ 2.8) 그림은 지금도 회전으로 태그가 갈린다.**
> 원인은 지표가 아니라 **32×32 이진 격자에 다시 그리는 것**(래스터화) 자체다.
> 상세·실측은 아래 「⚠ 알려진 판정 구멍」의 회전 항목 두 개.
> **거기를 안 읽고 이 절만 읽으면 오해한다.**

**무엇이 문제였나.** 종횡비를 축에 나란한 바운딩박스로 재기 때문에, 그림을 기울이면
바운딩박스가 정사각형이 되어 종횡비가 1.00 으로 무너졌다. 같은 막대를 45° 돌렸을 뿐인데
길쭉함이 뾰족함이 됐다. **"내가 그린 대로 나간다"가 정면으로 깨지는 자리였다.**

**얼마나 자주·얼마나 손해였나 (QA 4차 6장 실측).**
- 막대를 **13~20° 이상 기울이면 뾰족함**이 되고 **70~78° 를 넘어야** 길쭉함으로 돌아온다.
  0.5° 전수 스캔에서 **전 각도의 55~73%** 가 오판 구간(짧고 굵을수록 넓다).
- 손떨림을 넣은 실제 브러시 획도 30°/45°/60° 전부 뾰족함. **사선 획은 흔한 그림이다.**
- 손해: 조준 데미지 **60.00 → 32.57(1.84배)**, 범위 100 에선 **2.44배**. 한 발 데미지가 1/3이 되고
  6.4m 절벽이 생긴다. 게임 안에서 가장 큰 단일 격차이면서 **의도한 것이 아니라 버그**였다.

**무엇으로 바꿨나.** 칠한 픽셀 분포의 2차 모멘트(공분산) 고유값 비 — **신장도** `√(l1/l2)`.
회전은 공분산 행렬을 닮음변환할 뿐 고유값을 안 바꾸므로 **좌표축을 어디로 잡든 값이 같다.**

⚠ **그 불변성이 성립하는 것은 「같은 점 집합을 다른 축에서 볼 때」뿐이다.** 유저가 그림을
기울여 그리면 점 집합 자체가 달라진다 — 32×32 격자에 다시 그리는 순간 픽셀이 재배치되고
**값이 각도마다 흔들린다.** 종횡비처럼 1.00 으로 무너지지는 않지만 0 도 아니다.
지표를 바꿔서 **흔들림 폭이 줄어든 것이지 없어진 것이 아니다.** 아래 「알려진 판정 구멍」 참조.

**문턱 2.5 를 그대로 쓴 근거 — 지표가 바뀌었는데 숫자를 안 바꾼 이유**
1. **뜻이 안 바뀐다.** 축에 나란한 꽉 찬 직사각형에서 **신장도 = 가로/세로 픽셀 수**가
   정확히 성립한다(폭 w 픽셀의 이산 분산 (w²−1)/12 에 픽셀 폭 보정 +1/12 를 더하면 w²/12).
   [실행] 직사각 16×8 → 2.000 · 16×6 → 2.667 · W 관통창 6×32 → 5.333. 오차 0.
2. **안 새는 여유.** 십자 5종 · 별 10종을 0~90° 로 돌린 전수 스캔에서 신장도 최댓값 **1.11**,
   손떨림 원 **1,280 조합** 최댓값 **1.774**. 길쭉함으로 샌 것 **0건** (여유 1.41~2.25배).
3. **잡히는 여유.** QA 가 쓴 막대 8종 전수에서 신장도 최솟값 **4.16**(여유 1.7배),
   손떨림 브러시 획 285 조합 최솟값 **2.99**(여유 1.2배).
4. **다른 문턱은 더 나쁘다.** 2.2 → **45° 로 기울인 타원 12:5** 가 길쭉함이 되어 현행 동작이 바뀐다.
   2.8·3.0 → 길쭉함에서 밀려난 막대가 복잡도 판정으로 넘어가 **둥긂↔뾰족함 흔들림이
   50 → 69 → 75 조합으로 늘어난다**(막대 235조합 회전 전수 스캔).
   ⚠ 여기의 「50」은 **235조합 표본에서 잰 현행 문턱의 기준선**이다. QA 5차가 240조합으로
   다시 재면 **63조합(26.3%)** 이다(아래 복잡도 항목). **문턱을 올리면 늘어난다는 비교
   결론은 그대로**지만, **절대 수치를 인용할 때는 63 쪽을 쓸 것.**
   > **🐞 숫자 정정 (2026-08-01, 6차 감사 ⚠4).** 종전 판본은 「타원 12:5(신장 2.32)」라고만 적어
   > **각도를 안 밝혔다.** 감사가 재현하니 어긋났고, 내가 다시 재도 같은 결과다.
   > 도형 정의: **반축 12×5 픽셀의 채워진 타원**(픽셀 중심이 타원 안이면 칠함).
   > [실행] 축에 나란할 때(0°·90°) 신장 **2.884 → 이미 길쭉함**(현행 문턱 2.5 에서도).
   > 45° 로 돌리면 **2.311 → 뾰족함**. 즉 문턱을 2.2 로 낮추면 **그 45° 판이** 길쭉함으로 넘어온다.
   > 근거 자체는 그대로 서지만 **인용할 때 반드시 「45°」를 붙일 것.**
   > ⚠ 그리고 이 타원은 **그 자체가 아래 「알려진 판정 구멍」의 실례**다 —
   > [실행] 0~90° 91각도에서 신장 **2.020~2.884(변동 42.8%)** · 태그 **둥긂 82 · 길쭉함 8 · 뾰족함 1**.
   > 같은 타원을 어떻게 기울여 그리느냐로 태그 세 개가 다 나온다.

**검증 결과 [실행]** — ⚠ **모든 「오판 0」에 「명확히 길쭉한 그림에 한해」가 붙는다.**
표본이 전부 **비율 4.4 이상**이라 문턱 2.5 를 한 번도 안 건드리기 때문이다(바로 아래 상자).

| 항목 | 종전 | 개정 후 | QA 5차 독립 재현 |
|---|---|---|---|
| 막대 8종(**전부 비율 4.4↑**) × 181각도 = 1,448 지점 | 오판 **908 (55~73%)** | 오판 **0** | **0 / 1,448** ✅ |
| 손떨림 브러시 획 285 조합(신장도 최솟값 2.99) | 오판 **177 (62.1%)** | 오판 **0** | **0 / 285** ✅ |
| 십자·별 1,095 지점이 길쭉함으로 새는가 | – | **0건** |
| 손떨림 원 1,280 조합이 길쭉함으로 새는가 | – | **0건** |
| **기본 스킬 4개 태그** | 둥긂·길쭉함·흩어짐·둥긂 | **전부 동일** |
| 도형 40종 회귀 | – | **6종만 변함**(전부 의도한 방향, 아래) |
| 폭 정규화 오차 · `derive` 수치 | – | **한 자리도 안 변함** (이번 개정은 지표/태그만 건드렸다) |

> **⚠ 이 표를 「회전 문제가 다 끝났다」로 읽지 마라 (2026-08-01 정정, QA 5차 1-A 가 같은 판정).**
> 위 「오판 0」은 **표본이 전부 문턱에서 먼 그림**이라 나온 값이다. 근거: 바로 위 3번에
> 적힌 대로 **그 막대 8종은 전부 비율 4.4 이상**이고 신장도 최솟값이 **4.16**(문턱 2.5 의
> 1.7배), 브러시 획 285 조합의 최솟값이 **2.99**(1.2배)다.
> **흔들리는 띠(비율 대략 1.9~2.8)에 드는 그림은 이 표본에 한 개도 없다.**
> 그 구간을 따로 재면 오판이 0 이 아니다 — 아래 「알려진 판정 구멍」의 회전 항목 표.
> 표의 뜻은 「**명확히 길쭉한 그림에 한해** 오판 908 → 0」이지 「회전 오판 전멸」이 아니다.
> QA 표현 그대로: 「**문턱에서 먼 그림은 안 흔들린다**를 보인 것이지 회전 불변을 보인 게 아니다.」

바뀐 6종: 대각선 1px · 막대 26×2 45° · 14×2 45° · 10×4 45° 가 **뾰족함 → 길쭉함**(고쳐진 것),
원 r6 + 좌우끝 1px 이 **길쭉함 → 둥긂**, 원 r6 + 네 구석 1px 이 **뾰족함 → 둥긂**(아래 S3).

**반환 키 — `aspect` 는 안 건드렸다.**
`SkillEditor` 가 `aspect` 를 「종횡」으로 띄우고 있어 뜻을 바꾸면 화면이 조용히 거짓말한다
(이 프로젝트에서 「화면이 실제와 다른 값을 띄운다」가 이미 네 번 재발했다).
그래서 **교체가 아니라 추가**다 — 새 키 `elongation` 이 생겼고 판정은 그걸 본다.
`aspect` 는 뜻·이름 그대로 남아 계속 화면에 뜬다. 다만 **`aspect` 만 봐서는 이제 태그를
예측할 수 없으므로**(대각선 막대는 종횡 1.00 · 신장 11.7) 제작창 지표 줄에 신장도를 같이
띄우는 것이 개발 요청으로 올라가 있다. 상수 이름도 `T_ASPECT_LONG` → **`T_ELONGATION_LONG`**,
`T_ASPECT_LONG_FULL` → **`T_ELONGATION_LONG_FULL`** 로 바꿨다(둘 다 `Balance.gd` 안에서만 쓰인다).

### [S3] 잡픽셀로 태그 고르기 — 지표를 노이즈 제거 후에 잰다 (2026-08-01, **해결**)

`_count_blobs` 는 4픽셀 미만 덩어리를 노이즈로 버리는데 `fill_ratio`·`aspect`·`complexity` 는
버린 픽셀까지 포함한 바운딩박스로 계산했다. 그래서 구석에 점 몇 개만 찍으면 그림은 그대로인데
태그가 바뀌었다(QA 1차부터 **네 번** 미이행). → **다섯 지표가 전부 같은(노이즈 제거된) 그림을 본다.**

⚠ **이 조치는 [D-1] 과 세트다. 따로 넣으면 안 된다.**
바운딩박스는 잡픽셀에 비례해 커지지만 2차 모멘트는 **거리의 제곱**으로 반응하므로,
노이즈 제거 없이 신장도만 넣으면 **구멍이 오히려 넓어진다.**
[실행] 원 r3 + 구석 1픽셀 2개 → 노이즈 제거 전 신장도 **3.55(길쭉함 오판)** / 제거 후 **1.00(둥긂)**.

[실행] 검증 — 원에 잡픽셀을 붙인 7가지 케이스가 **전부 둥긂 유지**:
좌우끝 1px · 대각구석 1px 2개 · 네 구석 1px · 좌우끝 3×1(3px) · 작은 원(r3)에 붙인 경우까지.
(종전에는 좌우끝 1px → **길쭉함**, 네 구석 1px → **뾰족함** 이었다)

🔴 **남은 구멍 하나 — 그리고 2026-08-01 QA 5차에서 방향이 뒤집혔다.**
**구석에 2×2 점(4픽셀)을 찍으면 여전히 흩어짐이 된다.** 이건 바운딩박스가 아니라
`T_MIN_BLOB_PIXELS = 4` 라는 **값**의 문제다. 값을 올리면 흩어짐의
「덩어리 수 → 탄 수」 매핑을 다시 검산해야 해서 이번 개정에 넣지 않았다.
> ⚠ **이것을 「태그가 망가질 수 있다」로만 읽지 마라.**
> [QA 5차 실측] 별5(뾰족함, 정조준 **26.222**) + 구석 2×2 → 흩어짐(정조준 **41.235**)
> = **1.57배 강화**. **이득이 되는 방향이 있다.** 게다가 원뿐 아니라 **모든 그림**에
> 적용된다(막대·큰 원 전부 흩어짐이 된다). 상세는 아래 「알려진 판정 구멍」 **1순위 항목**.
> **다음 세션 1순위이며, 4단계(봇 대전) 전 필수로 보는 유일한 항목이다.**

### ⚠ 알려진 판정 구멍 (개발 반영 대기)

> 항목 순서 = **다음 세션 우선순위 순**이다. 1순위가 가장 위.
> **「2순위 묶음 ①②③」은 한 덩어리로 처리한다** — 유저에게는 셋 다 「그림을 조금 바꿨는데
> 태그가 튄다」로 보이고 원인을 구별하지 못한다. 근거와 전체 순서는 아래
> 「다음 세션 우선순위」 절.

- **[🔴 1순위 · 2026-08-01 갱신 · 방향이 뒤집혔다] 구석에 4픽셀만 찍으면
  「태그가 망가지는」 것이 아니라 「강해진다」. `T_MIN_BLOB_PIXELS = 4` 구멍.**

  🔴 **이 항목은 종전까지 「막혔다」 쪽으로 알려져 있었다. QA 5차가 방향을 뒤집었다.**
  1차부터 다섯 번 연속으로 남아 있던 구멍인데, 지금까지는 「태그를 마음대로 고를 수 있다」
  (= 대체로 손해거나 중립)였다. **이제는 명백한 이득이 되는 조합이 실측으로 나왔다.**

  **[QA 5차 3-C 실측]** 데미지 60 / 범위 50 · 0.4m 표적 81지점 정조준 평균:

  | 그림 | 태그 | 탄·퍼짐 | **정조준 평균 데미지** |
  |---|---|---|---|
  | 별 5갈래 | 뾰족함 | 5발 46° | **26.222** |
  | **별 5갈래 + 구석 2×2 (4픽셀)** | **흩어짐** | 3발 18° | **41.235** |

  → **1,024칸 중 4칸만 바꿔서 조준 데미지가 1.57배가 된다.**
  유효 명중 폭(2.8546)·쿨타임(7.697)은 똑같다 — **대원칙(폭 정규화)은 안 깨진다.**
  깨지는 것은 **「내가 그린 스킬로 싸운다」** 쪽이다.

  **[QA 5차 3-B 실측] 그리고 원에만 있는 구멍이 아니다 — 모든 그림에 적용된다.**

  | 원래 그림 | 원래 태그 | + 구석 2×2(4px) |
  |---|---|---|
  | 원 r6 / 원 r13 | 둥긂 | **흩어짐** |
  | 막대 22×3 | 길쭉함 | **흩어짐** |
  | 막대 26×2 45° | 길쭉함 | **흩어짐** |
  | 구석 L자 4px(8방향 연결) | – | **흩어짐** — 2×2 모양이 아니어도 4픽셀이면 된다 |

  덩어리 판정이 **판정 순서 1위**라 가장 먼저 걸린다. 그래서 **무엇을 그렸든 상관없이**
  구석에 점 4개를 찍으면 흩어짐이 된다.

  🔴 **결론 — 최적 플레이가 「뭘 그리든 구석에 점 4개」가 된다.** 이것은 이 게임의
  한 줄 요약(「내가 그린 스킬로 싸운다」)의 **정반대**다. 그림에 들인 정성이 보상받는
  축이 여기서 통째로 깎인다.

  **원인은 바운딩박스가 아니라 `T_MIN_BLOB_PIXELS = 4` 라는 값이다.**
  ([S3] 개정으로 지표는 전부 노이즈 제거 후 마스크를 보게 됐고, 1px·3px·네 구석·좌우 끝
  케이스는 **전부 막혔다** — QA 5차 3-A 에서 기준 그림과 지표가 완전히 동일함을 확인.
  남은 것은 「노이즈로 안 쳐주는 크기」 하나뿐이다.)

  → **처방 후보**: ① 문턱값 상향, ② **상대 하한**(최대 덩어리의 몇 % 미만인 덩어리는 무시),
  ③ 덩어리 수 대신 「유의미한 덩어리의 면적 분포」를 본다.
  어느 쪽이든 **흩어짐의 「덩어리 수 → 탄 수(3·5·7발)」 매핑을 다시 검산해야 한다.**
  그래서 이번 세션에 안 넣었다. **다음 세션 1순위.**

- **[신규 · 2026-08-01 · 🔴 2순위 묶음 ①] 문턱 2.5 근처 그림은 지금도 회전으로 태그가 갈린다.
  신장도 자체가 회전 불변이 아니다 — 범인은 지표가 아니라 32×32 래스터화다.**

  이 항목은 **6차 감사가 기획서를 반려하며 정정을 지시한 자리**다. 종전 판본은 「[D-1] 은
  길쭉함 축을 회전 불변으로 만들었다 … **길쭉함 여부는 안 흔들리는데**」라고 적었다.
  **틀렸다.** 같은 커밋의 `reports/developer.md` 가 정반대 데이터를 싣고 있었고
  (17×7 → 길쭉함 2/91 · 13×5 → 길쭉함 72/91), **감사 · 개발 · 기획 세 사람이 서로 다른
  래스터라이저로 독립 측정한 값이 전부 같은 말을 한다.**

  **왜 흔들리나.** 신장도는 *같은 점 집합*을 다른 축에서 볼 때만 불변이다. 유저가 그림을
  기울여 그리면 32×32 격자 위에서 **점 집합 자체가 달라진다**(계단 모양 재배치). 그래서
  값이 각도마다 움직인다. 종횡비처럼 1.00 으로 무너지지 않을 뿐이다.

  [실행] 0~90° 를 1° 씩 91각도. **감사 측정과 기획 재측정이 소수 셋째 자리까지 같다.**

  | 막대(길이×두께) | 비율 | 신장도 범위 | 변동폭 | 91각도 태그 분포 |
  |---|---|---|---|---|
  | 26×2 | 13.00 | 11.157~13.047 | 16.9% | 길쭉함 91 **(안정)** |
  | 11×4 | 2.75 | 2.499~3.000 | 20.0% | 길쭉함 89 · **둥긂 2** |
  | 13×5 | 2.60 | 2.333~2.690 | 15.3% | 길쭉함 83 · **둥긂 4 · 뾰족함 4** |
  | **10×4** | **2.50** | 2.349~2.951 | 25.6% | **길쭉함 43 · 둥긂 44 · 뾰족함 4** |
  | **15×6** | **2.50** | 2.321~2.667 | 14.9% | **길쭉함 22 · 둥긂 36 · 뾰족함 33** |
  | 9×4 | 2.25 | 2.029~2.554 | 25.9% | 둥긂 60 · 뾰족함 26 · **길쭉함 5** |
  | 17×7 | 2.43 | 2.250~2.763 | 22.8% | 둥긂 52 · 뾰족함 36 · **길쭉함 3** |

  (막대를 픽셀로 옮기는 방식이 조금 달라 마지막 줄만 한 칸 차이가 난다 — 개발은 17×7 을
  「둥긂 38 · 길쭉함 2 · 뾰족함 51」로 쟀다. **결론은 같다.** 다른 줄은 소수 셋째 자리까지 일치.)

  **구멍의 크기 [실행]** — 길이 6~30 × 두께 2~12 의 막대 **254 조합 × 91각도** 전수:
  - **21 조합(8.3%)** 이 각도에 따라 **길쭉함이 걸렸다 말았다** 한다.
  - 77 조합(30.3%)은 길쭉함은 안 나오고 **둥긂↔뾰족함만** 갈린다(아래 복잡도 항목).
  - 156 조합은 91각도 전부 같은 태그다. **길쭉함이 안 갈린 131 조합의 신장도 최솟값은
    전부 2.500 이상**이었다 — 즉 **「최악의 각도에서도 값이 문턱 위」인 그림만 안전하다.**

  **[QA 5차 실측] 같은 전수를 QA 가 독립 래스터라이저로 다시 쟀다** (막대 **240 조합** ×
  0~90° 0.5°). 표본이 조금 달라 조합 수는 다르지만 **결론과 띠 위치가 같다**:

  | 결과 | 조합 수 |
  |---|---|
  | 전 각도에서 태그가 하나 (안 흔들림) | **173** |
  | **길쭉함 여부가 각도로 갈림** | **17** |
  | 길쭉함은 안 갈리는데 둥긂↔뾰족함이 갈림 | **50** |

  🔴 **종전 판본이 「길쭉함 여부는 안 흔들리는데 둥긂↔뾰족함만 갈리는 조합 50개」라고
  적은 것을 여기서 정정한다.** **「50」이라는 수 자체는 정확하다.** 그러나 그 앞의
  「길쭉함 여부는 안 흔들리는데」가 **틀렸다** — 그 50조합 말고 **길쭉함 여부가 갈리는
  17조합이 따로 있다.** 합치면 **67조합(240 중 28%)** 이 각도에 따라 태그가 바뀐다.
  그 17조합은 전부 **비율 2.25 ~ 2.80** 이고
  (10×4·15×6·20×8·25×10·8×3·7×3·18×7·13×5·9×4·17×7·23×9·11×4·14×5·19×8·24×9·26×10·16×7),
  **합성 막대만의 이야기가 아니다 — 타원·손떨림 획 같은 실제로 그리는 그림에서도 갈린다**:

  | 실제로 그릴 법한 그림 | 비율 | 91각도 태그 분포 | 신장도 범위 |
  |---|---|---|---|
  | **타원 10×4** | 2.50 | 길쭉함 **33** · 둥긂 **58** | 2.206~2.891 (31.1%) |
  | **타원 12×5** | 2.40 | 길쭉함 **8** · 둥긂 **82** | 2.020~2.884 (42.8%) |
  | 타원 20×8 | 2.50 | 길쭉함 38 · 둥긂 52 | 2.399~2.686 (12.0%) |
  | 손떨림 획 8×4 | 2.00 | 길쭉함 1 · 둥긂 18 (19각도) | 2.14~2.53 |
  | 손떨림 획 10×5 | 2.00 | 길쭉함 4 · 둥긂 15 (19각도) | 2.12~2.57 |

  **「갈리는 띠」의 폭 — 정정.** 종전 판본은 **비율 2.25~2.80** 이라고 적었다.
  그것은 **길쭉함 여부가 갈리는 조합**의 폭이고, **뾰족함까지 포함한 전체 흔들림 띠는
  비율 대략 1.9 ~ 2.8 로 더 넓다**(QA 5차 실측. 막대·타원·손떨림 획 모두).
  아래 복잡도 항목의 8×4(비율 2.00)·11×5(2.20)·16×8(2.00) 이 그 아래쪽 절반이다.

  **손해가 얼마인가.** 폭 정규화 덕에 **기대 데미지·유효 명중 폭·쿨타임·발동·속도·reach 는
  소수점 끝까지 안 변한다**(QA 5차 2-B 실측). 대원칙은 한 자리도 안 깨진다. 변하는 것은
  **조준해서 맞혔을 때의 데미지** 하나다.
  - ✅ **길쭉함 ↔ 둥긂 전환은 실측상 완전히 무해하다** — 막대 10×4 를 0°(길쭉함)에서
    7°(둥긂)로 돌려도 **정조준 평균이 둘 다 60.000** 이다.
  - 🔴 **아픈 것은 「뾰족함으로 넘어가는 것」 하나뿐이다** — 같은 그림이 뾰족함이 되면
    **정조준 평균 60.000 → 30.864 (1.94배)**, 한 방 데미지 **60 → 20 (1/3)**.
  [D-1] 이 고친 것과 정확히 같은 종류의 손해가, 좁아진 띠 안에 그대로 남아 있다.
  「길이 10 두께 4 짜리 몽둥이」는 유저가 흔히 그리는 그림이다.

  **흔들림 폭을 정하는 것은 비율이 아니라 「얇은 축의 픽셀 수」다** [실행] (비율 2.5:1 고정,
  두께별 변동폭): 두께 2 → **66.0%** · 4 → 25.6% · 6 → 14.9% · 8 → 8.1% · 10 → 11.6% · 12 → **3.6%**.
  → **작게 그릴수록 심하다.** 32×32 안에서 5×2 짜리 막대는 사실상 태그를 못 정한다.

  → 처방 후보와 각각의 대가는 아래 「래스터화 흔들림 — 처방 후보」 절. **이번 세션에는
  아무것도 구현하지 않았다.** 반쯤 검산하고 넣는 것이 이 프로젝트에서 반복해서 사고를 냈다.

- **[신규 · 2026-08-01 · 🔴 2순위 묶음 ② — 위 항목과 한 덩어리] 복잡도·채움률도 회전에
  안 흔들리는 값이 아니다. 회전 흔들림 중 실제로 세기에 닿는 것은 이쪽 하나뿐이다.**
  **둥긂↔뾰족함**을 가르는 복잡도는 4방향 둘레로 재기 때문에 대각선 계단에서 둘레가 부풀어
  값이 커진다. (위 항목과 **원인이 같다** — 둘 다 래스터화다. 다만 이쪽은 지표 정의부터
  방향 의존적이라 더 나쁘다.)

  🔴 **범위 정정 (2026-08-01, QA 5차 2-A 실측).** 종전 판본은
  「~~막대 235조합 중 **50개** · 8×4 는 181각도 중 21각도(12%) · 전부 짧고 뭉툭한 막대~~」
  라고 적었다. **사실보다 작았다.** 「뾰족함이 되는 각도가 있는가」로 다시 세면:

  | 결과 | 조합 수 (막대 240조합 × 0~90° 0.5°) |
  |---|---|
  | 뾰족함이 한 각도도 안 나옴 (안전) | **177** |
  | 전 각도가 뾰족함 (일관됨) | 0 |
  | 🔴 **각도에 따라 뾰족함이 되었다 말았다 한다** | **63 (26.3%)** |

  | 막대 | 비율 | 길쭉함 | 둥긂 | 뾰족함 | **뾰족함 각도 비율** |
  |---|---|---|---|---|---|
  | **19×8** | 2.38 | 2 | 100 | 79 | **43.6%** ← 최대 |
  | **24×10** · 23×10 | 2.40 / 2.30 | 0 | 106 | 75 | **41.4%** |
  | 17×7 | 2.43 | 5 | 102 | 74 | 40.9% |
  | 21×9 | 2.33 | 0 | 108 | 73 | 40.3% |
  | 15×6 | 2.50 | 42 | 68 | 71 | 39.2% |
  | 25×10 | 2.50 | 44 | 72 | 65 | 35.9% |
  | 11×5 | 2.20 | 0 | 121 | 60 | 33.1% |
  | 16×8 | 2.00 | 0 | 146 | 35 | 19.3% |

  - **50 → 63조합(26.3%)**, **최대 12% → 43.6%**(19×8).
  - **「짧고 뭉툭한 막대뿐」도 틀렸다** — **19×8 · 24×10 처럼 32×32 안에서 꽤 큰 막대**까지
    들어온다. 「길이 24 두께 10 짜리 몽둥이」는 캔버스를 넉넉히 쓴 흔한 그림이다.
  - 위 254조합 전수에서도 **77 조합(30.3%)** 이 같은 방식으로 갈린다(같은 현상, 다른 표본).
  - 흔들림 폭은 [D-1] 전(55~73%)보다 좁지만 0 은 아니다.

  **심각도 [QA 5차 2-B 실측]** — 데미지 60 / 범위 50, 0.4m 표적을 1~21m 에 0.25m 씩 81지점:

  | 그림 | 태그 | 탄·퍼짐 | **정조준 평균** | 유효 명중 폭 | 쿨타임 |
  |---|---|---|---|---|---|
  | 막대 **24×10 @0°** | 둥긂 | 1발 | **60.000** | 2.8546 | 7.697 |
  | 막대 **24×10 @26°** | **뾰족함** | 3발 32° | **30.864** | 2.8546 | 7.697 |
  | 막대 10×4 @0° | 길쭉함 | 1발 | **60.000** | 2.8546 | 7.697 |
  | 막대 10×4 @39° | **뾰족함** | 3발 33° | **30.864** | 2.8546 | 7.697 |

  → **같은 그림을 26° 돌리면 정조준 데미지가 1.94배 오간다. 한 방 60 → 20.**
  유효 명중 폭·쿨타임·발동·속도·reach 는 **소수점 끝까지 동일** — 대원칙은 안 깨지고
  **조준 데미지만 깨진다.** 위 항목의 길쭉함↔둥긂 전환(60.000 → 60.000, 무해)과 달리
  **이쪽은 실제로 아프다.**

  → 고치려면 둘레를 대각선 이웃까지 √2 로 세는 식으로 바꿔야 하는데, 그러면
  **`T_COMPLEXITY_SHARP = 2.5` 와 뾰족함 미세 파라미터(각도 32~46°)를 통째로 재검산**해야 한다.
  5차 감사가 방금 통과시킨 「뾰족함 ↔ 흩어짐 정체성 분리」도 다시 재야 한다.
  이번 묶음에 넣지 않은 이유가 그것이다 — **반쯤 검산하고 넣는 것이 이 프로젝트에서 반복해서
  사고를 냈다.** 데이터를 남겨 두고 다음 세션에 **2순위 묶음 ①·③ 과 함께** 올린다.

- **[신규 · 2026-08-01 · 🔴 2순위 묶음 ③] 1픽셀 「꼬리」는 노이즈 제거가 못 막는다 —
  복잡도 지표 자체의 성질이다.**

  `_denoised` 는 **끊어진** 작은 덩어리만 지운다. 본체에 **이어진** 가는 획은 1픽셀이어도
  남는다. 복잡도는 둘레²/면적이라 **가는 돌기에 2차로 민감**하다.

  | 그림 [QA 5차 3-D 실측] | 태그 | 복잡도 | 신장도 |
  |---|---|---|---|
  | 원 r6 (기준) | 둥긂 | 1.64 | 1.00 |
  | 원 r6 + 오른쪽 **1픽셀 꼬리 15칸** | **뾰족함** | **3.02** | 1.48 |
  | 원 r6 + 좌우 1픽셀 꼬리 | **뾰족함** | 4.67 | 1.88 |
  | 원 r6 + 대각 1픽셀 꼬리 | **뾰족함** | 5.43 | 1.85 |
  | 원 r4 + 좌우 1픽셀 꼬리 | **길쭉함** | 6.70 | **3.61** |

  - 획 하나로 **둥긂(정조준 60) → 뾰족함(정조준 ~31)** 이 된다. 방향은 **손해 쪽**이지만,
    유저는 「원에 선 하나 그었을 뿐인데 왜 부채꼴이 되지?」를 알 수 없다.
  - 🔴 **[S3] 의 잔여가 아니다.** `_denoised` 로는 **원리적으로 못 막는다** — 이어진 획이라
    작은 덩어리가 아니기 때문이다. **복잡도 지표 자체의 성질**이라 고치려면 지표를 손봐야 하고,
    그것은 바로 위 **2순위 묶음 ②**(둘레 √2 재검산)와 **같은 한 묶음**이다. **기획 판단 항목.**

- ~~**[신규] `T_MIN_BLOB_PIXELS = 4` 는 「구석에 2×2 점 찍기」를 못 막는다.**~~
  → **이 목록의 1순위 항목으로 올렸다 (2026-08-01).** 그대로 남아 있을 뿐 아니라
  **이득이 되는 방향이 있다**는 것이 QA 5차에서 처음 확인됐기 때문이다. 맨 위 항목 참조.
- **[신규 · 2026-08-01 · 우선순위 낮음(표시 문제)] 3픽셀 이하만 찍은 그림은 지표가 통째로 사라진다.**
  [S3] 로 지표를 「노이즈 제거 후」에 재게 되면서 생긴 결과다. [실행] 1픽셀 점 3개만 찍으면
  `filled` **3 → 0** · 덩어리 0 · 태그 **둥긂**(폴백)이 되고, 제작창은
  「채움 0.00 · 신장 1.00 · 종횡 1.00 · 복잡 1.00 · 덩어리 0」을 띄우는데 **화면에는 점이 보인다.**
  **거짓말은 아니다**(지표의 정의가 바뀐 것이다) 그러나 유저는 이유를 알 수 없다.
  전투 수치에는 영향이 없다(둥긂 폴백 · 폭 정규화는 그대로).
  → **고칠 것은 판정이 아니라 표시다.** 제작창에 「잡픽셀(4픽셀 미만 덩어리)은 판정에서 뺐다」
  한 줄을 띄우도록 개발 요청([D-16])에 올렸다. 6차 감사 ⚠5.
- **[정정 · 2026-08-01] ~~대각선으로 길쭉하게 그리면 길쭉함이 안 나온다.~~**
  → ⚠ **명확히 길쭉한 그림에 한해 해결(2026-08-01, [D-1]).** 신장도가 최악의 각도에서도
  문턱 2.5 위에 있는 그림(실측: 안 갈린 131 조합 전부 · QA 표본으로는 173 조합)은
  기울여도 길쭉함을 유지한다. 성과는 진짜다 — **막대 8종 오판 908 → 0, 손떨림 획 177 → 0**
  (QA 5차가 독립 래스터라이저로 재현). **단, 그 표본은 전부 비율 4.4 이상이다.**
  **문턱 근처는 아직 갈린다 — 길쭉함 여부가 갈리는 띠는 비율 2.25~2.80, 뾰족함까지
  포함한 전체 흔들림 띠는 비율 대략 1.9~2.8 이다.** 위 회전 항목 참조.
  **「해결」로 읽지 마라 — 「명확히 길쭉한 그림에 한해 해결」이다.**
  (종전 판본은 이 줄에 취소선 + 「✅ 해결」만 적혀 있었다. 6차 감사 차단 사유.)
- ~~**대각선 점선은 덩어리 수가 0으로 세어진다.**~~ → ✅ **해결(2026-07-31, [D-2]).**
  `Balance._count_blobs` 의 연결 판정을 4방향 → **8방향**으로 바꿨다. 같은 원인으로
  「별을 그렸는데 산탄이 나가는」 문제도 함께 사라졌다(별 192 조합 오판 46 → 0).
  상세는 위 「[D-2] 연결 판정 4방향 → 8방향」 절.

### 래스터화 흔들림 — 처방 후보와 각각의 대가 (2026-08-01, 밸런스 담당 · **미구현**)

**이번 세션에는 한 줄도 구현하지 않았다.** 다음 세션이 고를 수 있게 후보와 대가만 적는다.
`Balance.gd` 는 6차 감사가 「되돌릴 것이 하나도 없다」고 통과시켰고, QA 5차가 재고 있는
판본이라 지금 건드리면 QA 가 다른 판본을 재게 된다.

**먼저 못박을 것 — 문턱이 있는 한 「갈리는 띠」는 0 이 안 된다.**
흔들림 폭이 ±13% 든 ±2% 든, 값이 그 폭 안에서 문턱을 걸치는 그림은 반드시 존재한다.
실측이 그렇다: 두께 12 짜리(변동 3.6%) 막대도 비율이 정확히 2.5 면 갈린다.
그러니 목표는 **「없앤다」가 아니라 「띠를 얼마나 좁히고, 유저에게 얼마나 보이게 하나」**다.
이 프레임을 안 잡으면 다음 세션이 또 「해결했다」고 쓰게 된다.

| # | 후보 | 얻는 것 (실측) | 대가 | 권고 |
|---|---|---|---|---|
| 1 | **제작창에 「경계 게이지」** — 신장도 값 + 문턱 + **흔들림 띠**를 같이 그린다. 「이 그림은 기울이면 태그가 바뀝니다」 경고 | 흔들림을 **없애지는 못하지만 유저가 예측 가능**해진다. 대원칙 「내가 그린 대로 나간다」의 회복 경로 중 **유일하게 전투 수치를 하나도 안 건드리는 것** | **전투 수치 변경 0.** UI 작업만. 이미 「나중 확장 여지」 우선순위 1번에 있는 항목과 같다 | ✅ **1순위.** 가장 싸고 위험 0 |
| 2 | **문턱 근처 데드존 / 히스테리시스** | (측정 안 함 — 아래 이유로 기각 권고) | ⛔ **이 문제에 안 듣는다.** 유저는 회전 버튼을 누르는 게 아니라 **기울여서 새로 그린다.** 「직전 상태」가 없으므로 히스테리시스가 붙을 자리가 없다. 억지로 편집 이력에 붙이면 **같은 그림이 이력에 따라 두 태그를 갖게 된다** — 「화면이 실제와 다른 값을 띄운다」 계열 사고(이 프로젝트 5회 재발). 데드존은 문턱을 **옮길 뿐** 새 문턱마다 새 띠가 생긴다 | ⛔ 전투 판정에는 **쓰지 마라.** 다만 **그림판에서 픽셀을 하나씩 찍는 동안 태그 표시가 깜빡이는 것**을 막는 **표시 전용 안정화**로는 쓸 수 있다 |
| 3 | **안티에일리어싱된 마스크** — 이진 마스크를 3×3 로 평활화해 가중치로 2차 모멘트를 잰다 | [실행] 변동폭 25.6% → **19.6%**(10×4) · 16.9% → **8.3%**(26×2). **줄기는 준다** | 🔴 **문턱 2.5 의 근거였던 항등식이 깨진다.** [실행] 꽉 찬 직사각형에서 16×8 → **1.915**(2.000 이 아니다) · 24×3 → **5.861**(8.000 이 아니다) · 12×5 → 2.146. **편향이 크기마다 다르다**(20×8 → 2.381 · 25×10 → 2.421). 즉 「2.5:1 보다 길면 길쭉함」이라는 **뜻이 통째로 사라지고** 문턱을 그림 크기별로 다시 잡아야 한다. 게다가 효과는 반쪽이다 | ⚠ **대가 > 이득.** 하려면 문턱 재확정 + 도형 40종 회귀 + 기본 스킬 4개 재검산이 **한 묶음**이다 |
| 4 | **지표 평활화(회전 평균)** — 마스크를 여러 각도로 돌려 신장도의 중앙값을 쓴다 | **미측정** | 회전 재래스터화가 **또 흔들림을 만든다**(같은 원인을 해법에 다시 넣는 꼴). 계산 비용 N배. 3번과 같은 계열의 하향 편향이 예상되므로 **항등식이 깨질 가능성이 높다** | ⚠ 3번을 재기 전에는 손대지 마라 |
| 5 | **격자 해상도** — 32×32 → 64×64 | 🔴 **이것이 원인을 직접 없애는 유일한 후보다.** [실행] 같은 그림(비율 2.5:1)의 변동폭: 32격자 25.6% → 64격자 **8.1%** → 128격자 **1.6%**. 얇은 축 픽셀 수가 흔들림을 정한다는 실측과 정확히 같은 말 | 🔴 **큰 공사다.** 32×32 는 파이프라인 1번에 박혀 있고 저장 포맷 · `SkillCanvas` · 스프라이트 · `Balance.GRID` 가 전부 걸린다. **그리고 그리는 맛이 달라진다**(32 칸은 「한 칸 한 칸 고르는」 손맛이다) | ⚠ **사용자 확인 필요.** 기획 담당이 임의로 못 정한다 |
| 6 | **태그 절벽을 없앤다** — 문턱 근처에서 히트박스를 보간 | 절벽 자체가 사라져 **갈려도 손해가 1/3 이 아니다.** 근본 해법 | 🔴 **태그 4종 구조는 사용자 확정 사항**이다. 대원칙 검산(폭 정규화 · 예산 곡선 · 홀수 탄)을 **전부 다시** 돌려야 한다 | ⚠ 4단계(QWER + 봇 대전) 이후 재상정 |

**기획 담당의 권고 순서**: 1번(공짜) → 복잡도 8방향 둘레 재검산 → 5번을 사용자에게 물어본다.
**3·4번은 항등식을 깨므로 문턱 재확정과 한 묶음이 아니면 금지.**
(전체 순서는 바로 아래 「다음 세션 우선순위」에 통합돼 있다.)

### 다음 세션 우선순위 (2026-08-01 확정 · 밸런스 담당 · QA 5차 의견 반영)

**바뀐 점 두 가지를 먼저 못박는다.**
1. **회전 문제는 「4단계 전 필수」에서 「다음 세션 상위」로 내린다.** QA 4차가 「필수」로 올렸던
   근거가 실측으로 약해졌다 — 빈도 **55~73% → 최대 43.6%**, 대상이 「모든 길쭉한 막대」에서
   「비율 1.9~2.8 인 뭉툭한 그림」으로 좁혀졌고, **탈출구가 생겼다**(비율 2.9 이상은 100% 안정).
   손해 크기(1.94배)만 그대로다. QA 5차 2-C 의 판단과 같다.
2. **대신 [S3] 4픽셀이 「필수」 자리로 올라온다.** 방향이 「손해/중립」에서 **「이득」**으로
   뒤집혔기 때문이다. 손해 나는 구멍은 유저가 피하지만, **이득 나는 구멍은 유저가 찾아온다.**

| 순위 | 항목 | 왜 이 자리인가 | 4단계 전 필수? |
|---|---|---|---|
| **1** | **[S3] 4픽셀 덩어리 하한** — `T_MIN_BLOB_PIXELS` 상향 또는 상대 하한 | **유일하게 이득이 되는 익스플로잇**(1.57배). 최적 플레이가 「뭘 그리든 구석에 점 4개」가 되면 게임 한 줄 요약이 무너진다. 모든 그림에 적용되고, 1차부터 다섯 번 미이행 | **예** |
| **2** | **회전 흔들림 묶음** = [D-1] 잔여(길쭉함 문턱) **+ 복잡도 흔들림 + 1픽셀 꼬리** | QA 권고대로 **하나로 묶는다.** 같은 그림(비율 1.9~2.8)·같은 각도 축·같은 결과(뾰족함 = 조준 데미지 1/3)라 **유저는 원인을 구별하지 못한다.** 나눠 매기면 하나도 안 고쳐진 것처럼 보인다 | 아니오 |
| 2-a | └ **제작창 경계 게이지** (처방 1번) | **전투 수치 변경 0 · 위험 0 · 가장 쌈.** 흔들림을 없애진 못해도 **유저가 예측**하게 된다. 「나중 확장 여지」 1순위와 같은 항목이라 어차피 할 일이다 | – |
| 2-b | └ **복잡도 둘레를 대각 √2 로 재검산** | 회전 흔들림 중 **실제로 아픈 것은 뾰족함 전환 하나뿐**(길쭉함↔둥긂은 60.000 → 60.000, 무해). 즉 **여기만 고쳐도 손해의 대부분이 사라진다.** 1픽셀 꼬리도 같은 지표라 같이 처리된다 | 아니오 |
| 2-c | └ (2-b 와 한 묶음) `T_COMPLEXITY_SHARP 2.5` · 뾰족함 미세 파라미터(32~46°) · 뾰족함↔흩어짐 정체성 **재검산** | 둘레 정의를 바꾸면 **반드시 같이** 다시 재야 한다. **반쯤 검산하고 넣는 것이 이 프로젝트에서 반복해서 사고를 냈다** | – |
| **3** | **표시 항목 2건** — [D-16] 「4픽셀 미만 덩어리는 판정에서 뺐다」 한 줄 · [N15] 흩어짐 5·7발 두 번째 낙폭 표시 | 전투 수치 0. 「화면이 거짓말한다」 계열 예방. 싸다 | 아니오 |
| **4** | **[S6] 슬롯 전환 확인창 실제 플레이 확인** | **다섯 세션째 아무도 못 눌러봤다.** 헤드리스로 불가 — 사용자/개발이 직접 켜야 한다 | – |
| **5** | **격자 32×32 → 64×64** (처방 5번) | 회전 흔들림의 **원인을 직접 없애는 유일한 후보**(변동폭 25.6% → 8.1%). 그러나 저장 포맷·`SkillCanvas`·스프라이트가 다 걸리고 **그리는 손맛이 달라진다** | **⚠ 사용자 확인 필요** |
| **6** | **태그 절벽 보간** (처방 6번) · **N13 길쭉함 정체성** | 절벽이 사라지면 갈려도 손해가 1/3 이 아니게 되는 근본 해법. 그러나 **태그 4종 구조는 사용자 확정 사항**이고 대원칙 검산을 전부 다시 돌려야 한다 | **4단계 이후 재상정** |

**1번과 2번을 붙여서 한 세션에 처리하기를 권한다.** 유저에게는 둘 다
「그림을 조금 바꿨는데 태그가 튄다」로 보이고, **원인을 구별하지 못한다**(QA 5차 8장 4번).
다만 **작업은 1번 먼저** — 1번은 덩어리 판정(판정 순서 1위)이라 2번의 측정 기준을 흔든다.

### 나중 확장 여지 — 그림 커스텀을 더 재미있게 (2026-07-31 밸런스 담당 설계)
픽셀 모양 그대로를 히트박스로 쓰는 완전 B안은 채택하지 않는다.

**지금의 문제**: 32×32 를 공들여 그려도 결과가 태그 4개 중 하나로 환원된다.
그리는 노력과 돌아오는 변화가 안 맞아서, 그림이 "골라 찍는 버튼 4개"처럼 느껴진다.

**확장의 안전 규칙 3개 (2026-07-31 개정 — 1번이 바뀌었다)**
아래 규칙을 지키는 한 무엇을 늘려도 대원칙이 안 깨진다.
1. **폭 보존.** 새 파라미터가 어떤 값이 나와도 `Balance.effective_width(box)` =
   `Balance.effective_width_for(total_area)` 여야 한다.
   (~~종전: 판정 면적의 합 = `total_area`~~ — 면적 보존은 기대 데미지를 보존하지 못했다)
2. **제로섬.** 이득을 주는 축에는 반드시 짝이 되는 손해가 붙고, 둘의 곱/합이 불변이어야 한다.
   (예: 탄을 늘리면 한 발 데미지가 나뉜다 — 탄 크기는 이제 안 줄어든다)
3. **정규화된 지표만 쓴다.** 크기·위치에 안 흔들리는 비율값만 읽는다.
   "크게 그리면 세진다"가 성립하는 순간 그림이 세기를 정하게 되어 대원칙 위반이다.
4. **(신설) 진행축 길이는 상한을 건다.** 폭이 고정되면 길이가 유일한 자유 축이 되고,
   길이는 체류 시간을 통해 움직이는 표적에 대한 관용을 키운다. 상한 없이 열지 않는다.

| 우선순위 | 안 | 그림에서 읽는 것 | 형평성 근거 |
|---|---|---|---|
| 1 | **에디터 실시간 미리보기 + 태그 경계 게이지** | (없음 — 표시만) | 전투 수치를 **하나도 안 바꾼다.** 위험 0 |
| 2 | **태그별 미세 파라미터 4종** | 신장도·복잡도·덩어리수·채움률 | 전부 면적 보존 + 제로섬 (아래 상세) |
| 3 | **다색 그림(팔레트 2~4색)** | (없음 — 겉모습 전용) | 마스크 값이 0인지 아닌지만 판정에 쓴다. 색은 세기와 무관 |
| 4 | 새 태그(설치·회전) | 무게중심 위치·회전대칭성 | 새 메커니즘이라 **사용자 확인 필요** |
| 5 | 복합 태그(길쭉함+흩어짐 = 연속 관통) | 조합 | 경우의 수가 폭발한다. 4단계 이후 재검토 |

#### 우선순위 2 상세 — 태그별 미세 파라미터 (2026-07-31 개정판)
수치는 `Balance.gd` 에 상수로 확정돼 있고, `hitbox_for(tag, area, distance, m)` 의
선택 인자 `m`(그림 지표)에 **로직까지 들어가 있다.** 개발이 지표를 넘기기만 하면 켜진다.

> **⛔ 옛 판본 정정 이력** — 이 표는 두 번 틀렸다가 고쳐졌다.
> (1) 「면적 보존 = 제로섬」이라 적었으나 면적이 보존돼도 기대 데미지는 1/√N 로 깎였다.
> (2) 그래서 각도 외 전 항목을 배선 금지했었다.
> **폭 정규화 개정으로 (1)의 원인이 사라져 탄 수가 해금됐다.** 아래는 개정 후 기준이다.

| 태그 | 읽는 지표 | 바뀌는 것 | 왜 세기가 안 변하나 | 악용 시나리오와 차단 근거 | 배선 |
|---|---|---|---|---|---|
| 흩어짐 | 덩어리 수 | 탄 수 **3·5·7발**(덩어리 수를 홀수로 올린다) | 탄 반경은 폭 정규화로 **N 과 무관하게 고정**. 한 발 데미지만 D/N 로 나뉘어 합이 불변 | *"덩어리를 많이 그리면 이득?"* → 기대 데미지가 N 과 무관함이 닫힌 식과 QA 실측으로 증명됐다. 많이 그리면 낙폭이 잘게 쪼개질 뿐이다 | ✅ |
| 뾰족함 | 복잡도 2.5→12.0 | 퍼짐각 **32°→46°**, 탄 **3→5발** | 각도는 기대 데미지에 안 들어간다. 탄 수는 위와 같은 이유 | *"좁혀서 저격?"* → 사거리는 여전히 `범위` 값만 정한다. 좁히면 전탄 유지가 늘지만 좌우 밴드가 좁아져 빗맞힘에 약해진다(제로섬) | ✅ |
| 길쭉함 | 신장도 2.5→8.0 | 길이 1.8:1 → 3.0:1 (상한에 걸려 실제 2.0~2.3:1) | 폭이 고정이라 정지 표적 기대 데미지는 불변 | *"1픽셀 선으로 맵 관통?"* → **폭이 안 줄어들고**, 길이는 8.0m·사거리 30% 상한에 걸리며, 비행 거리에서 길이를 빼 **도달점이 사거리에 고정**된다. 3중으로 막힌다 | ⛔ 보류(4단계 뒤 재상정 — 체류 시간 2차 효과) |
| 둥긂 | 채움률 < 0.35 (속이 빈 그림) | 최대 2명 관통 | 총 데미지를 관통 수로 나눈다 | *"관통이 공짜 이득?"* → 한 명만 맞히면 데미지가 절반이라 **손해**다. 일직선에 두 명이 겹쳤을 때만 본전. 순수 제로섬 | ⛔ 사용자 확인 필요 |

관통(둥긂)은 새 메커니즘이라 **사용자 확인 필요**. 나머지 셋은 기존 프리셋의 파라미터
조정이라 「태그별 미세 파라미터를 그림에서 더 뽑는」 확장 범위 안이다.

## 결정해야 할 빈칸
1. ~~그림=판정 vs 겉모습~~ → **하이브리드로 결정 (2026-07-31)**
2. 스킬 종류 범위 3. 자원 시스템 4. 캐릭터 차별화 5. 승리 조건/리스폰 6. 부정 스킬·부적절 그림 대응 7. 온라인 매칭(나중).

## ✅ 결정됨 — 엔진/시점: Godot 4, **3D 3인칭** (2026-07-31, 사용자 확정)
프로토타입부터 **3D 3인칭**으로 간다. 2D 탑다운 안은 폐기됐다.

- 움직임은 **멧챠 카멜레온풍** — 작고 날쌘 캐릭터가 톡톡 움직이는 느낌.
- 이동은 **방향키(화살표)** 전용. WASD 는 QWER 스킬과 손이 겹쳐 쓰지 않는다.
- 점프는 스페이스. 회피 수단은 이동 + 점프뿐이다.

### ✅ 결정됨 — 카메라: **고정 각도** (2026-07-31, 사용자 확정)
화면이 도는 일은 없다. 카메라는 정해진 각도로 캐릭터를 따라다니기만 한다.

- **마우스로 시점을 돌리지 않는다.** 마우스룩·마우스 커서 잠금(캡처) 없음.
- 각도는 캐릭터 뒤 위쪽 사선 고정(기본 yaw 0°, pitch -32°, 거리 7m).
  값은 `Player.gd` 의 `cam_yaw_deg` / `cam_pitch_deg` / `cam_distance` 로 조절.
- 화면이 안 도니 **방향키 ↑ = 언제나 화면 위쪽**. 맵 방향 감각이 흔들리지 않는다.
- 캐릭터는 몸만 진행 방향으로 돈다(카메라와 무관).
- 각도 자체를 바꾸고 싶으면 사용자에게 물어본다. 되돌아가는(회전) 카메라는 넣지 않는다.

### ⛔ 이 게임이 아닌 것 — 배그(슈터)가 아니다
기획서에서 "배그처럼"이라고 쓴 곳은 **자기장(안전구역 축소) 딱 하나**다.
조작·카메라·전투 감각은 배그와 무관하다. 아래는 넣지 않는다:

| 넣지 않는 것 | 이유 |
|---|---|
| 우클릭 조준(ADS)·어깨너머 줌 | 총 게임 문법. 이 게임은 마법봉 + QWER 스킬이다 |
| 조준 중 감속 | 위와 같음 |
| 스프린트(달리기 별도 키) | 기획에 없음. 속도는 한 종류 |
| 마우스로 시점 회전(마우스룩) | 카메라는 고정각으로 확정. 화면이 돌지 않는다 |

넣고 싶어지면 **먼저 사용자에게 물어보고 이 문서에 결정으로 남긴 뒤** 구현한다.

## 프로토타입 로드맵
1) 이동+점프 2) 고정 스킬 1개 발사 3) 스킬 에디터(수치판+예산공식) 4) QWER 4슬롯+봇 대전 5) 자기장 축소+5분 타이머 6) 재미 점검.

3단계는 **3a) 수치 슬라이더만 → 3b) 그림판 + 형태 태그 추출** 두 조각으로 쪼갠다.
3a가 돌아가고 재미가 확인된 뒤에 3b를 붙인다.

> **2026-07-31 사용자 판단으로 재미 확인 전에 3b 착수.**
> 3a→3b 게이트를 사용자가 명시적으로 면제했다("지금 붙여줘"). 규칙 3(「`✅ 결정됨`
> 항목은 사용자만 바꾼다」)에 따라 **승인 자체가 유효하며 규칙 위반이 아니다.**
> 3차 감사가 「면제 사실이 기준 문서에 없다」고 지적해(`reports/auditor.md` 3차
> ⚠확인 필요 1) 사용자 요청으로 이 줄을 남긴다. 기록만 남기는 것이고 순서 원칙 자체는
> 유지된다 — 이후 단계(4·5·6)는 로드맵 순서를 그대로 따른다.

> **2026-07-31 사용자 승인으로 3단계 감사 완료 전에 4단계 중 「QWER 4슬롯」만 착수.**
> 계기는 사용자가 직접 플레이하다 남긴 지적 **"WER 이 안써지긴해"** 다. 그 뒤 사용자가
> **"기획서에 한 줄 남기고 계속 진행해"** 로 명시 승인했다. 규칙 3(「`✅ 결정됨`·로드맵
> 순서는 사용자가 바꾼다」)에 따라 **승인 자체가 유효하며 규칙 위반이 아니다.**
> 4차 감사가 「기획서에 승인 기록이 한 글자도 없다」고 지적해(`reports/auditor.md` 4차
> 위반 1) 이 줄을 남긴다.
> - 착수한 것: **QWER 4슬롯뿐**(`Player.gd` 의 슬롯별 쿨타임·슬롯별 캐스팅).
>   입력 액션 `skill_w/e/r` 은 이전부터 커밋돼 있던 것이라 새로 만든 조작이 아니다.
> - **착수하지 않은 것: 봇 대전.** 4단계의 나머지 절반은 그대로 남아 있고,
>   `Dummy.gd`(허수아비)·`Arena.tscn` 은 손대지 않았다.
> - 「4단계에서 반드시 재검증할 것」(바로 아래)은 **봇 대전이 붙은 뒤에** 수행한다.
>   QWER 4슬롯만으로는 다중 표적·5v5 검증이 불가능하다.

#### 4단계에서 반드시 재검증할 것 (2026-07-31 폭 정규화 개정의 대가)
1. **다중 표적 위력** — 다탄 태그(흩어짐 5발·뾰족함 3발)는 판정 부피가 기준의 N 배라
   난전에서 여러 명에게 D/N 씩 흩뿌린다. 단발 태그는 한 명에게 D 를 몰아준다.
   한 명 상대 기대 데미지는 같지만 5v5 에서 같은지는 **아직 아무도 안 쟀다.**
2. **캡슐 체류 시간** — 길쭉함의 길이가 움직이는 표적에 주는 관용(범위 50 에서 +1.5m 상당).
   이 값이 크면 길이 상한을 더 조이거나 길쭉함의 폭을 깎아야 한다.
3. **대형 투사체 스폰 높이** — 다탄 태그의 탄이 커져 스폰 y 가 최대 2.03m 까지 오른다(QA N5).
